# תוכנית שינויים — מעבר למודל תמחור פר-קמפיין + Kill-Switch מחזורי

> **מסמך אפיון להעברה ל-Claude Code.** זהו לא תיעוד של מצב קיים אלא **הוראת-שינוי**: מה לבנות, מה להרוס, ולמה. המסמך מחייב את `spec.md` ואת ה-ROADMAP רק היכן שהוא סותר אותם במפורש (מסומן בכל מקום רלוונטי). בכל שאר המקומות — הארכיטקטורה הקיימת נשארת בתוקף.
>
> **הקשר קריטי למי שמבצע:** זהו שינוי במודל-התמחור, שהוא ההחלטה הכי-קשה-לשינוי-בדיעבד במערכת. באג כאן לא שובר פיצ'ר — הוא שורף כסף אמיתי של לקוח או גובה כסף שאסור לגבות. לכן כל שינוי כאן חייב להיות **פתרון-שורש ולא טלאי**: אם משהו במודל הישן סותר את החדש, הורסים אותו במפורש ובונים מחדש, לא עוטפים אותו בתנאי `if`. המסמך מסמן בכל מקום מה נהרס ומה מחליף אותו.
>
> עדכון אחרון: יולי 2026. מבוסס על הכרעות-מוצר סופיות של גיא (החלטות §0).

---

## 0. ההחלטות שהוכרעו (מקור-האמת לשינוי הזה)

לפני הפירוט הטכני, ריכוז כל ההכרעות הסופיות. אלה לא נתונות לפרשנות של מי שמבצע — הן הוכרעו ברמת המוצר. הפירוט הטכני בהמשך רק **מיישם** אותן.

**היחידה הכלכלית עוברת מפר-חשבון לפר-קמפיין — אבל המנוי נשאר פר-חשבון.** זה נשמע כמו סתירה, ולכן צריך לדייק: לקוח עדיין מחזיק **מנוי אחד** (רשומת `subscription` אחת, כמו היום). אבל **החיוב** כבר לא נגזר מה-tier של המנוי — הוא נגזר מ**סכום החיובים של הקמפיינים הפעילים**. לקוח עם 3 קמפיינים משלם על 3 קמפיינים, תחת מנוי-חשבון אחד. היחידה-החשבונאית היא החשבון; יחידת-החיוב היא הקמפיין.

**כל קמפיין רץ במחזור של 30 יום בדיוק, מעוגן ב-`Start_Timestamp` שלו-עצמו.** לא anniversary של החשבון, לא חודש קלנדרי, לא תאריך ה-signup. קמפיין שעלה ב-15 בחודש, המחזור שלו נגמר ב-15 בחודש הבא — ללא קשר לקמפיינים אחרים של אותו לקוח. **כל קמפיין הוא שעון עצמאי.**

**Trial חד-פעמי פר-יוזר, 10 יום, על הקמפיין הראשון בלבד, וכלול בתוך מחזור-ה-30.** זו הנקודה שהכי חשוב לא לטעות בה: ה-trial הוא **10 הימים הראשונים בתוך** מחזור-ה-30 של הקמפיין הראשון — לא תקופה נפרדת שקודמת למחזור. כלומר: הקמפיין הראשון עולה, ה-`Start_Timestamp` נקבע, ומאותו רגע רץ שעון של 30 יום. הימים 1–10 הם trial (בלי אשראי, בלי חיוב); ביום 9 שאלת-המשכיות; מי שאישר — משלים את 20 הימים הנותרים באותו מחזור וזה מתי שהחיוב נגבה. **ה-trial לא "מוסיף" ימים למחזור — הוא רק קובע שחלק מהמחזור חינמי.**

**אין אשראי מראש.** שינוי מהמצב הקיים. היום, אמצעי-תשלום הוא שער להקמת-קמפיין (Phase 2.6, "בלי אמצעי תשלום — אין הקמת קמפיין"). זה מתבטל. הקמפיין הראשון עולה ורץ 10 יום בלי שום אשראי במערכת. ביום 9 הלקוח מקבל מייל+וואטסאפ עם קישור לדשבורד → הזנת אשראי → אישור-המשך. **שער-האשראי עובר מ"לפני הקמפיין" ל"ביום 9 של הקמפיין הראשון".**

**קמפיין שני/שלישי — אין להם trial, והם משלמים מיום 1.** ה-trial הוא חד-פעמי-פר-יוזר, ונוצל על הקמפיין הראשון. כל קמפיין נוסף עולה ומתחיל לצבור חיוב מיום 1 של המחזור שלו. (האשראי כבר במערכת מהקמפיין הראשון — אין שער-אשראי חוזר.)

**קמפיין נוסף (2+) מקבל 15% הנחה קבועה.** לא מדורג ולא מצטבר — כל קמפיין מעבר לראשון מקבל בדיוק 15% הנחה על מחיר-הבסיס שלו, תמיד. הקמפיין הראשון תמיד במחיר-בסיס מלא. שני, שלישי, רביעי — כולם 15%. **על תוספת ה-VIP אין הנחה** (VIP מחויב במחיר מלא תמיד).

**חשבונית נפרדת פר-קמפיין.** לכל קמפיין מחזור-30 משלו, ולכן חשבונית-Green-Invoice משלו בכל חידוש. לקוח עם 3 קמפיינים מקבל 3 חשבוניות (כל אחת ביום-החידוש של אותו קמפיין), לא חשבונית מאוחדת.

**ה-tier (Basic/Premium) עובר להיות פר-קמפיין.** בעולם הישן זה היה או-או ברמת החשבון (הלקוח כולו Basic או כולו Premium). עכשיו כל קמפיין יכול tier משלו — לקוח יכול להריץ קמפיין אחד Basic וקמפיין שני Premium במקביל. זה משנה את הסכמה: ה-tier יורד מרמת ה-`subscription` לרמת ה-`campaign`.

**Kill-Switch פר-קמפיין בסוף המחזור.** בסוף מחזור-ה-30 של קמפיין (אם לא חודש), השרת מבצע PAUSE לקמפיין **במטא** — אבל הקמפיין **לא נעלם מהממשק**, הוא נשאר גלוי ללקוח כ-read-only עם אפשרות-חידוש. זו פעולה אוטונומית מסוכנת שדורשת הגנות (§5).

**נעילת-חשבון מלאה כשאין אף קמפיין פעיל.** ברגע שהקמפיין ה**אחרון** של לקוח נכבה בלי חידוש (אין לו שום קמפיין פעיל/משולם) — הלקוח מאבד גישה לממשק כולו. הדאטה שלו **נשמרת לצמיתות**: אם יחזור וישלם, הוא רואה את הכל.

---

## 1. למה המודל הישן לא מתאים — ומה בדיוק נשבר

לפני שבונים את החדש, צריך להבין מה במודל הקיים מפסיק להיות תקף. מי שמבצע חייב לזהות את זה כדי לא להשאיר שאריות של המנגנון הישן שיתנגשו עם החדש.

המודל הקיים בנוי סביב שלושה עוגנים שכולם משתנים עכשיו. הראשון הוא **עוגן-הזמן**: היום הכל נמדד ב-anniversary של החשבון — `current_period_start` ב-`subscriptions`, ו"החודש" מוגדר כ-`[current_period_start, +1 month)`. זה שירת מודל של מנוי-חשבון-אחד. במודל החדש, כל קמפיין הוא השעון של עצמו, ואין יותר עוגן-זמן ברמת החשבון. `current_period_start` מאבד את משמעותו המרכזית.

העוגן השני הוא **מכונת-המצבים של החיוב**. היום יש `trial → active → past_due → expired` עם חיוב-אוטומטי ביום 8 שמצליח או נכשל (זה מה ש-PR2 תיקן, וזה מה שכל ה-billing crons עושים). המודל החדש שובר את זה: אין יותר "חיוב אוטומטי ביום 8 שנכשל ומעביר ל-past_due". יש "שער-אשראי ביום 9 — הלקוח מזין אשראי ומאשר, או שהקמפיין נעצר". המצבים `past_due` ו-`expired` ברמת ה-subscription כמעט מתרוקנים מתוכן, כי אין יותר מחזור-חיוב-חשבון שנכשל. **החיוב עובר מרמת ה-subscription לרמת הקמפיין, והמצבים נעים איתו.**

העוגן השלישי הוא **שער-האשראי**. היום אמצעי-תשלום הוא תנאי-סף להקמת-קמפיין. במודל החדש הקמפיין רץ 10 יום בלי אשראי בכלל, והשער עובר לתוך המחזור (יום 9). זה הופך את כל ה-flow של Phase 2.6 — במקום "הזן אשראי כדי להקים", זה "הקם עכשיו, הזן אשראי ביום 9 אם אתה רוצה להמשיך".

**מה ששורד את השינוי:** ה-seam האגנוסטי של הסליקה (`billing.charge_saved_card` / `billing.py` מעל `pelecard.py`) נשאר שלם. זו בדיוק הנקודה שבגללה בנינו אותו — החלפת מתי-וכמה-מחייבים לא נוגעת ב*איך* מחייבים. גם החלטת מעבר-ה-PSP העתידית (פלאקארד → Grow) שורדת: ה-seam מגן, אז אפשר לדחות אותה לאחרי השינוי הזה בלי לצבור חוב. גם כל שכבת ה-Vault, ה-RLS, וה-composite-FK — לא נגעים.

הטעות שהכי קל ליפול בה כאן: **לנסות לשמר את מכונת-המצבים הישנה ולתפור עליה את המודל החדש בתנאים.** הסימפטום — קוד שבודק `if trial_ends_at` ברמת subscription וגם `if cycle_end` ברמת campaign, ומנסה ליישב ביניהם. הפתרון — להעביר את כל אחריות-הזמן-והחיוב לרמת הקמפיין, ולהשאיר ל-subscription רק את מה שבאמת פר-חשבון (הזהות, האשראי-השמור, ומצב-הנעילה-הכללי).

---

## 2. שינויי סכמה (DB)

כל השינויים כאן דרך migrations חדשות (לעולם לא עריכת migration קיים — חוק הפרויקט). כל טבלה חדשה חייבת `GRANT` תואם למדיניות RLS.

### 2.1 טבלת `campaigns` — הקמפיין הופך ליחידת-החיוב

הקמפיין כבר לא רק "מה שרץ במטא" — הוא נושא עכשיו את כל מצב-החיוב-והמחזור שלו. העמודות החדשות:

`tier` (`basic`/`premium`/`whatsapp`, TEXT + CHECK) — **יורד לכאן מרמת ה-subscription.** כל קמפיין ה-tier שלו-עצמו. (על סוג ה-CHECK-ולא-enum — אותה הנמקה כמו ב-spec §6: הרחבת-ערכים בעתיד = `DROP`+`ADD CONSTRAINT`, לא מיגרציית-enum.)

`lead_quota` (integer, nullable) — **יורד גם הוא לכאן.** המכסה היא פר-קמפיין (§3), נגזרת מה-tier+נפח של הקמפיין. `null` ל-whatsapp.

`cycle_start_at` (timestamptz) — ה-`Start_Timestamp`: הרגע שבו הקמפיין עלה בהצלחה למטא. זה עוגן-הזמן של הקמפיין. **נקבע פעם אחת** ולא משתנה בחידוש.

`cycle_end_at` (timestamptz) — נקודת-סיום-המחזור המדויקת (`cycle_start_at + 30 days` בקמפיין ראשון; ובחידוש: `cycle_end_at הקודם + 30 days` — ראה §4 לנוסחה ולמה מהתאריך-המקורי). זה המשתנה שה-Kill-Switch עוקב אחריו.

`billing_status` (TEXT + CHECK) — מצב-החיוב של הקמפיין (מכונת-מצבים ב-§4.4). ערכים: `trial_active` / `paid_active` / `pending_shutdown` / `paused_unpaid` / `paused_expired`.

`is_first_campaign` (boolean) — האם זה הקמפיין הראשון של הלקוח (נושא ה-trial). נחוץ כי רק הראשון מקבל trial, ורק הראשון בלי-הנחה. **חד-פעמי פר-user** — ראה §3.3 להנמקה על האכיפה.

`discount_pct` (integer, default 0) — אחוז ההנחה על הקמפיין (0 לראשון, 15 לכל הבאים). נשמר על הקמפיין ולא מחושב-בכל-פעם, כי הוא משפיע על החשבונית וצריך audit-trail.

`shutdown_requested_at` (timestamptz, nullable) — מתי הלקוח בחר "לא" בשאלת-ההמשכיות. `null` = לא ביקש כיבוי.

> **הערה על `service_name` ונכסי-Meta:** אלה כבר קיימים ב-`campaigns` היום ולא משתנים. השינוי כאן הוא **תוספת** של שכבת-החיוב-והמחזור על הקמפיין הקיים.

### 2.2 טבלת `campaign_cycles` — audit-trail של המחזורים (חדשה)

כל חידוש-מחזור הוא אירוע כלכלי שצריך תיעוד. במקום לדרוס את `cycle_end_at` ולאבד היסטוריה, כל מחזור נרשם כשורה:

`campaign_id` + `user_id` (composite FK — חוק §7.2) · `cycle_number` (1, 2, 3...) · `started_at` · `ended_at` · `status` (`trial`/`paid`/`shutdown`) · `charged_amount_agorot` (כמה נגבה על המחזור — כולל ההנחה וה-VIP) · `invoice_id` (FK ל-`billing_invoices`, nullable — trial ראשון בלי חשבונית) · `created_at`.

זה מה שמאפשר להציג ללקוח "הוכחת-ביצוע" של כל מחזור, ולנו — לחשב הכנסות ולתחקר חיובים. append-only, RLS SELECT-only, כתיבה דרך service.

### 2.3 טבלת `subscriptions` — מה נשאר ומה מתרוקן

ה-subscription **לא נמחק** — הוא נשאר יחידת-החשבון. אבל הוא מאבד את מה שירד לקמפיין:

**יורד לקמפיין (מתרוקן מ-subscription):** `tier`, `lead_quota`, `current_period_start` (עוגן-הזמן), וכל הלוגיקה של `trial_ends_at` ברמת החשבון. **אל תמחק את העמודות במיגרציה** (זה שובר קוד קיים בטרנזיישן) — במקום זה, סמן אותן כ-deprecated והפסק לכתוב אליהן. הקריאה עוברת לקמפיין. (ניקוי-העמודות עצמו — מיגרציית-cleanup נפרדת אחרי שהכל עובד, כדי לא לשבור באמצע.)

**נשאר ב-subscription (באמת פר-חשבון):** `billing_customer_id`, `billing_provider`, `billing_token` (רפרנס-Vault לאשראי-השמור — אחד לחשבון, משותף לכל הקמפיינים) · `billing_profiles` (פרטי-חשבונית) · `agent_alert_email` / `agent_alert_phone` · `tos_version` / `tos_accepted_at`.

**עמודה חדשה ב-subscription:** `account_access_status` (TEXT + CHECK: `active` / `locked`) — מצב-הנעילה-הכללי (§6). `active` = יש לפחות קמפיין פעיל אחד, גישה מלאה. `locked` = אין אף קמפיין פעיל, הלקוח ננעל החוצה. זה נגזר-ונשמר (לא מחושב-בכל-request) כדי שה-gate ב-frontend יהיה בדיקה זולה.

**מצב `status` של subscription:** נשאר, אבל משמעותו מצטמצמת. `trial`/`active` עדיין רלוונטיים כמצב-על, אבל `past_due`/`expired` כבר לא מגיעים מ"חיוב-חודשי-שנכשל" (אין כזה יותר) — הם מפנים מקום ל-`account_access_status`. **אל תמחק את המצבים** (ה-CHECK נשאר), אבל המעברים אליהם משתנים — ראה §4.4.

### 2.4 טבלת `subscriptions` — הדגל `vip_owner_alerts`

VIP נשאר ברמת ה-subscription? **לא — VIP עובר גם הוא לרמת הקמפיין**, כי ה-tier פר-קמפיין ו-VIP תלוי-tier (זכאות רק ל-basic/premium). הוסף `vip_owner_alerts` (boolean) ו-`vip_alert_phone` ל-`campaigns`, לא ל-subscription. לקוח יכול להדליק VIP על קמפיין Premium אחד ולא על קמפיין Basic אחר. (ההנמקה: אם VIP היה נשאר פר-חשבון בזמן שה-tier ירד לקמפיין, היינו מקבלים מצב בלתי-אפשרי — VIP-חשבון על קמפיין-whatsapp שלא זכאי.)

---

## 3. תמחור ומכסות — הלוגיקה החדשה

### 3.1 חישוב המחיר פר-קמפיין

מקור-האמת נשאר `app/integrations/billing.py` — הלקוח **לעולם לא שולח מחיר**, השרת מחשב. הפונקציה `derive_monthly_price_agorot` מתרחבת: היא מקבלת עכשיו גם `discount_pct` ומחשבת פר-קמפיין.

הנוסחה: `מחיר-בסיס(tier, lead_quota)` × `(100 - discount_pct) / 100` + `תוספת-VIP(lead_quota)` — כשה-VIP **לא** מקבל את ההנחה. כלומר ההנחה חלה על מחיר-הבסיס בלבד, ואז מוסיפים VIP במחיר-מלא.

דוגמה קונקרטית: קמפיין Premium שני עם 500 לידים + VIP. מחיר-בסיס Premium-500 = ₪597. אחרי 15% הנחה = ₪507.45. תוספת VIP-500 = ₪250 (בלי הנחה). סה"כ = ₪757.45. הקמפיין הראשון של אותו לקוח, אם גם הוא Premium-500+VIP, היה ₪597 + ₪250 = ₪847 (בלי הנחה כי הוא ראשון).

טבלת מחירי-הבסיס וה-VIP **לא משתנה** (`_BASE_PRICES_AGOROT`, `_VIP_ADDON_AGOROT`) — מה שמשתנה זה שהחישוב רץ פר-קמפיין ומחיל את ה-`discount_pct`. שילוב `(tier, lead_quota)` לא-מוכר → fail-loud (בלי ניחוש-מחיר), בדיוק כמו היום.

### 3.2 המכסות פר-קמפיין

היום המכסה נספרת פר-חשבון בחלון anniversary. עכשיו **כל קמפיין מכסה משלו**, נספרת בחלון-המחזור-שלו (`[cycle_start_at, cycle_end_at)`). לקוח עם 3 קמפיינים Basic-500 מקבל 3×500 = 1500 לידים סה"כ, אבל **לא כמכסה-משותפת** — כל קמפיין 500 משלו, ואם קמפיין אחד מיצה את ה-500 שלו, הבוט מפסיק להגיב ללידים של **אותו קמפיין** בלבד (השאר ממשיכים).

ההנמקה: כל קמפיין צורך לידים, שיחות-בוט, וקריאות-AI — וזה עולה כסף בפועל. מכסה-משותפת הייתה מאפשרת ללקוח להעביר את כל הצריכה לקמפיין אחד ולעקוף את המודל. מכסה-פר-קמפיין תואמת לעלות-בפועל.

**איפה זה נוגע בקוד:** `get_quota_status` (Phase 4.2) עובר מלספור-פר-user לספור-פר-campaign. החלון עובר מ-`current_period_start` של ה-subscription ל-`cycle_start_at` של הקמפיין. ההתראות (80%/100%) נשלחות פר-קמפיין. ה-flags `notified_80_at`/`notified_100_at` יורדים גם הם לרמת הקמפיין (או לטבלת-מחזור), ומתאפסים בכל חידוש-מחזור.

**מכסות של קמפיין שנכבה:** כשקמפיין ב-`paused_unpaid`/`paused_expired`, המכסה שלו קפואה (הוא לא מקבל לידים ממילא — נעצר במטא). בחידוש, המחזור החדש מתחיל מכסה נקייה (§4). הדאטה ההיסטורית (הלידים שכבר נאספו) נשארת גלויה תמיד.

### 3.3 אכיפת ה-trial החד-פעמי

ה-trial הוא חד-פעמי-פר-יוזר, וזו נקודה שקל מאוד לפרוץ אם לא אוכפים אותה ברמת-שורש. הסימפטום של אכיפה-חלשה: לקוח מוחק קמפיין ופותח חדש כדי לקבל trial נוסף, או פותח שני קמפיינים במקביל ומצפה ש-trial יחול על שניהם.

האכיפה חייבת להיות **דטרמיניסטית ברמת ה-DB**, לא בדיקה-בקוד שאפשר לעקוף. המנגנון: דגל `trial_consumed` ברמת ה-subscription (או בדיקת-קיום — האם ל-user יש **אי-פעם** קמפiין עם `is_first_campaign=true`). כשלקוח מקים קמפיין, ה-service בודק אטומית: אם `trial_consumed=false` → זה הקמפיין הראשון, `is_first_campaign=true`, `billing_status=trial_active`, וסמן `trial_consumed=true` **באותה טרנזקציה** (CAS על `trial_consumed=false`). אם `trial_consumed=true` כבר → הקמפיין החדש הוא רגיל (`is_first_campaign=false`, `billing_status=paid_active` מיום 1, `discount_pct=15`).

מחיקת-קמפיין **לא מאפסת** את `trial_consumed`. זו ההגנה מפני trial-riding. (אם בעתיד נרצה לאפשר trial-מחדש ללקוח שחזר — זו החלטת-מוצר נפרדת, לא ברירת-מחדל.)

### 3.4 חישוב ה-`discount_pct` בהקמת-קמפיין

הכלל פשוט: `is_first_campaign=true` → `discount_pct=0`. אחרת → `discount_pct=15`. זה נקבע פעם-אחת בהקמה ונשמר על הקמפיין. **אל תחשב את ההנחה מחדש בכל חיוב** — היא תכונה-קבועה של הקמפיין (אחרת מחיקת-הקמפיין-הראשון הייתה משנה רטרואקטיבית את ההנחות של האחרים, וזה באג).

נקודת-קצה שצריך להחליט עליה (ולסמן ל-CC): מה קורה אם הלקוח מוחק את הקמפיין-הראשון בזמן שיש לו קמפיינים 2+ פעילים? הקמפיינים ה-2+ **נשארים עם 15% ההנחה שלהם** (ההנחה קבועה, לא-מחושבת-מחדש). אין "קידום" של קמפיין-2 לקמפiין-ראשון. ה-`trial_consumed` נשאר `true`. זה העקבי — ההנחה היא תכונת-הקמפיין, לא פונקציה-של-הסדר-הנוכחי.

# הכרעה: 
כן, ההנחות נשארות.

---

## 4. מחזור-החיים של הקמפיין — מכונת-המצבים החדשה

זה הלב. כל קמפיין הוא מכונת-מצבים עצמאית שהשרת (cron) מנהל, **ללא תלות ב-AI**. ה-AI מתקשר עם הלקוח; השרת מבצע את הפעולות הקשיחות (כיבוי, בדיקת-ודאות, חסימה).

### 4.1 נוסחת חידוש-המחזור

`New_End = Current_End + 30 days` — **מהתאריך המקורי של סוף-המחזור, לא מיום-הלחיצה של הלקוח.**

ההנמקה קריטית: אם לקוח לוחץ "המשך" יומיים לפני סוף-המחזור, וחישבנו מיום-הלחיצה, היינו נותנים לו 28 יום במקום 30 (או "חור" של יומיים). החישוב מהתאריך-המקורי מבטיח שהלקוח תמיד מקבל 30 יום מלאים רצופים, בלי חפיפות ובלי חורים. **הלחיצה של הלקוח רק "פותחת" את המחזור הבא — היא לא קובעת את עוגן-הזמן שלו.**

### 4.2 הקמפיין הראשון — מחזור-ה-trial-הכלול

זה התרשים המלא של הקמפיין הראשון (נושא ה-trial):

יום 1: הקמפיין עולה למטא בהצלחה. `cycle_start_at = now()`, `cycle_end_at = now() + 30d`, `billing_status = trial_active`. הקמפיין **רץ במטא ומביא לידים אמיתיים מיום 1** — ה-trial הוא רק על החיוב, לא על הפעילות.

ימים 1–9: `trial_active`. הקמפיין רץ, לידים נכנסים, הבוט עובד (אם Premium). אין אשראי במערכת, אין חיוב.

יום 9 — הזנת-האשראי (J2, בלי חיוב): הסוכן (AI) פונה ללקוח (מייל+וואטסאפ): "הגענו לסוף תקופת ה-10 ימי ניסיון. הקמפיין רץ מצוין וממשיך איתך לעוד 20 ימים כדי להשלים את מחזור ה-30 המלא. האם תרצה לאשר את המשך הפעילות?" — עם קישור לדשבורד. הלקוח נכנס, מזין את פרטי-הכרטיס, ומאשר-המשכיות. מה שקורה כאן טכנית הוא `J2` של פלאקארד בלבד — אימות-הכרטיס וטוקניזציה, **בלי שום חיוב**. פלאקארד מוודאת שהכרטיס תקין ומחזירה טוקן שנשמר ב-Vault. אף שקל לא יורד ביום 9. זו רק הכנסת-האשראי-למערכת + אישור-ההמשכיות.

חשוב להבחין: **הזנת-האשראי (J2) וגביית-החיוב (J4) הן שתי פעולות נפרדות בשתי נקודות-זמן שונות.** ההפרדה הזו היא בכוונת-תחילה, והיא מה שמגן על ההבטחה של גיא — "שום שקל לא ייגבה על תקופה שלא-אושרה". מציאות שבה הלקוח הזין-אשראי-ביום-9-אך-התחרט-לפני-יום-10 חייבת להסתיים בלי-חיוב, ולכן הגבייה מותנית באישור-ההמשכיות-בנקודת-יום-10, לא בעצם-קיום-הטוקן. אזהרה מפורשת למי שמבצע: **אל תריץ `J4` ביום 9.** יום 9 הוא טוקניזציה בלבד; הגבייה היא בסוף יום 10.

**נקודת-ההכרעה — גביית-החיוב (סוף יום 10, בשעה המדויקת של ה-`Start_Timestamp`):** בנקודת-יום-10 המדויקת נורה הטריגר של סוף-ה-trial, וכאן — ורק כאן — הכסף יורד:
- הלקוח אישר והזין אשראי ביום 9 → עכשיו נגבה החיוב-הראשון בפועל (`J4` על הטוקן-השמור ב-Vault), `billing_status = paid_active`, חשבונית מופקת (מחיר-בסיס מלא, ראשון בלי הנחה), והקמפיין ממשיך לרוץ רצוף עד `cycle_end_at` (20 הימים הנותרים). המחזור **לא מתארך** — הוא נגמר ב-30 כמתוכנן.
- הלקוח לא אישר / לא ענה / התחרט → `trial_expiry_kill` (§5.2): הקמפיין נעצר במטא בנקודת-יום-10 המדויקת. **אין `J4`, אין חיוב** — אף שקל לא נגבה על 20 הימים שלא-אושרו, גם אם טוקן כבר נשמר ב-J2. `billing_status = paused_expired`.

### 4.3 חידוש-מחזור — יום 28 של כל מחזור

זה חל על **כל** קמפיין (ראשון או נוסף), בכל מחזור אחרי שהחיוב-הראשון כבר נגבה:

יום 28 (48 שעות לפני `cycle_end_at`): הסוכן פונה ללקוח: "המחזור הנוכחי מסתיים ב-[תאריך ושעה]. האם תרצה להמשיך לחודש נוסף?"
- **"כן"** → `cycle_end_at = cycle_end_at + 30d` (§4.1), חיוב נגבה, חשבונית מופקת, `billing_status` נשאר `paid_active`, מכסה מתאפסת למחזור החדש. שורת `campaign_cycles` חדשה.
- **"לא"** → `billing_status = pending_shutdown`, `shutdown_requested_at = now()`. הלקוח מקבל: "קיבלתי. הקמפיין ימשיך לרוץ עד סוף המחזור ב-[תאריך], ואז יכבה אוטומטית." הקמפיין ממשיך לרוץ עד `cycle_end_at`.

**נקודת-הכיבוי (`cycle_end_at`):** `cycle_expiry_kill` (§5.3) — הקמפיין נעצר במטא. `billing_status = paused_unpaid`. הקמפיין **נשאר גלוי בממשק** כ-read-only עם כפתור-חידוש (§6, מצב-1).

### 4.4 מכונת-המצבים המלאה (`billing_status` ברמת קמפיין)

```
                  הקמת קמפיין ראשון (trial_consumed=false)
                            │
                            ▼
                   ┌─────────────────┐
                   │  trial_active   │ ימים 1-10, בלי אשראי
                   └────────┬────────┘
                   סוף יום 10 — נקודת הכרעה
                   ┌────────┴────────┐
                   ▼ אישר+אשראי      ▼ לא אישר
            ┌─────────────┐   ┌─────────────────┐
            │ paid_active │   │ paused_expired  │ (Kill, בלי חיוב)
            └──────┬──────┘   └─────────────────┘
                   │
     ┌─────────────┤ הקמת קמפיין נוסף (2+) נכנסת ישר לכאן
     │             │   (trial_consumed=true, discount 15%, חיוב מיום 1)
     │             │
     │        יום 28 — שאלת המשכיות
     │        ┌────┴────┐
     │        ▼ "כן"    ▼ "לא"
     │  ┌──────────┐  ┌──────────────────┐
     └──┤חידוש+30d │  │ pending_shutdown │ (רץ עד סוף מחזור)
        │paid_active│  └────────┬─────────┘
        └──────────┘           │ הגעה ל-cycle_end_at
                                ▼
                       ┌─────────────────┐
                       │  paused_unpaid  │ (Kill, נשאר בממשק)
                       └────────┬────────┘
                          לחיצת "חדש" + תשלום
                                │
                                ▼  (מחזור חדש, cycle_end מחושב מ-now)
                          paid_active
```

**מעברים חוקיים בלבד** (נאכף ב-service):
- `trial_active → paid_active` (אישר ביום 10)
- `trial_active → paused_expired` (לא אישר ביום 10 — Kill בלי חיוב)
- `paid_active → pending_shutdown` (בחר "לא" ביום 28)
- `paid_active → paid_active` (חידוש ביום 28, `cycle_end += 30d`)
- `pending_shutdown → paused_unpaid` (הגיע ל-cycle_end אחרי בחירת-"לא" — Kill)
- `pending_shutdown → paid_active` (התחרט לפני ה-Kill, בחר לחדש) — **מעבר-חזרה מותר**
- `paused_unpaid → paid_active` (לחץ "חדש" ושילם — מחזור חדש)
- `paused_expired → paid_active` (לקוח trial שחזר ושילם — מחזור חדש; ה-trial לא חוזר)
- קמפיין 2+ נכנס ישירות ל-`paid_active` בהקמה (בלי לעבור `trial_active`).

> **הבחנה קריטית שאסור לפספס:** `paused_expired` (Kill של trial — בלי חיוב מעולם) ו-`paused_unpaid` (Kill של סוף-מחזור — אחרי שכבר שולם). שניהם "קמפיין מושהה", אבל ההיסטוריה הכלכלית שונה, וההודעות ללקוח שונות. הפרד ביניהם.

---

## 5. ה-Kill-Switch — המנגנון הקריטי

### 5.1 שני Kill-Switch שונים עם גרעין משותף

יש **שני** מנגנוני-כיבוי שונים, וחייבים להפריד אותם בקוד למרות שהם חולקים גרעין. הבלבול ביניהם הוא באג-כסף.

**`trial_expiry_kill`** — נורה בסוף יום 10 של הקמפיין הראשון, אם הלקוח לא אישר. ההקשר: הלקוח **מעולם לא שילם**, ואין אשראי במערכת. המטרה: לוודא ששום שקל לא נגבה על תקופה שלא-אושרה. אחרי הכיבוי → `paused_expired`.

**`cycle_expiry_kill`** — נורה בסוף מחזור-30 של כל קמפיין ב-`pending_shutdown` (הלקוח בחר "לא"). ההקשר: הלקוח **כבר שילם** לפחות מחזור אחד, והשאלה היא רק אי-חידוש. אחרי הכיבוי → `paused_unpaid`, והקמפיין **נשאר בממשק**.

הגרעין המשותף — הפעולה הטכנית מול מטא — זהה בשניהם, ומופיע פעם אחת (§5.4). מה ששונה: הטריגר, ההקשר-הכלכלי, ה-`billing_status` שאליו עוברים, וההודעה ללקוח.

### 5.2 `trial_expiry_kill` — הזרימה

טריגר: cron שסורק קמפיינים ב-`trial_active` שהגיעו ל-`cycle_start_at + 10 days` בלי אישור. דיוק של דקה.

פעולה: PAUSE מאובטח (§5.4). בהצלחה → `billing_status = paused_expired`. **אין חיוב, אין הפקת-חשבונית.** הלקוח מקבל הודעה: "תקופת הניסיון הסתיימה. הקמפיין הושהה. תוכל להפעיל אותו מחדש בכל עת מהדשבורד (עם הזנת אמצעי-תשלום)."

### 5.3 `cycle_expiry_kill` — הזרימה

טריגר: cron שסורק קמפיינים ב-`pending_shutdown` שהגיעו ל-`cycle_end_at`. דיוק של דקה.

פעולה: PAUSE מאובטח (§5.4). בהצלחה → `billing_status = paused_unpaid`. הקמפיין **נשאר ב-DB וגלוי בממשק** (§6). הלקוח מקבל: "המחזור הסתיים והקמפיין הושהה כמבוקש. תוכל לחדש אותו בכל עת."

חסימת-הפעלה-מחדש: אחרי `cycle_end_at`, **כל** ניסיון-הפעלה (בין אם מ-AI ובין אם מהמשתמש) חסום ברמת-השרת עד שנפתח מחזור-חדש (דרך תשלום). זו הגנה מפני מצב שבו ה-AI או המשתמש "מפעיל" קמפiין שלא-שולם.

### 5.4 גרעין ה-PAUSE המאובטח (משותף לשניהם)

זו הפעולה האוטונומית המסוכנת ביותר במערכת. ה-spec מסמן במפורש: "באג בפעולה אוטונומית שורף כסף אמיתי". אם ה-cron ייכשל (worker קרס, טוקן פג), הקמפיין ממשיך לרוץ ולשרוף כסף אחרי שהיה אמור להיעצר. לכן הגרעין בנוי כ-fail-safe אגרסיבי:

שלב 1 — **PAUSE**: קריאת `update` דרך ה-SDK של מטא (`facebook-business`, כמו כל קריאות-Meta — לא httpx) לשינוי סטטוס הקמפיין ל-PAUSED.

שלב 2 — **בדיקת-ודאות אחרי 60 שניות**: `GET` לסטטוס הקמפיין ממטא. זו לא הנחה — קוראים את המצב-האמיתי (אותו עיקרון כמו cleanup-cron של Phase 3.4, "בדיקת אמת מול Meta לפי meta_id, לא הנחה").

שלב 3 — **FORCE_PAUSE בכשל**: אם הסטטוס עדיין ACTIVE → ניסיון-PAUSE חוזר + **Critical Error Log** ל-Sentry (התראה דחופה למפתח, לא רק log). זהו כשל-טכני-דחוף כי משמעותו כסף-נשרף-עכשיו.

שלב 4 — **תיעוד**: כל פעולת-כיבוי (מוצלחת או לא) נרשמת ב-`optimization_actions` (או טבלת-audit ייעודית) עם `action_type = kill_switch`, סוג (`trial`/`cycle`), לפני/אחרי, ותוצאה. זה מה שמאפשר להציג ללקוח "הוכחת-ביצוע".

**טיפול ב-token-פג:** אם הטוקן פג בזמן ה-Kill (הקמפיין לא ייעצר!), זה תרחיש-החירום החמור ביותר. ה-cron חייב: לזהות `FbTokenUnavailableError`, לנסות refresh (Phase 6), ואם נכשל — Critical alert **מיידי** + retry-loop הדוק (לא backoff רגיל — כל דקה שהקמפיין רץ עולה כסף). זה תרחיש שדורש התערבות-אדם, אז ההתראה קריטית.

**דפוס ה-jobs:** ה-Kill רץ דרך תשתית-ה-jobs הקיימת (enqueue → claim → retry). אבל שים לב — retry-policy של Kill שונה מ-default: כשל ב-Kill הוא דחוף, לא "ננסה בעוד 5 דקות". קצר את ה-backoff ל-Kill jobs, והעלה alert על כל attempt-כושל, לא רק על ה-terminal.

### 5.5 למה תקציב-יומי ולא lifetime — וההשלכה

גיא בחר תקציב **יומי** (לא lifetime) על בסיס ניסיון — lifetime מניב ביצועים גרועים במטא. ההשלכה הטכנית ישירה: **מטא לא עוצרת את הקמפיין לבד.** ב-lifetime-budget, מטא הייתה מפסיקה אוטומטית כשהתקציב נגמר; ב-daily-budget הקמפיין רץ לנצח עד שמישהו עוצר אותו. **זו בדיוק הסיבה שה-Kill-Switch חייב להיות אקטיבי ואמין** — אין רשת-ביטחון מצד מטא. אם ה-Kill נכשל, אין דבר שני שיתפוס את זה. זה מעלה את הרף לאמינות ה-cron ובדיקת-ה-fail-safe.

---

## 6. נעילת-חשבון וגישה לממשק

### 6.1 שני מצבי-נעילה שונים — הפרדה קריטית

יש הבדל מהותי בין "קמפיין ספציפי נכבה" לבין "המשתמש איבד גישה". קל לבלבל, והבלבול נועל לקוחות שלא-אמורים-להינעל.

**מצב-1: קמפיין נכבה, אבל יש עוד קמפיינים פעילים.** המשתמש שומר **גישה מלאה** לממשק — הוא צריך לנהל את השאר. הקמפיין-שנכבה נשאר גלוי כ-**read-only עם כפתור-חידוש**: הלקוח רואה את כל הדאטה שלו (לידים, ביצועים, היסטוריה), ויכול ללחוץ "חדש" → תשלום → הקמפיין רץ עוד 30 יום (מחזור חדש, `cycle_end` מחושב מ-`now`).

**מצב-2: הקמפיין ה-אחרון נכבה — אין למשתמש שום קמפיין פעיל.** כאן נכנסת נעילת-החשבון: `account_access_status = locked`, והמשתמש מאבד גישה לממשק כולו. הנעילה **מיידית** ברגע שהקמפiין האחרון עובר ל-`paused_*` (אין grace-period).

### 6.2 מתי נגזר `account_access_status`

זה נגזר-ונשמר (לא מחושב-בכל-request), ומתעדכן בכל שינוי-מצב-קמפיין. הכלל: אחרי כל מעבר של קמפיין ל/מ-`paused_*`, ה-service בודק — **האם ל-user יש עדיין לפחות קמפיין אחד ב-`trial_active`/`paid_active`/`pending_shutdown`?**
- כן → `account_access_status = active`.
- לא (כל הקמפיינים ב-`paused_unpaid`/`paused_expired`) → `account_access_status = locked`.

זה הופך את ה-gate ב-frontend לבדיקה זולה: `if account_access_status == 'locked'` → חסום. בלי לספור קמפיינים בכל טעינת-עמוד.

### 6.3 מה קורה בנעילה — והדאטה

בנעילה (`locked`), ה-frontend חוסם את כל המסכים חוץ ממסך-אחד: "אין לך קמפיינים פעילים. הפעל קמפיין מחדש כדי לחזור." — עם רשימת-הקמפיינים-הכבויים וכפתור-חידוש לכל אחד.

**הדאטה נשמרת לצמיתות.** אם הלקוח יחזור וישלם (יחדש קמפיין כלשהו) → `account_access_status = active`, והוא רואה את **כל** הדאטה ההיסטורית שלו: כל הלידים, כל הקמפיינים, כל ההיסטוריה. שום דבר לא נמחק בנעילה. (מחיקת-דאטה, אם תידרש אי-פעם, היא תהליך-נפרד עם דרישות-פרטיות משלו — §10 ב-spec — לא חלק מהנעילה.)

הטעות שקל ליפול בה: **לנעול פר-קמפיין במקום פר-חשבון.** הסימפטום — לקוח עם 3 קמפיינים ננעל החוצה כשהראשון נכבה, בזמן ששניים עוד רצים. הפתרון — הנעילה נגזרת מ"אין אף קמפיין פעיל", לא מ"קמפיין נכבה". תמיד בדוק את מצב-כל-הקמפיינים, לא את הקמפיין-הבודד.

---

## 7. חשבוניות פר-קמפיין

כל קמפיין מפיק חשבונית-Green-Invoice משלו בכל חיוב (סוף-trial של הראשון, וכל חידוש של כל קמפiין). לקוח עם 3 קמפיינים = 3 חשבוניות נפרדות, כל אחת ביום-החידוש של אותו קמפיין.

תיאור-החשבונית: `"מנוי Campaign AI — קמפיין {service_name} — {Basic/Premium/WhatsApp}[ + התראות VIP] — מחזור {cycle_number} — {תאריך}"`. אם יש הנחה, היא משתקפת בסכום (לא צריך שורת-הנחה נפרדת — הסכום כבר מנוכה).

הקישור ל-`campaign_cycles`: כל חשבונית מקושרת לשורת-המחזור שלה (`invoice_id`). זה נותן audit מלא — לכל מחזור, כמה נגבה ואיזו חשבונית הופקה.

**מה נשאר מ-Phase 2.6.2:** כל תשתית Green Invoice (JWT caching, retry על 401, PDF דרך webhook, "כשל-הפקה לא מבטל חיוב" + Sentry + cron-retry) **נשארת שלמה**. השינוי היחיד — החשבונית מופקת פר-קמפיין-מחזור במקום פר-חשבון-anniversary. ה-handler שמפיק אותה עובר להיות מופעל מ-flow-החידוש-של-הקמפiין.

---

## 8. VIP במודל החדש

VIP עובר לרמת הקמפיין (§2.4). ההשלכות:

הזכאות נבדקת פר-קמפיין: רק קמפיין `basic`/`premium` יכול VIP. קמפיין `whatsapp` — הכרטיס מוסתר.

התמחור: תוספת-VIP (250/350 לפי מכסת-הקמפיין) מתווספת לחיוב-הקמפיין, **בלי הנחת-15%** (גם על קמפיין 2+). כלומר בקמפיין-שני, מחיר-הבסיס מקבל 15% הנחה אבל ה-VIP לא.

מודל-החיוב של VIP: הפרוציג'ר הקיים (`derive_vip_proration_agorot`, `finalize_vip_addon_charge`, cancel-at-period-end) **נשאר** — אבל עכשiו ה"מחזור" הרלוונטי ל-proration הוא מחזור-הקמפיין (30 יום מ-`cycle_start_at`), לא anniversary-החשבון. הדלקת-VIP באמצע מחזור-קמפiין → proration על הימים שנותרו עד `cycle_end_at` של אותו קמפיין. הדלקה ב-trial → דגל בלי-חיוב, מתמזג לחיוב-הראשון.

> **סתירת-תיעוד שנפתרת כאן:** מסמך-התמחור הנוכחי (§5.2) כבר תיקן ש-VIP הוא proration-מיידי (לא "מהמחזור הבא"). המודל החדש שומר את זה — רק מעגן ל-מחזור-הקמפיין. `spec.md:259-260` ו-`VIP_ADDON_PLAN.md` שעדיין אומרים "מהמחזור הבא בלי proration" — לא-מדויקים, והמקור-הנכון הוא `vip-immediate-charge-plan.md`.

---

## 9. שער-האשראי החדש (החלפת Phase 2.6 flow)

היום: אמצעי-תשלום הוא שער להקמת-קמפיין. **זה מתבטל.**

החדש: הקמפיין הראשון עולה **בלי שום אשראי**. שער-האשראי עובר ליום-9-של-הקמפ-הראשון. ה-flow:

הקמת-קמפ-ראשון → הקמפ עולה למטא, רץ, מביא לידים (ימים 1–10 trial) → יום 9: מייל+וואטסאפ "הטרייאל נגמר, הזן אשראי כדי להמשיך" + קישור-דשבורד → הלקוח מזין אשראי (J2 של פלאקארד — טוקניזציה, כמו היום) → אישור-המשך → יום 10: החיוב הראשון (J4 על הטוקן) → `paid_active`.

**מה נשאר מ-Phase 2.6:** כל תשתית-הסליקה (`pelecard.py`, `billing.py`, J2/J4, Vault-token, IPN-verification, idempotency על `PelecardTransactionId`) **שלמה**. השינוי — **מתי** נכנס האשראי (יום 9 במקום לפני-הקמפ) ו**מה** מפעיל את החיוב (אישור-המשכיות במקום trigger-אוטומטי-יום-8).

**קמפיין 2+:** אין שער-אשראי חוזר (הטוקן כבר ב-Vault מהראשון). הקמפ עולה, ומיום-1 צובר חיוב על הטוקן-הקיים. החיוב הראשון של קמפ-2 נגבה... **מתי בדיוק?** — ראה §11 (שאלה פתוחה לגיא: מיידי בהקמה, או בסוף מחזור-30-הראשון של אותו קמפ?).

---

## 10. סדר-ביצוע מומלץ ל-CC

השינוי גדול ונוגע בהרבה מקומות. הסדר הזה ממזער שבירה-באמצע:

1. **סכמה קודם (migrations)** — הוסף עמודות ל-`campaigns`, צור `campaign_cycles`, הוסף `account_access_status` ל-subscription. **בלי למחוק** עמודות ישנות עדיין. GRANT + RLS לכל חדש.
2. **שכבת-החישוב** — עדכן `derive_monthly_price_agorot` ל-`discount_pct` פר-קמפ. הוסף אכיפת-trial-חד-פעמי (CAS על `trial_consumed`).
3. **מכונת-המצבים של הקמפ** — service שמנהל את `billing_status` עם המעברים-החוקיים (§4.4). זה הליבה.
4. **ה-Kill-Switch** — גרעין-PAUSE-מאובטח משותף (§5.4) + שני ה-triggers (`trial_expiry_kill`, `cycle_expiry_kill`). crons ב-runner.
5. **נעילת-חשבון** — גזירת `account_access_status` + gate ב-frontend.
6. **חשבוניות פר-קמפ** — העבר את handler-ההפקה ל-flow-החידוש.
7. **שער-אשראי חדש** — העבר את הזנת-האשראי מלפני-הקמפ ליום-9.
8. **frontend** — מסכי read-only-לקמפ-כבוי, כפתור-חידוש, מסך-נעילה, שאלות-ההמשכיות (יום-9/יום-28).
9. **ניקוי** — רק אחרי שהכל עובד: מיגרציית-cleanup שמוחקת את העמודות-הישנות מ-subscription (`tier`/`lead_quota`/`current_period_start`).

**עיקרון-על לכל השלבים:** אל תשבור את ה-seam האגנוסטי של הסליקה. `billing.charge_saved_card` וכל שכבת-ה-abstraction נשארים. אתה משנה **מתי וכמה** מחייבים, לא **איך**. אם אתה מוצא את עצמך נוגע ב-`pelecard.py` בשביל השינוי הזה — עצור, כנראה טעית בשכבה.

---

## 11. שאלות פתוחות שנשארו לגיא (לא לממש בלי הכרעה)

אלה נקודות שלא הוכרעו סופית, ו-CC **לא צריך לנחש** אותן — צריך לסמן ולחכות:

**מתי נגבה החיוב-הראשון של קמפיין 2+?** קמפ-2 אין לו trial ומשלם מיום-1 — אבל "מיום-1" זה: (א) חיוב-מיידי-בהקמה (הלקוח משלם ברגע שהוא מקים את הקמפ-השני), או (ב) הקמפ רץ 30 יום ואז נגבה בסוף-המחזור-הראשון שלו? זה משנה את ה-cash-flow ואת חווית-הלקוח. **צריך הכרעה.**

**הכרעה: חיוב מיידי בהקמה, מרגע עליית הקמפיין למטא.**

---

**קמפיין שנכבה — כמה זמן נשאר ניתן-לחידוש?** מצב-1 (read-only עם כפתור-חידוש) — האם הכפתור זמין לנצח, או פג אחרי X ימים? כרגע ההנחה: לנצח (הדאטה נשמרת לצמיתות ממילא). **לאמת.**

---

## נספח: מיפוי סתירות מול spec.md ו-ROADMAP

המסמך הזה **מחליף** את החלקים הבאים במסמכי-המקור. מי שמעדכן את spec/ROADMAP אחרי הביצוע צריך לתקן שם:

| נושא | spec/ROADMAP הישן | המודל החדש (מסמך זה) |
|---|---|---|
| יחידת-חיוב | פר-חשבון (spec §5) | פר-קמפיין (§0) |
| עוגן-זמן | anniversary, `current_period_start` (spec §6) | `cycle_start_at` פר-קמפיין (§4) |
| trial | 7 יום ברמת subscription (Phase 2.6) | 10 יום ברמת קמפ, חד-פעמי, כלול ב-30 (§4.2) |
| שער-אשראי | לפני הקמת-קמפ (spec flow §6.6) | יום-9 של קמפ-ראשון (§9) |
| tier | פר-חשבון או-או (spec §5) | פר-קמפיין (§2.1) |
| מכסות | פר-חשבון בחלון-anniversary (spec §5) | פר-קמפיין בחלון-מחזור (§3.2) |
| מכונת-מצבים | `trial→active→past_due→expired` (spec §6) | `billing_status` פר-קמפ (§4.4); מצבי-subscription הישנים מתרוקנים |
| Kill-Switch | לא היה קיים | שני מנגנונים + fail-safe (§5) |
| VIP scope | פר-חשבון (spec §5) | פר-קמפיין (§8) |
| חשבוניות | פר-חשבון (Phase 2.6.2) | פר-קמפיין-מחזור (§7) |
| PR2 (`cps` timing) | חיוב-יום-8 מעדכן `current_period_start` | לא-רלוונטי — אין חיוב-אוטומטי-יום-8; `cycle_start_at` פר-קמפ מחליף |

**מה ש-*לא* משתנה (נשאר תקף מ-spec/ROADMAP):** כל הארכיטקטורה-בשכבות (spec §2א), חוקי-האבטחה (RLS/composite-FK/Vault/admin-client — spec §7), חוק-7 (LLM-מסווג-קוד-מחליט), ה-seam-האגנוסטי-לסליקה, כל תשתית-ה-jobs, כל שכבת-הסוכן והאופטימיזציה (Phase 7), הבוט (Phase 5), ורענון-הטוקן (Phase 6). השינוי הזה הוא **שכבת-חיוב-ומחזור חדשה על קמפיין** — לא נגיעה בליבת-המוצר.

---

# עוד דבר חשוב לשנות

מחיר** קמפיין ווטסאפ: לשנות ל397₪**
