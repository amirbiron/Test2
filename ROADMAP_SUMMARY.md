# Campaign AI — תקציר ROADMAP 

> תקציר נאמן של כל ה-ROADMAP (~14K שורות), פר-פאזה ופר-סשן: מה כל סשן עושה, ההחלטות שננעלו, ומה נדחה.
> `spec.md` = ה"מה". מסמך זה = תמצית ה"סדר ומתי" + ההחלטות.
> סטטוס: ✅ הושלם · 📋 מתוכנן (טרם מומש) · 🔨 בתהליך · 🔮 עתידי · ⏸️ נדחה.
> עדכון אחרון: 2026-07-04 — **סקירה מלאה מול הקוד** (לא רק דלתא): 162 commits ו-26 מיגרציות (0088→0113) מאז. עיקרי השינויים: **4 סוגי-בעיות בסוכן** end-to-end (נוספה **בעיה 4 — budget_mismatch**), **אופטימיזציה עם תמונות** (opt-images), **סנדבוקס מומש** (היה 🔮), **8.1 ניטור-יזום high_cpl מומש** (היה 🔮), **ערוץ התראות-בעלים (agent-notify) מומש**, **VIP addon**, **קמפיין CTWA** + **יומן-Google-ישיר** (במקום Calendly), **frontend מחווט end-to-end** (~79 endpoints), ושכבת-ההקשחה **26 ✅ / 2 נותרו**. **מעבר PSP ל-Grow** מתוכנן (📋, טרם מומש). מקורות-אמת: `docs/code-reviews/PROGRESS.md`, `docs/frontend-integration.md`. **סבב-העשרה (אותו יום):** הושלם התוכן המהותי פר-סשן (זוויות-הקופי, שדות טופס-הליד, מבנה תשתית-הפרומפטים, states, templates) + תיקוני-עובדה (16 states לא 10 · 9 templates לא 7 · מספרי מיגרציות-הבוט · 4.5.1 הוא helper פנימי ללא endpoint) + סעיפים שחסרו (**7.7Q Creative VIP Studio**, **7.4.X ארכיטקטורת פרומפט-הסוכן**, 0.2 ✅).
> עדכון קודם: 2026-06-18 — נוספה **שכבת הקשחה (code-review, תוכנית 24 PR)**: 10 PR-ים הושלמו (PR1–PR8b-2) + 3 root-fixes רוחביים, ושינו התנהגות של פאזות שכבר מומשו (מסומן `🔧` פר-פאזה). מקור-אמת: `docs/code-reviews/PROGRESS.md`.
> עדכון קודם: 2026-06-13 — Phase 5 (בוט WhatsApp) הושלמה במלואה, 6.1 הושלם, ו-Phase 7 התרחב (7.1–7.4ב) ובלע את האופטימיזציה ה**מאושרת** שתוכננה כ-Phase 8.

**עקרונות-על חוצי-פאזות:** Invariant "משתמש ⟺ subscription" · חיבור Meta יושב על המשתמש, לעולם לא הזהות (user_id מ-Supabase Auth) · jobs pattern (enqueue → claim עם FOR UPDATE SKIP LOCKED → retry `{1:60,2:300}` max 3) · idempotency בכל מקום · migrations: לעולם לא לערוך קיים, `create or replace` בחדש · trigger/RPC = SECURITY DEFINER + search_path ריק · RLS SELECT-only לרוב הטבלאות, כתיבה דרך service · הכל UTC · סודות ב-Vault · ספקים מבודדים מאחורי שכבת abstraction (`billing.py` מעל `pelecard.py` וכו') · חוק 7: LLM לעולם לא מחליט על מעבר state — רק מסווג, קוד דטרמיניסטי מחליט.

**Frontend (חוצה-פאזות):** הפרונטד החי (`app/web/` — `index.html` + `api.js`, same-origin) **מחווט end-to-end** ל-~79 endpoints ב-22 routers. רוב המסכים ✅ (Creative Studio, Dream Wizard מלא, My Plan + VIP/agent-notify toggles, CRM-lite לידים, פאנל תורים, סוכן WhatsApp, תשלום+חבילה). עדיין 🟡/🔴: Dashboard KPIs, Diagnostics, Context. מפה per-screen: `docs/frontend-integration.md`; פערים: `docs/backend-gaps.md`.

---

## Phase 0 · תשתית

**0.1 — שלד FastAPI מודולרי ✅** · מבנה תיקיות לפי spec (routers/services/integrations/models/db/core), `config.py` מ-env, `db/client.py` (user client + admin מבודד), `/health`. אפס לוגיקה ב-`main.py`.

**0.2 — חיבור ל-Render ✅** · `render.yaml` (IaC): **2 שירותים** — `campaign-ai-web` (uvicorn, `healthCheckPath:/health`, plan `starter` — נדרש ל-preDeployCommand, frankfurt) + `campaign-ai-worker` (`python -m app.worker.runner`). **`preDeployCommand: python -m app.db.migrate`** — migrations רצות אוטומטית לפני שהגרסה עולה; כשל בהן חוסם את ה-deploy. env אמיתיים כולל `SUPABASE_DB_URL` (session-mode pooler ל-migrations); סודות `sync:false` (מוזנים ב-Dashboard). אבן הדרך "הצינור repo→Render→Supabase חי" הושגה (הכותרת ב-ROADMAP לא סומנה ✅, אך הפריסה חיה בפועל — תיקוני production של egress/health מעידים).

## Phase 0.5 · הרשמה ו-subscription

**0.5.1 — Auth (Supabase Auth) ✅** · signup/login (email+password), JWT (access בגוף, refresh ב-httpOnly cookie עם rotation), `/auth/refresh`, `/auth/logout`. אימות אימייל מופעל + auto-link כבוי (מניעת OAuth takeover). Supabase מטפל בשחזור סיסמה. **🔧 PR1 (P0-2-S):** שרשרת הגנה-בעומק מ-takeover — logout `scope='global'` (מבטל את כל ה-refresh tokens) + מבחין בין כשל `set_session` ל-`sign_out` (אין 204 כוזב על session חי) + `sign_up` דוחה email שכבר רשום דרך OAuth (RPC 0075) + OAuth state nonce single-use (RPC 0076, חוסם replay).

**0.5.2 — subscription אוטומטי ✅** · DB trigger על `auth.users` יוצר שורת subscription (`status=trial`, `tier=pending`, `lead_quota=null`). כשל ב-trigger = rollback להרשמה (אוכף את ה-invariant). RLS: SELECT-only למשתמש. `trial_ends_at`/billing נשארים NULL — נמלאים ב-2.6 בהזנת כרטיס, לא בהרשמה.

## Phase 1 · Meta OAuth

**1.1 — הפניית OAuth ✅** · `GET /auth/meta/start` בונה redirect ל-Meta עם scopes `ads_management/ads_read/pages_show_list/pages_read_engagement/business_management` + `auth_type=rerequest`. state חתום ב-cookie (CSRF, ללא טבלת oauth_states). ה-user_id מוטמע ב-state (כי ל-callback אין Bearer); ה-URL נושא רק nonce.

**1.2 — Callback + Vault ✅** · `GET /auth/meta/callback` מאמת state → מחליף code לטוקן → **מיד ל-long-lived (~60 יום)** → שולף meta_user_id → UPSERT ל-`fb_connections` (טוקן מוצפן ב-Vault דרך RPC `upsert_fb_connection`). `GET /me/fb-connection` למצב חיבור (בלי הטוקן). אישור-לפני-דריסה ב-frontend. ניתוק לא נדרש (UPSERT).

**1.3 — Facebook Login ✅** · Facebook provider ב-Supabase Auth, scopes `email`+`public_profile` בלבד (נפרד לחלוטין מחיבור חשבון המודעות). Backend-mediated PKCE. **מגן takeover:** email קיים → 409. **Magic Link מומש** (`POST /auth/magic-link` — שליחה לרשומים-בלבד + `GET /auth/magic-link/verify` → refresh cookie; דורש התאמת email-template ב-Supabase).

## Phase 2 · שליפת נכסים

**2.1 — שליפת דפים וחשבונות מודעות ✅** · `GET /me/meta-assets` (דפים + ad accounts, **רק שדות לא-רגישים** — page access_token לעולם לא נשלף). פענוח טוקן דרך RPC ייעודי `get_fb_token` (תלת-מצבי: לא-מחובר/מחובר+טוקן/מחובר-בלי-טוקן). שגיאות: 409 מתויג (`fb_not_connected`/`fb_token_invalid`, לא 401 — כדי לא להתנגש ב-interceptors). אין caching. שליפת מספרי WhatsApp נדחתה ל-2.1.5/Phase 5 (כולל scope `whatsapp_business_management`).

**2.2 — שמירת בחירת נכס בקמפיין ✅** · `POST /campaigns` יוצר draft עם נכסי Meta + `type` (lead/whatsapp). `service_name` נשאר NULL (נקבע ב-3.1). enum status `draft/pending_review/pushing/live/paused/archived` מוגדר מהיום הראשון. RLS: CRUD מלא למשתמש. ולידציה: type↔נכסים + **אימות בעלות מול Meta** (הגנה מערכים מזויפים). **🆕 קמפיין CTWA (Click-to-WhatsApp, `type=whatsapp`) הושלם end-to-end** (`370c963`; היה שבור עד אז) — מודעה שמפנה למספר-הוואטסאפ **של הלקוח** (מודל A): ad-set `destination_type=WHATSAPP` + CTA `WHATSAPP_MESSAGE`, אימות-בעלות מול `get_whatsapp_business_numbers` של המשתמש (לא קו-הפלטפורמה; IDOR נשמר). **לא תלוי WABA/templates** (אלה רק לבוט). `docs/deployment/whatsapp-campaign-setup.md`.

## Phase 2.5 · בחירת חבילה

**2.5 — עדכון חבילה + ToS ✅** · `PATCH /me/subscription` מעביר מ-`pending` ל-tier נבחר (`basic/premium/whatsapp`) + שומר `lead_quota` (נגזר server-side מ-tier+volume ∈ 500/1000; whatsapp→null) + ToS, באותה עסקה. **CAS אטומי** (`WHERE tier='pending'`, לא SELECT-then-UPDATE). ניסיון שני → 409. אין שדרוג/downgrade (ממתין לגיא). server-authoritative — לא מ-body.

## Phase 2.6 · סליקה אוטומטית

**2.6.1 — פלאקארד (Pelecard) ✅** · PSP ישראלי (Stripe אחרי MVP). שכבות: `integrations/pelecard.py` (אגנוסטי) מתחת ל-`integrations/billing.py`. trial 7 ימים — **אנחנו** סופרים (cron יומי), פלאקארד רק שומרת טוקן וסולקת. אימות כרטיס ב-J2 (לא חיוב), חיוב יום 8 ב-J4 על אותו טוקן. טוקן ב-Vault (ב-`billing_token` רק reference). IPN לא חתום → **אימות חוזר server-to-server חובה** + idempotency על `PelecardTransactionId` (לא ParamX). state: `trial→active→past_due→expired/canceled`. `billing_profiles` נפרד (שם/ת"ז/טלפון/שם-עסק — ת"ז אופציונלי אבל נדרש לקיזוז מע"מ). חיוב חודשי: 3 ניסיונות ב-3 ימים → expired. ביטול = canceled, גישה עד סוף period. helper `has_active_billing` (לא נאכף כאן — Phase 3). **קריאת SKILL של פלאקארד חובה לפני קוד.** פתוח לגיא: כמה ניסיונות חיוב לפני expired; פלאקארד Gateway21 vs Match (לאמת עם תמיכה). **🔧 PR2 (0-2#7):** `current_period_start` נכתב **רק** בחיוב מוצלח (`charge_trial_end`), לא ב-`activate` (migration 0077) — אחרת ביטול בזמן trial היה משאיר `cps=NULL` ופותח grace של ~23 ימי גישה חינם. **🔧 PR8c-1/PR8c-2 (0-2#8/#11/#15):** early-IPN reserve-before-fill (IPN שמקדים את שמירת `confirmation_key` → 500→retry של פלאקארד, לא trial תקוע) + מסלול **יישוב ידני** ל-`charge_unknown` (RPC `resolve_charge_unknown`, migration 0083 — אין txn_id ל-auto-reconcile). **billing crons מחווטים ל-`runner.py`** (`process_trial_expirations`/`process_monthly_charges`/`process_past_due_retries`/`retry_failed_invoices`) — פער-ההכנסות נסגר.

**2.6.2 — חשבוניות Green Invoice ✅** · ספק חשבוניות נפרד (PSP=כסף בלבד). מסמך סוג 320 (חשבונית מס/קבלה), ILS, תשלום כרטיס. `integrations/green_invoice.py` (JWT caching, retry על 401). טבלת `billing_invoices` (לקוח אחד = הרבה חשבוניות). הפקה מ-handler של חיוב מוצלח; PDF מגיע דרך webhook (אימות חוזר server-to-server). **כשל הפקה לא מבטל חיוב** (הכסף כבר ירד) → Sentry + cron retry מבוקר (לא לולאה — כפילות). פרטים מ-`billing_profiles`; שינוי בפרופיל לא משכתב חשבוניות ישנות (חוקי). שע"מ/allocation נדחה (מתחת לסף ₪5K). מסלול Green Invoice Extra נדרש.

**תוספת VIP (owner alerts) ✅ 🆕** · (מסמך עצמאי `docs/VIP_ADDON_PLAN.md` — לא סשן ממוספר ב-ROADMAP.) add-on **בתשלום** ל-Basic/Premium: "התראות VIP לבעלים" (וואטסאפ+מייל על לידים). הדלקה על מנוי **active** → **חיוב יחסי מיידי** (proration על הימים שנותרו, payment-first — הדגל נדלק רק אחרי חיוב מוצלח): `derive_vip_proration_agorot` + RPC `finalize_vip_addon_charge` (migration 0097) + `GET /me/subscription/vip/preview` + חשבונית יחסית. הדלקה ב-**trial** = דגל בלי חיוב. כיבוי = **cancel-at-period-end** (migration 0098; מצב "פעיל עד X, לא יתחדש"). נתיב-החיוב **provider-agnostic** (`billing.charge_saved_card`) → יעבור הגירת PSP אוטומטית. התראת-הוואטסאפ עצמה (migration 0090, `vip_lead_alert` בתוך `insert_lead_and_event`, קו-שולח `META_NOTIFY_PHONE_NUMBER_ID`) **רדומה מאחורי gate** עד WABA+template+env; מייל עובד מיד. frontend toggle ב"החבילה שלי" (מסלול preview→אישור→חיוב). *(נפרד מ-ערוץ עדכוני-הסוכן הכללי — agent-notify — ראה 8.3; טלפון נפרד `vip_alert_phone` מול `agent_alert_phone`.)*

## Phase 3 · שאלון + יצירת מודעות (הליבה)

**3.0 — תשתית jobs (queue + worker) ✅** · `enqueue` (תומך user_id=None למערכת), `claim_next_job` (FOR UPDATE SKIP LOCKED — בטוח לריבוי workers), runner עם retry policy `{1:60,2:300}` max_attempts=3. הבסיס לכל הפעולות האסינכרוניות בפרויקט. **🔧 PR7 (P3-W):** הקשחת תשתית ה-worker — `HANDLER_TRANSIENT` registry פר-handler (unknown→fail-fast, External SDK 10) + `_format_error` (str לא repr) + enqueue validation + `AsyncOpenAI(timeout=60)` + `wait_for` סביב `to_thread` ב-meta + migration 0081 (`id` ב-tail של `idx_jobs_claim` — tiebreaker יציב). **🔧 PR8a:** Jobs reaper (RPC `reap_stuck_jobs`, migration 0082) — job תקוע ב-`running`>30 דק' (worker crash בין claim ל-update) → `pending` אם `attempts<max`, אחרת `failed`+alert; tick שעתי.

**3.1 — שמירת שאלון ✅** · `POST/GET /campaigns/{id}/quiz` (upsert, draft-gate) → טבלת `quiz_responses` (migration 0016): `answers` jsonb גמיש + `brand_tone` **עמודה ייעודית** עם CHECK על 5 טונים (`professional/friendly/luxury/direct/authoritative` — תואמים לקבצי-הטון של 3.1.5), composite FK `(campaign_id,user_id)`, RLS. תוכן השאלון: קהל / הצעה / בידול / טון / תקציב / מיקום / גיל+מגדר — הדאטה שמזין את יצירת הקופי. `service_name` מתמלא כאן (מתוך מטרת הקמפיין).

**3.1ב — הגדרת שדות טופס ליד ✅** · (flow שלב 10; מותנה `type=lead` — קמפיין whatsapp מדלג) מבנה ה-**Meta Lead Form**: מה הליד ממלא בפייסבוק בלחיצה על המודעה (נדחף ל-Meta ב-3.4 כ-`FULL_NAME/PHONE/EMAIL/CUSTOM`). שדות קשר: `name`+`phone` **נעולים כחובה** (ניסיון לכבות → 400), `email` — toggle, כבוי כברירת-מחדל. **עד 4 שאלות סינון** מותאמות — **multiple-choice בלבד** (2-3 תשובות לשאלה; התשובות נשמרות אח"כ ב-`leads.screening_answers`). נשמר כ-JSONB יחיד (`lead_form_fields.configuration`, migration 0017, `unique(campaign_id)` — טופס אחד פר-קמפיין, upsert) דרך `POST/GET /campaigns/{id}/lead-form`; ולידציה ב-service (הודעות עברית). עריכה נחסמת אחרי draft (409). **נדחה:** טקסט-חופשי, שאלות מותנות, שדות Meta מובנים (city וכו').

**3.1.5 — תשתית פרומפטים + ממשק בדיקה ✅** · פרומפטים בקבצים, לא בקוד (חוק 8 נולד כאן). **המבנה:** `app/prompts/{phase}/{name}.txt` — היום 4 תיקיות: `phase3/` (copy_generation, copy_generation_single, extract_business_context, image_generation + `tones/` — 5 קבצי-טון עם תוכן אמיתי), `phase5/` (classifiers של הבוט + templates), `agent/` (core + `modules/` פר-בעיה — ראה 7.4.X), `creative/`. **הפורמט:** placeholders של `str.format()` (**לא Jinja2** — החלטה; JSON בתבנית נכתב `{{}}`); הטון מוזרק כ-`{tone_instructions}` מ-`tones/{tone}.txt` (`tone_phase` מאפשר reuse בין phases). **חוזה `build(prompt_name, tone, phase, untrusted, **params)`:** קובץ חסר/ריק/TODO → `PromptNotFoundError`; placeholder חסר → `PromptFormatError`; `untrusted=[...]` (נוסף ב-PR16) עוטף קלט-משתמש ב-`<<USER_INPUT>>` אחרי sanitize (bidi/control/boundary). `ad_generation_service` נכתב **כאן** (מקור-אמת יחיד — ה-tester וה-handlers מריצים את אותו pipeline). **ממשק הבדיקה:** `GET /admin/prompt-tester` (טופס HTML+vanilla JS) + `POST .../generate` מוגן Header `X-Admin-Token` — מריץ extract→copy→3 תמונות ומחזיר גם `prompts_used` (הפרומפטים המלאים, ל-debug). סטייה מהתכנון: `copy_generation.txt` **יחיד** ל-3 הווריאציות (לא קובץ-פר-זווית).

**3.1.6 — חילוץ Business Context ✅ (עבר רה-דיזיין)** · במקור: שלב LLM שחילץ 3 שדות. **הרה-דיזיין (מיגרציות 0091/0092/0096):** `business_type` **בוטל** (לא נצרך); `business_name`+`business_description` נאספים **ישירות בוויזרד** ונשמרים per-user ב-`business_profile` (0091, `GET/PATCH /me/business-profile`); `service_name`+`problem_solved` per-campaign ב-`onboarding_draft` (0092). ה-LLM צומצם מ-`extract_business_context` ל-`extract_industry` (industry בלבד). עמוד עריכה/השלמה `view-business` (נראות מותנית-דילוג דרך דגל `problem_skipped`, 0096). **דילוג בוויזרד חוסם יצירת-קופי** (שער frontend + backend `BusinessContextExtractionError`); השלמת-הבעיה בהגדרות → שחרור. מודל **gpt-5.2**, **נכשל בקול** (לא fallback).

**3.2 — יצירת קופי (gpt-5.2) ✅** · לחיצה ייעודית "צור קופי" → 3 וריאציות נשמרות ב-`ads` — **אחת לכל זווית קבועה**: `emotional` (רגש/סיפור/שייכות) / `pain_solution` (כאב→פתרון) / `result_success` (תוצאה קונקרטית); כל וריאציה = headline+body+cta+image_concept *(לא לבלבל עם 5 זוויות-הפרוטוקול של 7.4)*. **`UNIQUE(campaign_id, angle)`** (`uq_ads_campaign_angle`, migration 0020) — בדיוק 3 ads פר קמפיין; רענון **פר-וריאציה** = UPDATE על אותה שורה לפי angle (התמונה נשארת). ב-0067 ה-UNIQUE הפך partial + זווית `creative_vip` ל-Ad רביעי (ראה 7.7Q). פרומפטים: `copy_generation.txt` (3 בקריאה אחת) / `copy_generation_single.txt` (רענון — מקבל את הקיימות כ"אסור לחקות"). **סינכרוני** (~2-3s; temperature 0.8, timeout 25s). אין retry אוטומטי (המשתמש לוחץ שוב); יש-כבר-3 → 409. status נשאר draft. טרמינולוגיה: "קופי".

**3.3 — יצירת תמונות (gpt-image-2) ✅** · תמונה לכל וריאציה. יצירה ראשונה **async** (jobs, 30-45s); רענון יחיד **sync** (~10-15s). flow משולב: קופי sync → תמונות async אוטומטית. Supabase Storage, bucket `campaign-images`, `{user_id}/{ad_id}/{ts}.png` (RLS לפי prefix). Handler **מקבילי** (`asyncio.gather`), idempotent (`image_url IS NULL`). כשל פר-תמונה לא מפיל את השאר. Medium 1:1 בלבד. status: `generating→pending/failed_image`. עריכת image_concept לא ב-MVP.

**3.4 — העלאת מודעות ל-Meta ✅** · *השלב הכי מסוכן* — אטומי, אוטונומי, כסף אמיתי. יוצר ב-Meta: Campaign + Ad Set + Lead Form (אם type=lead) + 3 Ads, מקושרים, ACTIVE מיד. **החלטות מרכזיות:** SDK רשמי · idempotency כעמודות ב-`campaigns` (כל meta_*_id נשמר תוך כדי flow) · **8 validation gates** (כולל Reach Estimate שחוסם לפני push) · state ביניים `pushing` (CAS+UX) · הפרדת `rejected` (תוכן) מ-`failed_push` (טכני) · **rollback אטומי = DELETE Campaign**; כשל ב-DELETE → `failed_rollback_pending` + Sentry, ידני · כל כשל = restart מלא (לא resume) · retry היברידי: transient אוטומטי (backoff 2s/5s), permanent ידני · token פג → refresh + ניסיון יחיד נוסף · סדר: Lead Form→Campaign→Ad Set→3 Ads (סדרתי) · 3 Ads בסדר angle קבוע (`emotional→pain_solution→result_success`) · שם קמפיין `{service}—{DD-MM-YYYY}` · Privacy URL: default שלנו + אופציונלי ללקוח · cleanup cron לקמפיין `pushing`>5 דק' (בדיקת אמת מול Meta לפי meta_*_id, לא הנחה; 5 ניסיונות) · attempt_error כ-JSONB. **מיפוי כשלים פר-שלב:** `fail_user` (הודעת לקוח ברורה) / `fail_system` (Sentry) / `refresh_and_retry` (טוקן) / `retry` (5xx/429/timeout). **אבן דרך MVP — כאן מביאים לקוח בטא ראשון.** **🔧 PR6 (3#1):** כשל-token באמצע push משאיר את הקמפיין ב-`pushing` (ה-cleanup הקיים 3.4c מנקה אחרי reconnect), לא `failed` עם orphans חיים ב-Meta; 2 Cursor follow-ups — ה-cron מטפל ב-`FbTokenUnavailableError` שנזרק (לא לולאה אינסופית) + ניקוי lead-form יתום שנוצר לפני ה-campaign. **🔧 PR8b-1 (3#2):** token-190 באמצע push → `pushing` ל-cleanup (סימטריה ל-PR6), לא rollback עם token פג שייכשל ויותיר `failed_rollback_pending` יתום. **🔧 Meta socket-timeout (Cursor double-write):** `FacebookSession(timeout=(10,60))` ברמת ה-socket (לא רק `wait_for` שלא מבטל threads) — מונע thread זומבי שמשלים create כפול אחרי retry; `except requests.exceptions.RequestException`→`MetaTransientError` (External SDK 12).

**3.5 — עריכת/שיפור תמונות ✅ (חזר ומומש ב-7.7Q)** · נדחה במקור; חזר כמסלול **Studio** ב-Creative VIP Studio — שדרוג תמונה שהלקוח מעלה דרך images.edit (ראה 7.7Q).

## Phase 4 · קליטת לידים

**4.1 — webhook לידים + idempotency ✅** · Meta שולחת webhook על כל ליד → שמירה ב-`leads` (טבלה משותפת lead+whatsapp; ב-4.1 רק lead). idempotency דרך `webhook_events` (`event_type='meta_lead'`). מיפוי webhook→user דרך `ad_id`→`campaigns` + GIN index. משיכת lead details **סינכרונית** (1-2s, מתחת ל-timeout). כשל → 500 ל-Meta (תעשה retry). **HMAC על raw body** (לא JSON parsed). GET challenge endpoint חובה. subscribe ל-Page קורה **ב-3.4** (אחרי Lead Form), לא ב-OAuth.

**4.2 — ספירת מכסה + התראה ✅** · 80% → warning, 100% → "שקול שדרוג". הליד **תמיד נספר ונשמר** (לא חוסמים קליטה — אכיפה אמיתית ב-Phase 5). **COUNT חי** על leads (לא counter). helper מרכזי `get_quota_status`. התראה פעם אחת בחלון (flags `notified_80_at`/`notified_100_at`, מתאפסים ב-renewal). שליחה דרך jobs (`send_quota_alert`, ה-handler ב-4.6). `tier=whatsapp` (quota=null) → דילוג מוחלט.

## Phase 4.5 · שליפת Meta Insights

**4.5 — Insights ✅** · 4 מטריקות פר-קמפיין (Spend/Leads/CPL/CTR) דרך helper `get_campaign_insights`. leads count: DB ל-lead, Meta API ל-whatsapp. aggregation Campaign+Ad (לא Ad Set). cache **in-memory** dict + TTL (default 300s, גמיש; לא DB/Redis). טווח default `maximum`. **stale-while-error** ל-transient, fail-loud ל-permanent. CPL ב-**same range** (לא anniversary). אין proactive refresh (lazy).

**4.5.1 — חשיפת Insights ✅ (helper פנימי — לא REST)** · **אין endpoint HTTP** (docstring מפורש: "internal — אין endpoint ב-4.5"): `meta_service.get_campaign_insights` + `lead_stats_service` (ספירת לידים בטווח + per-ad) נצרכים פנימית — כרטיס-הסוכן (7.2), ה-monitor (7.4ב-3), הסנדבוקס. endpoint חיצוני ל-Dashboard KPIs = פער פתוח (`backend-gaps.md`).

## Phase 4.6 · התראות מערכת ✅

**4.6 — System Notifications (Email) ✅** · 3 התראות (קמפיין עלה / חיוב הצליח / סף מכסה), ערוץ אחד, handler אחד (`send_notification`). **Resend** (free tier 3K/חודש). idempotency אטומי דרך `sent_notifications`. משלים את ה-jobs של 4.2 ומחבר triggers מ-3.4 ו-2.6.1. **וואטסאפ לא כאן** (דורש 5.0). **התראות הסוכן לבעלים ≠ אלה** — ערוץ נפרד (WhatsApp + agent_alerts_quota — **מומש**, ראה 8.3). מייל בלבד ב-MVP. **הסט התרחב מאז** (ב-`sent_notifications`): `new_lead` (7.2.5) · `quality_followup` (7.5) · `budget_increased` (7.6.5) · `proposal_ready` (8.1) · `vip_lead_alert` + ערוץ `whatsapp` (VIP/agent-notify) — מתועדים בסשנים שלהם.

## Phase 5 · בוט WhatsApp (Premium) ✅ — הושלמה במלואה

**5.0 — הקצאת קו וואטסאפ ✅** · provisioning ידני (admin רושם קו שהוגדר ב-Meta Business Manager). migration `0032` טבלת `whatsapp_lines` (user_id UNIQUE, phone_number_id UNIQUE nullable, status + `pending_provisioning`). `integrations/meta_whatsapp.py` (Cloud API wrapper), webhook handshake, טוקנים מוצפנים (Vault/Fernet). admin client בלבד (§7.3-ב). WABA יחיד שלנו, קו ייעודי פר Premium (trial זכאי). **נדחה:** provisioning אוטומטי של מספר/Display Name דרך Meta APIs, sync תקופתי של quality_rating/name_status, template buttons.

**5.1 — הגדרת בוט ✅** · backend ל-config (1:1 עם user): הודעת פתיחה, עד 5 שאלות סינון (`expected_answer_hint` כרמז ל-LLM בלבד, לא ולידציה), `fallback_action` (ב-UI: `bot_schedule_appointment` + `human_handoff`; `calendly_link` נשאר legacy-runtime לקונפיגים ישנים) ב-`fallback_value` (URL או טלפון E.164). migration `0033_bot_config` (שתי הטבלאות `bot_configs`+`bot_questions`; composite UNIQUE על `(id,user_id)` — הגנת §7.2 מ-service_role leak). יצירת קו `pending_provisioning` אוטומטית בשמירה ראשונה (patch על 5.0). SELECT-only RLS, כתיבה דרך service. (5.4 מפצל את opening_message ל-3 שדות business_name/service_label/closing_cta; 5.5 דורש יומן מחובר אם נבחר bot_schedule_appointment.)

**5.2 — webhook נכנס + מכונת מצבים + שליחה יוצאת ✅** · ליבת הבוט. migration `0034_bot_conversations` (שתי הטבלאות: `bot_conversations` — state כ**עמודה מפורשת**, `answers` jsonb — ו-`bot_messages` append-only). **16 states** (10 מקוריים + 6 תיאום-תורים שנוספו ב-0044): `awaiting_opening_ack` → `asking_question_1..5` → terminal `completed_calendly`/`completed_handoff`/`abandoned`/`send_failed`; תיאום: `scheduling_date`/`scheduling_time`/`scheduling_confirm`/`awaiting_cancellation_confirm` → `completed_appointment_scheduled`/`completed_appointment_cancelled`. `POST /webhooks/whatsapp` עם HMAC (META_APP_SECRET) + idempotency דרך `webhook_events` (Meta message ID). עיבוד **תמיד אסינכרוני** (Meta דורשת 200 תוך 30s) — handlers `initiate_bot_conversation` (אחרי ליד מ-4.1) ו-`process_inbound_message`. מעבר state דרך **CAS** (`UPDATE...WHERE state=:from`). שליחה יוצאת idempotency **דו-שכבתי** (Meta Idempotency-Key + `internal_message_key` דטרמיניסטי). פירוש **נאיבי** (כל טקסט לא-ריק = תשובה תקפה; AI ב-5.3). מדיה → "תומך רק בטקסט". שיחה אחת פעילה פר (user_id, contact_key) — ליד נוסף מתקשר ל-`lead_ids`. transient→retry (3.0), permanent→`send_failed`+Sentry.

**5.3 — אינטליגנציה (AI סינון/חימום) ✅** · `bot_intelligence_service` מחליף את הפירוש הנאיבי בשני classifiers (temperature=0, JSON): `classify_bot_answer` (yes/no/unclear/deviation) ו-`classify_intent` (cancel_appointment/other, ל-5.5e). **חוק 7 בולט:** ה-state machine לא משתנה — LLM מסווג בלבד, קוד דטרמיניסטי מקדם. סטיה → תגובת fallback משפט-חד (LLM, temp 0.7, בטון). JSON פגום/timeout → fallback **סטטי**. prompts `classify_answer.txt`/`classify_intent.txt` (reuse SDK מ-ad_generation). context = 3 הודעות אחרונות. patch ל-`answers`: `{raw, category, value}`.

**5.4 — Templates + Follow-up (23.5h) ✅** · המעבר מ-dev (test phone number) לייצור. **9 templates UTILITY עברית** (registry; היו 7 — עודכן ב-`5ea77f5`): **core-3** שחוסמות את הבוט הבסיסי — `bot_opening` (4 משתנים) / `bot_followup` / `bot_handoff_to_owner`; **6 feature-gated** — 4 של תורים (`bot_pending_appointment`, `bot_appointment_confirmation`, `bot_appointment_rejected`, `bot_appointment_cancelled_owner`) + `bot_new_lead_owner_vip` (VIP) + `agent_update_owner` (עדכוני-סוכן, 8.3). `send_template_message` ב-`meta_whatsapp.py`, `whatsapp_template_registry` ממפה שם דטרמיניסטי→פרמטרים (`meta_name` מ-env). migrations: `0035` (פיצול opening ל-`business_name`/`service_label`/`closing_cta`) + `0036_app_settings` + `0038` (`followup_sent_at`, `pending_initial_send`). **soft-disable** `whatsapp_production_ready` ב-`app_settings` — חוסם שליחה עד אישור כל ה-templates (הרשמה ל-Premium ממשיכה). **follow-up cron שעתי גלובלי** (`last_message_at < now()-23.5h` → template מעיר לפני סגירת חלון 24h), `abandoned` אחרי 48h. שליחה בתוך חלון 24h נשארת טקסט חופשי. הגשה ידנית (admin), versioning ידני. **🔧 PR8b-2 (5.4#5):** `NotConfigured` (env מטא חסר) מטופל כ-**degradation, לא terminal** — לסמן ליד `send_failed` על misconfig בר-תיקון שורף לידים אמיתיים (בפרט ב-soft-disable עד WABA). cron-guard `_whatsapp_sends_enabled` עוטף את השליחות: production-off→skip שקט / production-on+env-off→skip+alert **edge-triggered** (flag ב-`app_settings`, פעם אחת לאירוע, לא per-send) / תקין→שולח. `abandon` (DB בלבד) רץ **תמיד** — שיחה תקועה 48h מגיעה ל-terminal גם כש-WhatsApp כבוי.

**5.5 — תיאום תורים ✅** · פותח `bot_schedule_appointment` (היה נדחה ב-422). migrations `0040-0045` (`google_calendar_credentials` Vault-encrypted, `appointments`, `working_hours`, `special_days`). **5.5a-b:** Google Calendar OAuth (scope `calendar` בלבד, `/auth/google/*`), `google_calendar_service` (connect/refresh/health), RPCs ל-Vault, runbook. **5.5c:** `availability_service` — `get_available_slots` = working_hours ∩ NOT(GCal busy via FreeBusy) ∩ NOT(DB pending/confirmed), special_days גובר. **5.5d:** booking flow — List Picker תאריך→שעה→אישור, `create_appointment` **reserve-first** עם הגנת double-booking (במקור UNIQUE partial index; **🔧 PR5/5.5#2 החליף ל-EXCLUDE constraint על tsrange**, migration 0080 — תופס חפיפת-**משך** 60 דק' מול grid 30 דק', לא רק זהות-slot), **`auto_booking_mode=manual` בלבד**. **5.5e:** פאנל בעל עסק approve/reject/cancel (CAS), calendar sync best-effort (**DB=source-of-truth, יומן=shadow**), ICS endpoint **ללא auth** (UUID=גישה), ביטול ע"י הליד (NLU detect→Quick Reply confirm→ביטול אטומי + מחיקת אירוע + הודעות). יומן אחד לכל user. אישור/דחייה בפאנל בלבד. **נדחה:** reschedule ע"י הליד, recurring, multi-calendar, חגים ישראליים אוטומטיים, reminder jobs. **🔧 PR5 (5.5#1):** `get_freebusy` עושה fail-closed על `errors` פר-יומן (notFound→permanent, rateLimit→transient) במקום `[]`="פנוי" כוזב שהציע slots מעל אירועים אמיתיים. **🔧 PR8a (5.5#3):** GCal orphan reconciler — tick שעתי, 2 flows (forward=קישור event_id חסר, reverse=מחיקת אירוע יתום ב-Google אחרי ביטול ב-DB), `list/delete` מבחינים no-creds מ-lost (לא false-alert ללקוח מנותק); follow-up — reverse-query מסנן `status='cancelled'` בלבד. **🆕 הפעלה + חיווט (סשן 7.x, יולי):** תיאום-התורים היה **dark-launch** (דגל `appointment_scheduling_enabled=false` מ-0045) — **migration 0112 הדליקה אותו**. ה-UI עבר מ-Calendly ל**קביעה ישירה ביומן Google** (`c1724c3`). **חיווט bot-config frontend** (`2cc3dc7` — wizard + דשבורד `view-bot`, Premium-gated, כלל 12: איסוף/ולידציה משותפים). **פאנל התורים (חלק 11):** `view-appointments` (אישור/דחייה/ביטול, CAS) + **תצוגת לוח-שנה חזותית** (גריד חודשי RTL) + deep-link `/my-appointments` (נתיב-SPA **נפרד** מ-API `/appointments`, ממצא Cursor HIGH) + badge חיבור-יומן. defense-in-depth `is_appointment_scheduling_enabled()` ב-runtime.

## Phase 6 · token refresh (תחזוקה) ✅

**6.1 — cron רענון טוקן Meta ✅** · הטוקן תקף 60 יום; בלי רענון הקמפיין "מת בשקט" ביום 61. migration `0047` (RPC `refresh_fb_token`, Vault+DB אטומי) + **patch חוסם:** `upsert_fb_connection` מאפס `refresh_failed_at`/`refresh_error` בחיבור מחדש. handler `refresh_meta_token` (3 בדיקות idempotency; reuse `exchange_long_lived_token`; transient→raise+retry, permanent→`mark_token_expired`). **tick יומי monotonic** ב-`runner.py` (כמו cleanup של 3.4) — מרענן טוקנים `token_expires_at ≤ now()+30d AND refresh_failed_at IS NULL`. לא: notification ללקוח / scheduler_state persistent / טבלת היסטוריה. **🔧 PR8b-1 (6.1#1/#2):** ה-handler מבדיל transient (mixin → raise+retry) מ-unknown (terminal+Sentry, לא propagate→re-pick אינסופי); `mark_token_expired` עם `best_effort=False` (כשל ה-mark עצמו → raise → retry, אחרת ה-terminal לא נכתב). מקביל ב-notifications (4#10/#12): `UnexpectedAuthError`→mark_failed+capture (לא zombie pending), `mark_notification_sent` בודק `has_returning_rows`→alert על drift.

## Phase 7 · סוכן AI פנימי + אופטימיזציה מאושרת ✅ (רובה)

> **שינוי מבני:** Phase 7 התרחב מ"צ'אט בלבד" ובלע את האופטימיזציה ה**מאושרת** (creative-swap end-to-end עם חלון מדידה). **המצב העכשווי: 4 סוגי-בעיות מחוברים end-to-end** — `high_cpl`(1), `low_quality_leads`(2), `meta_rejection`(3), `budget_mismatch`(4, נוסף ב-7.6.5). האופטימיזציה **כוללת תמונות** (opt-images — לא רק טקסט). בנוסף: **Creative VIP Studio** (7.7Q) ו**סנדבוקס** (בדיקת הסוכן בלי Meta אמיתי, ראה 7.x) — מומשו. מה שנשאר ל-Phase 8: ניטור **יזום** (8.1 — high_cpl **מומש**) ופעולות **בלי אישור אנושי** (8.3).

**7.1 — צ'אט הסוכן בדשבורד ✅** · migration `0048` (`agent_conversations` + `agent_messages`, RLS write-deny + composite FK לקמפיינים). `agent_service` (reuse seam OpenAI) + 5 endpoints `/me/agent`. system prompt מומחה-שיווק + הזרקת `service_name` (מטריקות ב-7.2). **מודל "סופרים רק אחרי הצלחה":** בדיקת quota → קריאת OpenAI → **רק אז** INSERT אטומי (RPC) של 2 הודעות + `updated_at`. כשל OpenAI = לא נשמר, quota נשאר, UI retry. 50/חודש. עדיין רק צ'אט — אין פעולות אוטונומיות.

**7.2 — מטריקות קמפיין בצ'אט ✅** · בלי migration. `GET /me/agent/conversations/{id}/status` — כרטיס 4 מטריקות פר type (lead: עלות-לפנייה/שיעור-פניות/פניות-היום/סך-הפניות; whatsapp: לשיחה/שיחות). helper `get_campaign_status_for_agent`: `link_clicks` + conversion_rate = **leads/קליקים** (לא חשיפות — מניעת כפילות CTR) + today (DB לצבירים, Meta `date_preset=today` לוואטסאפ). `system_v2` מזריק את הכרטיס + extra_context בכל הודעה. cache **ממופתח לפי `date_preset`** (max-range + today במקביל). `_today_count` best-effort — כשל ב-today לא מפיל סטטוס/צ'אט.

**7.2.5 — עמוד לידים (CRM-lite) ✅** · migration `0049` (`lead_status` + RLS + התראת מייל `new_lead` אטומית, reuse תשתית 4.6). `leads_service`: `GET /me/leads` (offset pagination + tiebreaker על created_at) + `PATCH /me/leads/{id}` (סטטוס). **חריג ל-server-authoritative:** רק `lead_status` נכתב ע"י הלקוח דרך **user client + column-grant**; ה-INSERT עדיין admin/webhook. `screening_answers` JSONB (תשובות טופס-סינון; מוצג בעמוד הליד `view-lead`).

**7.3.5 — תשתית פרוטוקול האופטימיזציה ✅** · migrations `0050` (`campaigns.industry` — denormalize מ-3.1.6, NULL מותר/fail-safe) + `0051` (2 טבלאות). `optimization_sessions` (**סדרה**: problem_type, status, loop_count, `starting_metric` jsonb, current_step) + `optimization_actions` (**פעולה**: step_number, action_type, post_change_metric, `window_ends_at`, requires_approval, approved_at). **partial unique index `uq_open_session_per_problem`** — סדרה פתוחה **אחת** פר (campaign, problem) = חוק ה-Lock. RLS SELECT-only, כתיבה admin. `benchmark_service` (**דטרמיניסטי 100%**): טוען JSON, מסווג CPL → amazing/average/expensive/unknown, 7 ענפים. `docs/agent/KNOWLEDGE_BASE.md` בריפו (גיא עורך ישירות — benchmarks, troubleshooting triggers, עונתיות).

**7.4א — בעיה 1: חוק הברזל, Lock, אבחון ✅** · migration `0052` (RPCs `open_optimization_session` עם advisory lock + `insert_agent_assistant_message`). **חוק 7 כקוד:** `POST /me/agent/conversations/{id}/diagnose` **לא** קורא LLM ישר — `agent_orchestrator` דטרמיניסטי קודם: (1) שליפת מטריקות, (2) `lock_service.check_lock` על `window_ends_at` של חלון קודם, (3) `benchmark_service.classify_cpl` → `benchmark_stop` אם amazing/average (chips "מקבל"/"בכל זאת" — עצירת חוק הברזל), (4) היסטוריה `build_mixed_analysis` (X→Y→Z אם יש, None לתלונה ראשונה), (5) פתיחת סדרה race-safe עם `starting_metric`=CPL נוכחי, (6) **רק עכשיו** קריאת LLM לניסוח, (7) **עצור לפני פתרון** (הקופי/הצעה ב-7.4ב). 3 תוצאות: lock / benchmark_stop / diagnosis. prompt `problem_1.txt` מלא (לא נאכף בקוד עד 7.4ב).

**7.4ב — בעיה 1: מנוע הפרוטוקול (3 חלקים) ✅**
- **ב-1 · פתרון:** `solution_service` — 3 וריאציות (קופי+כותרת), בחירת זוויות מתוך 5 (כאב/חלום/דחיפות/הוכחה-חברתית/סמכות) בדילוג על מה שכבר נוצל בסדרה. `POST .../propose` מחזיר 3 + step_number. **🆕 opt-images (מיגרציות 0105-0109):** כל וריאציה מקבלת **תמונה חדשה** מ-`image_concept` משלה — **לא עוד "טקסט בלבד"** (התיעוד המקורי היה שגוי; text-only מעולם לא נדרש ע"י גיא). **async (A3):** יצירת-תמונות eager בזמן propose (job `optimization_image_generate`, 0107) → מוצגות ב-`view-action` לבחינה **לפני** approve; fallback gen→push ב-approve (0106). דגל `include_images` per-action (כבוי→recycle text-only הישן). **מתג-חירום גלובלי** `optimization_images_forced_off` ב-`app_settings` (כופה recycle בלי deploy). idempotency: `generated_image_paths[i]` reserve-first אחרי upload (RPC אטומי פר-slot 0109; `image_gen_settled` 0108 מונע "images_generating נתקע"). **`save_proposed_action` (0093):** ההצעה נשמרת כ-action כבר ב-**propose** (`push_status=NULL`, `approved_at=NULL` = "מוצע, טרם אושר"); ה-approve תופס ב-CAS (NULL→pushing) — הבסיס לעמוד-האישור (`view-action`) ולסריקת-הרקע של 8.1.
- **ב-2 · אישור + העלאה:** `optimization_push_service` — creative-swap **אטומי בדפוס 3.4** (claim/resume RPC, save-as-you-go, rollback מלא). migrations `0053`+`0054`. **D1:** pause מתבצע **אחרון** (אחרי כל המודעות חיות) → כשל ביצירה לא נוגע בישנות. **D2:** reactivate-fail → `push_rollback_pending` + Sentry (ידני, אין auto-recovery). **D3:** זיווג וריאציה↔מודעה **פוזיציוני** (לא לפי angle key). `POST .../approve` עם `step_number` (optimistic concurrency). בהצלחה: `window_ends_at = now()+120h` (**הנעילה מתחילה רק אחרי ההעלאה**). **🔧 PR3 (7.4#1):** CAS-lease ל-push action (migration 0079) — approve מקבילי→409, `p_acquire_lease` מבחין approve(true) מ-worker resume(false, idempotent כלל 9); מונע מודעות כפולות. **🔧 PR4 (7.4#2):** כשל ב-`finalize` (אחרי שה-swap כבר בוצע ב-Meta) → **תמיד** transient→resume, **לעולם לא rollback** — אחרת היה מוחק 3 מודעות חיות תקינות; משלים: class-40 (serialization/deadlock) נוסף ל-`_TRANSIENT_SQLSTATE_PREFIXES` (תופס בכל ה-services).
- **ב-3 · סגירת לולאה:** `optimization_monitor_service` — cron **שעתי דטרמיניסטי בלי LLM** (חוק 7: השוואת מספרים בלבד). חלון נגמר → מדידת Y מול X: שיפור (cpl<X) → `success_monitoring` + חלון שני +120h; אחרת → `done`. חלון שני: עדיין-משופר → סדרה `done`; נסוג → חזרה ל-`in_progress`. `advance_to_next_step` (2→6 angle_change, 6→7 offer_change, 7→loop_count++; חוק הלולאה דחוי). migration `0055`. tick ב-`runner.py`.

**7.4.X — ארכיטקטורת פרומפט הסוכן (Core+Module+Payload) ✅** · פרומפטי-הסוכן במבנה מודולרי: **Core קבוע** (זהות/כללים) + **מודול-בעיה פעיל אחד** + **SYSTEM_PAYLOAD מובנה** (המספרים/ההקשר), מורכבים ב-`build_agent_prompt(mode=chip|free)` ב-`prompts_service`; המודולים ב-`app/prompts/agent/modules/{high_cpl,low_quality,rejection,budget_mismatch}/` (למשל high_cpl: stage_a/stage_c/lock/benchmark_stop/expensive_*). חוק 7 בארכיטקטורת הקבצים עצמה: הקוד מכריע דטרמיניסטית ובוחר מודול+payload — הפרומפטים בלי if/then, ה-LLM רק מנסח. (ב-ROADMAP מופיע כתכנון בלבד; אומת כממומש מהקוד.)

**7.5 — בעיה 2: לידים לא-איכותיים (filter_addon) ✅** · **redesign** — במקום 3 קופי חדשים מאפס, **עריכת 3 המודעות החיות במקום + הוספת משפט מסנן** (edit-in-place; הבעיה = חוסר פרט-סינון, לא קופי גרוע). **2 קטגוריות אבחון:** `wrong_area` (לא מהאזור) / `no_budget` (אין תקציב מתאים) — `dont_understand`/`standards` **בוטלו**. **אבחון תלת-שלבי** (`diagnose_problem_2`): 2 chips → value-turn (איסוף אזור/מחיר, בלי סדרה) → פתיחת סדרה + LLM. `_STEP_PLANS["low_quality_leads"]={1:filter_addon, 2:screening_question}` (angles זהים ל-`_OLD_AD_ANGLE_ORDER` לזיווג פוזיציוני). מיגרציות 0094 (עריכת draft ידנית + autosave, RPC CAS על `push_status IS NULL`) + 0095 (מצב-2 "השתפר? כן/לא", מייל `quality_followup` 120ש'). `docs/low-quality-leads-filter-redesign.md`.

**7.6 — בעיה 3: דחיית Meta (rejection_fix) ✅** · מונע ע"י **webhook+worker**, לא צ'יפ-אבחון: `ad_review` webhook → `rejection_intake_service` → job `handle_meta_rejection` → `generate_rejection_fix` (גרסה מתוקנת **אחת**, שומרת angle — חוק 7) → התראת `ad_rejected`. `diagnose_problem_3` **רק מציג** את התיקון המוכן (אין LLM/benchmark). `rejection_fix` = action_type **עצמאי** (מחוץ ל-`_STEP_PLANS`; `rejection_push_service` נפרד, בלי equality-gate 3:3, `window=None`). **opt-images בדחייה:** תמונה חדשה למודעה הנדחית **בלבד** (כירורגי 1+1 — לא מאפס פאזת-למידה של 2 התקינות). **נדחה ל-setup ידני:** subscribe ל-`ad_review` ב-Meta App Dashboard.

**7.6.5 — בעיה 4: חוסר-התאמה תקציבית (budget_mismatch / "מעט פניות") ✅ 🆕** · הקמפיין **בריא** אבל הלקוח מתלונן על מעט לידים כי התקציב קטן. **אבחון מתמטי דטרמיניסטי** (חוק 7 — הקוד מחשב, LLM רק מנסח; `market_cpl=(amazing_max+expensive_above)/2`, ה-CPL החי **אינו** קלט — מניחים קמפיין בריא). **redesign** (`problem4-budget-redesign-plan.md`): **אין יעד-לידים** בזרימה (`monthly_lead_goal` הוסר מהשאלון; העמודה 0102 נשמרה nullable), **המשתמש מקליד תקציב** (`budget_input`→preview→approve; ולידציה server: `>נוכחי` ו-`≤נוכחי+100₪`, `math.isfinite`). **2 מסלולים:** (א) **הגדל תקציב** (`approve_budget`: reserve-first `budget_agreements` → `update_ad_set(daily_budget)` → sync quiz → התראת `budget_increased`; רמת ad_set, אגורות/`ceil`, rollback על כשל Meta); (ב) **השאר תקציב** → אופציונלי **force-creative** (מיני-session בדפוס rejection, 3 וריאציות, `push(window=None)`, סגירה מיד ל-`done`). **לולאה reactive** דרך `budget_agreements.fetch_last` — **בלי cron** (התיעוד הוא הזיכרון). מיגרציות 0103 (`budget_agreements` audit append-only) + 0104 (`problem_type += budget_mismatch`) + 0111 (`budget_increased` notification). `docs/budget-mismatch-flow.md`.

**7.7Q — Creative VIP Studio ✅ 🆕** · לשונית **Premium-בלבד** ליצירה/שדרוג קריאייטיבים **ביוזמת הלקוח**, מנותקת מהקמפיין/פרוטוקול. 3 מסלולים + גלריה: **Creator** — תמונה מ-prompt חופשי (gpt-image-2), הלקוח כותב את הקופי **ידנית** (AI = תמונה בלבד), פרסום כ-**Ad רביעי** (`angle='creative_vip'`) לקמפיין ה-live; **Studio** — שדרוג תמונה שהלקוח מעלה (images.edit; ה-`UploadFile` הראשון בפרויקט, JPG/PNG≤10MB) — **החזרה של 3.5 שנדחה** — לגלריה בלבד, לא מתפרסם; **Revise** — שינוי איטרטיבי של תוצר קיים בהוראת-משתמש. מסלול וידאו/אנימציה — בוטל. טבלת `creative_assets` (migrations 0063-0071 + 0074), **מכסות חודשיות** (anniversary, COUNT חי, אכיפה אטומית ב-RPC + advisory lock פר-user): יצירה **15** / פרסום **15** / שדרוג **30** / revise **5 פר-תמונה**. 6 endpoints `/me/creative/*` (quota/gallery/generate/publish/upgrade/revise). החלטות: **אין בדיקת-מדיניות על הטקסט הידני** — רשת-הביטחון היא דחיית-Meta → בעיה 3 (תלות מפורשת ב-7.6); checkbox אישור-אחריות לפני פרסום. `image_url` = storage path, signed URL טרי on-read.

**7.x — סנדבוקס (בדיקת הסוכן) ✅ 🆕** · (היה 🔮 "תשתית עתידית" — **מומש**). migration 0101: `campaigns.is_sandbox` + טבלת `sandbox_inputs` (admin מקליד ידנית CPL/spend/leads/ctr/clicks). **הרעיון:** קמפיין `is_sandbox=true` → `meta_client_factory` מזריק **אוטומטית** `SandboxMetaClient` (no-op ל-Meta, שולף מ-`sandbox_inputs`) במקום `RealMetaClient` — הסנדבוקס מריץ את **אותם services** של הזרימה האמיתית, **בלי** `if is_sandbox` בקוד-הסוכן. `sandbox_service`: `run_diagnose` (4 הבעיות) / `run_budget_action` / `run_propose` / `advance_time` (מזיז חלון-מדידה ומריץ cron אמיתי). admin UI (`admin/sandbox.html`) + `capture_prompts` (מציג את הפרומפטים שנשלחו). `docs/sandbox-agent-prompts.md`.

## Phase 8 · ניטור יזום + אופטימיזציה אוטונומית מלאה 🔨 (התחיל)

> מה שנשאר אחרי ש-7.4 בלע את האופטימיזציה ה**מאושרת**. תקרת-שינוי + לוג מלא ב-`optimization_actions` כבר קיימים מ-7.4.

**8.1 — ניטור יזום (high_cpl) ✅** · (היה 🔮 — **מומש** ל-high_cpl). cron **יומי** `daily_high_cpl_tick` סורק את **כל הקמפיינים החיים** ומזהה CPL גבוה **בעצמו** בלי לחיצת לקוח (בשונה מ-cron 7.4ב שמודד רק פעולות **מאושרות**). גרעין-אבחון **משותף** `assess_high_cpl` ב-`agent_orchestrator` (חולץ מהצ'אט — מונע drift, פתרון-שורש). רק `expensive` → פעולה (`amazing/average/unknown/no_data` → **skip שקט**; הרקע שמרן — **אין override**). `expensive` → `open_session` (אידמפוטנטי) → `generate_solution(eager_images)` (3 קופי + 3 תמונות) → **עוצר ב-`push_status=NULL`** → התראת `proposal_ready` → מכאן approve→push→ניטור זהה לצ'אט (**אפס פעולה על Meta בלי אישור אנושי**). **רק high_cpl** (היחיד מדיד-מספרית לזיהוי-עצמאי; דחייה=webhook, מעט-לידים=תלונה). migration 0113. `high_cpl_scan_service.py`, `docs/high-cpl-flow.md`.
**8.2 — בעיות 2-4 ✅ (מומשו ב-7.5/7.6/7.6.5)** · ראה Phase 7 — בעיה 2 (filter_addon), בעיה 3 (Meta-rejection), בעיה 4 (budget_mismatch). מחוברות end-to-end + נתמכות בסנדבוקס.
**8.3 — ערוץ התראות-בעלים ✅ / ביצוע אוטונומי 🔮** · **הערוץ מומש** (agent-notify, מוקדם — לפני שאר Phase 8): עדכוני-סוכן **מייל (תמיד) + וואטסאפ (opt-in)** per-account, מכסה `agent_alerts_quota=30/חודש` (ספירה אטומית ב-RPC, anniversary window). יעד **נפרד מ-VIP**: `agent_alert_email` (חובה, נאסף בשאלון) + `agent_alert_phone` (אופציונלי), נערך בטוגל ב"החבילה שלי". 6 סוגי-התראות (`step_advanced`/`series_resolved`/`ad_rejected`/`quality_followup` + `budget_increased` + `proposal_ready`). וואטסאפ רדום עד WABA+template (`whatsapp_production_ready`); מייל עובד (Resend). מיגרציות 0100/0110/0111/0113. **מטריצת `notification_preferences` (חזון 7.7W) לא נבנתה** — מודל פשוט (opt-in יחיד + מייל-תמיד). **נותר ל-8.3:** רק הטריגר לפעולות **אוטונומיות בלי אישור** (אחרי שבעיה 1 תעבור אישור-סדרה).

## תשתיות רוחביות

**Sandbox פנימי לבחינת הסוכן ✅ 🆕** · (היה 🔮 — **מומש**, ראה 7.x). קמפיין-דמה (`is_sandbox`) + הזנת מספרים ידנית (`sandbox_inputs`); הסוכן האמיתי רץ מולו דרך **נקודת-החלפה אחת** (`meta_client_factory`→`SandboxMetaClient`), פעולות נכתבות ל-`optimization_actions` (לא ל-Meta). admin UI + `capture_prompts`. **הושג היעד "נדרש לפני Phase 8".** *(מגבלה: לא בודק איכות-שיפוט על דאטה אמיתית — רק קמפיין חי.)* **תוספת מתוכננת 📋:** השוואת Multi-LLM פר-תפקיד — ממשק `LLMClient` יחיד (עיקרון `MetaClient`) עם תווית-תפקיד (`classifier`/`advisor`/`copywriter`), 3 ספקים במקביל בסנדבוקס (`sandbox_llm_comparisons`), ספק-אחד-פר-תפקיד בפרודקשן; תנאי-מקדים: ריכוז כל קריאות ה-LLM לשכבה אחת.

**תמיכה דו-כיוונית 🆕** · (החליף את bug-reports הישן שהוסר — migration 0137 DROP). כפתור-תמיכה גלובלי → `support_threads`/`support_messages` (thread דו-כיווני, פתוח-תמיד) + התראת-טלגרם לצוות (ערוץ-notification חדש); ניהול ב-`/admin/support`. אסקלציות-אוטומטיות (4 תנאי-כשל Meta → טלגרם+הודעה-אחידה) = סבב-2. פירוט: `docs/SUPPORT_ESCALATION_PLAN.md`.

---

## שכבת הקשחה — תיקוני code-review (תוכנית 24 PR) 🔨

> **לא פאזה ב-ROADMAP** אלא שכבת איכות/הקשחה רוחבית מעל הפאזות הקיימות, מתוך 5 סקירות-קוד. מאורגנת כ-24 PR סביב **תבניות-שורש** (race/TOCTOU, terminal-state, classify-by-meaning, create→persist gap...). מקור-אמת: `docs/code-reviews/` (`FIXES_EXECUTION_PLAN.md` = מסמך-העל, `PROGRESS.md` = מעקב). כל PR מתקן את ה**שורש**, לא טלאי. הפאזות המושפעות מסומנות `🔧` לעיל.

**הושלם (26 יחידות + 3 root-fixes רוחביים):**
- **PR1 · Auth/OAuth takeover (P0-2-S)** — 4 שכבות הגנה-בעומק (logout global / set_session≠sign_out / sign_up דוחה OAuth-email / nonce single-use). migrations 0075-0076. → פאזה 0.5.1
- **PR2 · cps timing (0-2#7)** — `current_period_start` רק בחיוב מוצלח → אין ~23 ימי גישה חינם. migration 0077. → פאזה 2.6.1
- **PR3 · claim/lease אטומי (תבנית A)** — `open_meta_rejection_session` (session+job אטומי, אין session יתום שחוסם דחיות) + CAS-lease ל-push action (approve מקבילי→409). migrations 0078-0079. → 7.4ב + טיפול Meta-rejection
- **PR4 · finalize≠rollback (7.4#2)** — כשל finalize אחרי swap מוצלח → resume, לא מחיקת מודעות חיות; class-40 transient בכל ה-services. → 7.4ב
- **PR5 · double-booking (P5-A)** — FreeBusy fail-closed + EXCLUDE constraint על tsrange (חפיפת-משך). migration 0080. → 5.5
- **PR6 · orphan-on-token-fail (3#1)** — push שכשל על token נשאר `pushing` ל-cleanup (לא orphans חיים ב-Meta); 2 Cursor follow-ups (FbTokenUnavailable + lead-form יתום). → 3.4
- **PR7 · תשתית worker (P3-W)** — HANDLER_TRANSIENT registry, AsyncOpenAI timeout, wait_for ב-meta, migration 0081. → 3.0
- **PR8a · reaper + GCal reconciler (P5-B 1/3)** — jobs reaper (0082) + orphan reconciler ל-Google Calendar (forward/reverse, no-creds≠lost). → 3.0 + 5.5
- **PR8b-1 · terminal-state: token+notifications (2/3a)** — unknown→terminal+Sentry (לא re-pick אינסופי); `mark_token_expired(best_effort=False)`. → 6.1
- **PR8b-2 · terminal-state: conversation (2/3b)** — NotConfigured כ-degradation≠terminal; cron-guard `_whatsapp_sends_enabled` + alert edge-triggered; abandon רץ תמיד. → 5.4
- **PR8b-3 · terminal-state: optimization (7.4#3/#7)** — permanent/unknown במדידה→terminal (escalated); reconciler `reconcile_stuck_pushes` (action תקוע `pushing`>שעה→escalate). → 7.4ב
- **PR8c-1/PR8c-2 · billing reconciliation (0-2#8/#11/#15)** — early-IPN reserve-before-fill (500→retry) + מסלול יישוב ידני ל-`charge_unknown` (RPC, migration 0083). → 2.6.1
- **PR9 · classify-by-meaning** — 5 מסווגים לפי משמעות (meta_whatsapp/lead_intake/meta/image/observability). → רוחבי
- **PR10 · create→persist gap** — search-before-create ל-ad (double-spend בלבד), migration 0085. → 3.4
- **PR12 · LLM-לפני-idempotency** — search-before-write לפני LLM (3 מופעים). → 5.4/7.6
- **PR13 · agent chat robustness (חלקי)** — סיווג Meta/Fb + `PromptsServiceError` מרכזי (7.1#3 נדחה→חוב). → 7.1
- **PR14 · duplicate invoice** — retry search על טווח [period_start, today]. → 2.6.2
- **PR15 · RPC duplicate (no-op)** — השורש כבר נסגר ב-0039 (Session 5.2). → 4.1
- **PR16 · prompt-injection guardrail** — `build(untrusted=[...])` מרכזי (wrap+sanitize): phase3×4 + phase5×3 + free-chat. → 3/5
- **PR17 · billing/state correctness (0-2#10/#12/#13/#28)** — CAS `status='trial'`, Pelecard normalize, whatsapp IDOR דרך DB. → 2.x
- **PR18 · insights/quota correctness (4#2/#3/#5, 3#8)** — action_types set, `_safe_float` clamp, migration 0087. → 3/4
- **PR19/PR20/PR22 · תחזוקה** — PR19 primitive דו-משמעי (no-op) · PR20 derive-once/SSoT (5 action_types מרוכזים) · PR22 CAS predicate (3 תוקנו). → תחזוקה
- **PR21 · soft-disable (5.4#1)** — `clear_pending_initial` ב-inbound (מונע opening כפול + follow-up אובד; היה ⏸️ חסום — שוחרר). → 5.4
- **PR23a · PII — Pelecard** — masking 10 לוגים + סינון `webhook_events.payload`. → 2.6.1
- **root-fixes רוחביים:** (א) **TransientError mixin** — אחרי 7 ממצאי Cursor רצופים על tuples ידניים שנשכחו, marker אחיד ב-`app/core/transient` ל-46 transient classes + טסט CI אוכף (כלל 11 התהפך tuple→mixin); (ב) **Meta socket-timeout** — `FacebookSession(timeout=(10,60))` מונע thread זומבי→double-write (External SDK 12); (ג) **class-40 transient** ב-`db/errors` (serialization/deadlock).

**ממתין (2 יחידות):** PR23b (PII — Meta/WhatsApp payload + APIError audit) · PR24 (אצוות תחזוקה פר-קובץ). **PR11 ⛔ בוטל** (rollback, החלטת-מוצר — no_signal 3-way + `insufficient_data` בוטלו כ"תכנון-AI ישן"; cpl=None→אי-שיפור→advance).
>
> _ספירה: 24 PR במקור, חלקם פוצלו (PR8→8a/8b-1/8b-2/8b-3/8c-1/8c-2; PR23→23a/23b). **26 יחידות ✅ · PR11 ⛔ בוטל · 2 ⬜ נותרו (PR23b, PR24).**_

**חוב טכני פתוח (לא ב-PR ייעודי):** `google_calendar.py` — אין socket timeout על ה-Google SDK (worker עלול להיתקע על call איטי; לא double-write כי אין retry-on-timeout). תיקון: timeout דרך `AuthorizedHttp` ב-build_service.

---

## אבני דרך

| אבן דרך | מתי | סטטוס |
|---|---|---|
| הצינור עובד (Render: web+worker, migrations אוטומטיות) | אחרי 0.2 | ✅ |
| MVP מוכח (מודעות באוויר) — **לקוח בטא ראשון** | אחרי 3.4 | ✅ |
| לולאת לידים סגורה | אחרי 4.2 | ✅ |
| בוט עובד (כולל תיאום תורים ביומן Google) | אחרי 5.3 / 5.5 | ✅ |
| רענון טוקן (אין מוות שקט ביום 61) | 6.1 | ✅ |
| סוכן צ'אט + מטריקות + CRM-lite | 7.1–7.2.5 | ✅ |
| אופטימיזציה **מאושרת** (בעיה 1, end-to-end) | 7.4ב | ✅ |
| 4 סוגי-בעיות בסוכן + תמונות + Creative Studio + סנדבוקס | 7.4–7.7Q | ✅ |
| ניטור יזום (high_cpl, בלי אישור-מוקדם) | 8.1 | ✅ |
| frontend מחווט end-to-end (~79 endpoints) | רוחבי | ✅ |
| אוטונומיה מלאה (ביצוע בלי אישור) | 8.3 | 🔮 |
| שכבת הקשחה (code-review) | רוחבי | 🔨 26 ✅ / 2 נותרו |

---

## פתוח / דחוף

- ✅ **פער הכנסות — נסגר:** החיוב עבר **לפר-קמפיין** (PR6–PR8b; `campaign_billing_service` — trial-expiry/cycle-renewal/resume, כולם מחווטים ורצים ב-`runner.py`). ה-billing crons הפר-user (`process_trial_expirations`/`process_monthly_charges`/`process_past_due_retries` + `_charge_subscription`) **הוסרו ב-PR10-A** (התייתרו — היו DEAD-by-design מאחורי `_has_campaign_billing`).
- **ייצור WhatsApp (תפעולי, חוסם הפעלה אמיתית):** הקמת WABA (טרם בוצעה) + הגשת 7 ה-templates ל-Meta + App Review + הפעלת flag `whatsapp_production_ready` ב-`app_settings`. עד אז הבוט עובד רק עם ה-test phone number של Meta. **מדריכים:** `docs/deployment/waba-setup.md` (הקמה צעד-אחר-צעד) · `docs/deployment/meta-templates-submission.md` (הגשת templates).
- **ממתין לגיא:** תוכן `KNOWLEDGE_BASE.md` לענפים (benchmarks, troubleshooting triggers, עונתיות) · אפיון בעיות 2-3 · domain `api.campaign-ai.com` סופי · Privacy policy URL.
- **ממתין לגיא (כללי):** מדיניות ניסיונות חיוב לפני expired (2.6.1) · Magic Link (1.3) · שדרוג/downgrade חבילה (2.5) · אפיון Phase 8 (ניטור יזום + אוטונומיה).
