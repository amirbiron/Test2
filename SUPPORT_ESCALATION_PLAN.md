# תוכנית שינויים — מערכת תמיכה דו-כיוונית + אסקלציות-אוטומטיות לטלגרם

> **מסמך אפיון להעברה ל-Claude Code.** זהו הוראת-שינוי: מה לבנות, מה להסיר, ולמה. המסמך נשען על הארכיטקטורה הקיימת (spec.md, ROADMAP) ומרחיב אותה — הוא לא סותר אותה, פרט להסרת `bug-reports` שמוסבר במפורש בהמשך.
>
> **הקשר למי שמבצע:** המנגנון הזה מורכב משני חלקים שיושבים על אותה תשתית-התכתבות אבל מופעלים מכיוונים הפוכים — **תמיכה-יזומה** (המשתמש לוחץ כפתור) ו**אסקלציות-תגובתיות** (המערכת מזהה תקלה ומתריעה לצוות). חשוב לא לבלבל ביניהם: הם חולקים את ה-thread-בממשק ואת צינור-הטלגרם, אבל מה-שמפעיל-אותם, ומה-שהמשתמש-רואה, שונה לגמרי.
>
> עדכון אחרון: יולי 2026. מבוסס על הכרעות-מוצר של אמיר (החלטות §0).

---

## 0. ההכרעות שהוכרעו (מקור-האמת לשינוי הזה)

ריכוז כל ההכרעות הסופיות לפני הפירוט הטכני. אלה לא נתונות לפרשנות — הפירוט בהמשך רק מיישם אותן.

**bug-reports הקיים מתבטל לגמרי.** המנגנון הקיים (`POST /me/bug-reports`, עמוד-האדמין-המצרפי, המיגרציה שלו) נועד לאיסוף-פאסיבי-לניתוח-עתידי. הוא מיותר עכשיו, כי כל דיווח מגיע ישירות לצוות לטיפול-מיידי. המנגנון החדש **מחליף** אותו — לא יושב לצידו. זו הסרה, לא הוספה.

**כפתור-תמיכה גלובלי.** כפתור "פנייה לתמיכה" נגיש מהממשק (לא דווקא בצ'אט-הסוכן — כפתור גלובלי, זמין מכל מסך). המשתמש כותב את הבעיה שלו → נפתח thread-תמיכה + נשלחת התראה לצוות בטלגרם.

**טלגרם הוא צינור-התראה חד-כיווני.** הצוות מקבל התראה בטלגרם ("משתמש X פתח פנייה: [תוכן] → לינק לממשק"), אבל **לא עונה מהטלגרם**. כל ניהול-השיחה הדו-כיווני קורה בממשק שלכם. טלגרם מודיע, הממשק מנהל. המשמעות: אין צורך בבוט-טלגרם-נכנס, אין reply-threading, אין מיפוי-הודעה-לthread. רק שליחת-התראה-החוצה.

**ההתכתבות מול המשתמש קורית בממשק.** אחרי שנפתח thread (בין אם מכפתור-התמיכה ובין אם מאסקלציה), הצוות מתכתב עם המשתמש דרך הממשק. זה thread דו-כיווני בתוך הפלטפורמה.

**thread פתוח-תמיד, בלי מצב "סגור/נפתר".** אין כפתור-סגירה ואין ניהול-סטטוס. thread נפתח ונשאר גלוי; כשהבעיה נפתרת, הצוות פשוט מפסיק להתכתב. פשטות — בלי מכונת-מצבים מיותרת.

**אסקלציות-אוטומטיות — התראה-לצוות + הודעה-אחידה-למשתמש.** ארבעה תנאי-תקלה (§3) מפעילים אוטומטית: התראה-לטלגרם לצוות **וגם** הודעה-אחידה-למשתמש. ההודעה זהה בכל מקרה: *"זיהינו תקלה בקמפיין, אנחנו בתהליך תיקון, תקבל עדכון מצב בקרוב"*. המשתמש אף-פעם לא רואה את המנגנון-הפנימי (לא את הטלגרם, לא יודע שיש אסקלציה-אוטומטית, אין ג'רגון-טכני) — הוא מקבל משפט-אנושי-אחד מרגיע.

**רוב-התקלות לא מגיעות בוובהוק — וזו נקודת-השורש של כל התכנון.** ראה §1 להסבר המלא. בקצרה: רק דחיית-מודעה מגיעה מוובהוק (מטא יוזמת ושולחת). כל שאר התקלות מתגלות כשהקוד קורא למטא ומקבל שגיאה. אין "עוד קוד לוובהוק" להוסיף — יש לחבר את הסיווג-שכבר-קיים לצינור-התראה-חדש.

---

## 1. נקודת-השורש: איך תקלות מגיעות למערכת

זו ההבנה שכל התכנון תלוי בה, ולכן היא ראשונה. יש הנחה טבעית שהמערכת "מקבלת" תקלות מבחוץ — וזה נכון רק למקרה אחד מאוד-ספציפי. מי שמבצע חייב להפנים את ההבחנה הזו, אחרת ינסה לבנות מנגנון-וובהוק שלא-צריך-להתקיים.

יש שני מקורות שונים-לחלוטין לתקלות, וההבדל ביניהם קובע איך תופסים כל אחת.

**מקור ראשון — אירועים שמטא יוזמת ושולחת אליכם (וובהוק).** דחיית-מודעה היא הדוגמה היחידה-כמעט. מטא בודקת מודעה אחרי שעלתה, מחליטה לדחות, ושולחת `ad_review` webhook לשרת שלכם. האירוע מגיע "מעצמו" כי מטא היא הצד-הפעיל. זה מה שבעיה-3 הקיימת כבר תופסת (`rejection_intake_service` → job `handle_meta_rejection`). המנגנון הזה כבר קיים ועובד — אנחנו רק מוסיפים עליו שכבת-אסקלציה (§3).

**מקור שני — כשלים שאתם מגלים תוך-כדי-עבודה (שגיאת-API).** זה הרוב-המכריע של התקלות שמנית, ואף-אחת מהן לא מגיעה בוובהוק. `חשבון-מושהה` מתגלה כשאתם מנסים לדחוף קמפיין או לשלוף Insights ומטא מחזירה קוד-שגיאה. `בעיית-הרשאה/אימות` מתגלה כשטוקן פג ואתם מקבלים error-190 באמצע פעולה. אף-אחד לא "שולח" לכם את התקלות האלה — אתם **נתקלים** בהן כשאתם קוראים למטא וזה נכשל.

ההבחנה בתמונה אחת: וובהוק זה מישהו שדופק לכם בדלת. שגיאת-API זה משהו שאתם מגלים כשאתם פותחים מגירה ומוצאים שהיא תקועה. מטא דופקת בדלת רק על דחיות-מודעה; כל השאר — אתם מגלים כשאתם פותחים את המגירה תוך-כדי-עבודה.

**המשמעות לתכנון — וזו הנקודה הקריטית:** אין צורך להוסיף "עוד endpoints לוובהוק" עבור חשבון-מושהה או בעיית-הרשאה. מטא לא-שולחת אותם, אז אין מה לקלוט. מה שצריך הוא **נקודה-מרכזית שתופסת שגיאות-מטא בכל מקום שהקוד כבר קורא למטא**, מסווגת אותן, ועל הסוגים-הרלוונטיים — יורה אסקלציה. והתשתית לזה כבר קיימת אצלכם: `classify-by-meaning` (PR9) כבר מבחין בין סוגי-שגיאות; `MetaTransientError`/`MetaUnexpectedError` כבר מבדילים חולף-מקבוע; שכבת-ה-error-classification ב-`db/errors` כבר עובדת. **הדלתא היא לחבר את המסווג-הקיים לערוץ-התראה-חדש (טלגרם + הודעה-למשתמש), לא לכתוב תפיסת-שגיאות מאפס.**

הטעות שקל ליפול בה כאן: לחשוב "צריך webhook לכל סוג-תקלה". הסימפטום — CC מתחיל לבנות `POST /webhooks/account-suspended` שאף-אחד-לא-יקרא-לו-לעולם (מטא לא שולחת כזה). הפתרון — לזהות שהתקלות-הלא-וובהוק מתגלות ב-**נקודות-הקריאה-הקיימות למטא**, ולתפוס אותן שם דרך המסווג.

---

## 2. תשתית-ההתכתבות המשותפת

זו התשתית ששני החלקים (תמיכה-יזומה ואסקלציות) חולקים. בונים אותה פעם-אחת.

### 2.1 סכמה — טבלת `support_threads` (חדשה)

thread-תמיכה הוא שיחה-מתמשכת בין משתמש לצוות. שדות:

`user_id` (FK, RLS) · `campaign_id` (FK nullable — פנייה יכולה להיות כללית או קשורה-לקמפיין) · `origin` (TEXT + CHECK: `user_initiated` / `auto_escalation` — מאיפה נפתח ה-thread) · `escalation_type` (TEXT nullable — סוג-האסקלציה אם `origin=auto_escalation`; ראה §3) · `created_at` · `last_message_at` (למיון). **אין עמודת-status** — thread פתוח-תמיד (החלטה §0).

### 2.2 סכמה — טבלת `support_messages` (חדשה)

הודעות ה-thread, append-only. שדות:

`thread_id` (FK) · `user_id` (composite FK — חוק §7.2) · `sender` (TEXT + CHECK: `user` / `staff` / `system`) · `body` (text) · `created_at`.

ההבחנה ב-`sender` חשובה: `user` = המשתמש כתב; `staff` = הצוות ענה דרך הממשק; `system` = הודעה-אוטומטית (למשל ההודעה-האחידה של אסקלציה, §3, נכתבת כ-`system`). זה מאפשר להציג את ההודעה-האוטומטית ב-thread כחלק-מהשיחה בלי לזייף שהיא באה מהצוות.

RLS: המשתמש רואה רק את ה-threads שלו (SELECT). כתיבת-`user` דרך user-client + column-grant (כמו `lead_status` ב-7.2.5 — חריג-מבוקר ל-server-authoritative). כתיבת-`staff`/`system` דרך service (admin).

### 2.3 צינור-הטלגרם (חד-כיווני, החוצה)

שליחת-התראה לצוות בטלגרם. זה **לא** דורש בוט-נכנס — רק שליחה-החוצה דרך ה-Telegram MCP המחובר (או Bot API ישיר, לפי מה שפשוט יותר לחווט מהסביבה).

ההתראה נשלחת דרך תשתית-ה-jobs הקיימת (enqueue → claim → retry), לא בלוקינג. הפורמט: מזהה-המשתמש (או שם-העסק), סוג-הפנייה (`user_initiated`/`auto_escalation` + סוג-האסקלציה), תמצית-התוכן, ו**לינק-עמוק לממשק-הצוות** ל-thread הספציפי. הצוות לוחץ על הלינק → נוחת ב-thread → מתכתב.

**סוד-הטוקן של הטלגרם** ב-Vault/env (כמו כל סוד — spec §7). ערוץ-היעד (chat-id של הצוות) ב-`app_settings` או env.

**עמידות:** כשל בשליחת-הטלגרם **לא** מבטל את פתיחת-ה-thread ולא את ההודעה-למשתמש. אם הטלגרם נופל, ה-thread עדיין נפתח וגלוי בממשק-הצוות (אתם עדיין תראו אותו כשתיכנסו) — הטלגרם הוא נוחות-התראה, לא the-source-of-truth. כשל → Sentry + retry מבוקר, בדיוק כמו כשל-הפקת-חשבונית ב-2.6.2 ("כשל לא מבטל את הפעולה העיקרית").

### 2.4 ממשק-הצוות (admin)

עמוד-אדמין שמציג את כל ה-threads הפתוחים (מיון לפי `last_message_at`), עם אפשרות להיכנס לכל אחד, לקרוא את השיחה, ולכתוב תשובה (`sender=staff`). זה המקום שבו הניהול-הדו-כיווני קורה. הלינק-מהטלגרם מוביל ישירות ל-thread הספציפי כאן.

### 2.5 ממשק-המשתמש

כפתור-תמיכה גלובלי (§0) שפותח thread חדש או ממשיך thread קיים. מסך-thread שבו המשתמש רואה את השיחה (הודעות-`user`, תשובות-`staff`, והודעות-`system` של אסקלציות) וכותב הודעות-חדשות. ההודעות-האוטומטיות של אסקלציה מופיעות כאן כ-`system` — כך המשתמש רואה "זיהינו תקלה..." בתוך אותו thread שבו תמשיך השיחה.

---

## 3. אסקלציות-אוטומטיות — התנאים והמנגנון

זה החלק התגובתי. ארבעה תנאי-תקלה יורים אסקלציה אוטומטית. כל אסקלציה עושה **שני דברים**: (א) התראה-לטלגרם לצוות (§2.3); (ב) פתיחת/עדכון thread עם ההודעה-האחידה-למשתמש כ-`system` (§3.5).

### 3.1 תנאי 1 — כל שלוש המודעות נדחו

בעיה-3 הקיימת מטפלת בדחיית-מודעה **בודדת**: מנסחת תיקון-אוטומטי ומציגה למשתמש לאישור. הדלתא כאן: **כשכל השלוש נדחו** (לא אחת, לא שתיים), זה חורג מ"תקן-והצג-לאישור" — כנראה יש בעיה-מערכתית שהתיקון-האוטומטי-הבודד לא-יפתור. אז במקום לנסות שלושה-תיקונים-אוטומטיים, המערכת מסלימה לצוות.

**איפה זה נתפס:** ב-`rejection_intake_service` הקיים, שכבר מקבל את אירועי-הדחייה מהוובהוק. הוא כבר יודע על כל דחייה. הדלתא — לספור: אם שלוש-המודעות-של-הקמפיין ב-`rejected` (בו-זמנית, באותו חלון) → יורה אסקלציה במקום שלושה-מסלולי-תיקון-נפרדים.

הבחנה חשובה: זה **לא** מבטל את בעיה-3 לדחייה-בודדת-או-כפולה. מודעה-אחת-נדחתה או שתיים-נדחו → בעיה-3 הקיימת ממשיכה כרגיל (תיקון-אוטומטי + אישור). רק שלוש → אסקלציה.

### 3.2 תנאי 2 — דחייה פעם-שנייה ברצף (אותה מודעה)

מודעה נדחתה, המערכת ניסחה תיקון (בעיה-3), התיקון עלה, ומטא **דחתה אותו שוב**. כשל-שני-ברצף על אותה מודעה. זה סימן שהתיקון-האוטומטי לא-מספיק — צריך עין-אנושית.

**הכלל הקריטי שהדגיש אמיר:** אחרי כשלון-שני-ברצף, **לא מרנדרים תיקון-אוטומטי פעם-שלישית.** המערכת מפסיקה לנסות-לבד ומעבירה לצוות. זו הגנה מפני לולאת-תיקונים-אינסופית שסתם-שורפת-קריאות-LLM ומעצבנת-את-מטא.

**איפה זה נתפס:** צריך למנות ניסיונות-תיקון פר-מודעה (או פר-`optimization_action` מסוג `rejection_fix`). ה-`rejection_push_service` הקיים כבר מנהל את מחזור-התיקון. הדלתא — counter על מספר-הדחיות-הרצופות: הגיע ל-2 → אסקלציה + עצירת-רינדור-אוטומטי-נוסף.

### 3.3 תנאי 3 — חשבון-מושהה

חשבון-המודעות של המשתמש הושעה במטא. זה מתגלה (כמו שהוסבר ב-§1) **כשהקוד קורא למטא ומקבל קוד-שגיאה** של חשבון-מושהה — לא מוובהוק. זה יכול לקרות בכל נקודת-קריאה: דחיפת-קמפיין, שליפת-Insights, פעולת-אופטימיזציה, רענון-טוקן.

**איפה זה נתפס:** במסווג-השגיאות-הקיים. כשקריאה-למטא מחזירה שגיאת-חשבון-מושהה, המסווג (שכבר מבחין בין סוגי-שגיאות) מזהה את הקוד-הספציפי ויורה אסקלציה. **הדלתא היא בזיהוי-הקוד-הספציפי והפניה-לצינור-האסקלציה** — לא ב-endpoint-חדש.

### 3.4 תנאי 4 — בעיית-הרשאה/אימות

טוקן-מטא פג או נשלל, וה-refresh (Phase 6) לא-הצליח לפתור. גם זה מתגלה תוך-כדי-קריאה (error-190 וכדומה), לא מוובהוק.

**איפה זה נתפס:** ב-handler של רענון-הטוקן (Phase 6) וב-מסווג-השגיאות. Phase 6 כבר מזהה `mark_token_expired`. הדלתא — כשה-refresh נכשל סופית (לא חולף), לירות אסקלציה. שים לב: זה כבר-כמעט-קיים — ב-Phase 6 סומן "notification ללקוח" כלא-ממומש. האסקלציה הזו היא בדיוק המימוש-החסר.

> **הבחנה מ-§9 של מסמך-החיוב:** אל תבלבל בין "בעיית-הרשאה" כאן (טוקן-מטא פג, תקלה-תפעולית) לבין "שער-אשראי" במסמך-החיוב (הזנת-אמצעי-תשלום). שונים לגמרי.

### 3.5 ההודעה-האחידה למשתמש

בכל ארבעת התנאים, המשתמש מקבל **בדיוק את אותה הודעה**, ללא תלות בסוג-התקלה:

> *"זיהינו תקלה בקמפיין, אנחנו בתהליך תיקון, תקבל עדכון מצב בקרוב"*

ההודעה נכתבת כ-`system` ב-thread (נפתח thread-אסקלציה אם אין, או מתווספת לקיים). המשתמש **אף-פעם לא רואה**: את הטלגרם, את סוג-התקלה-הטכני, את העובדה-שזו-אסקלציה-אוטומטית, שום ג'רגון. משפט-אנושי-אחד מרגיע. זהו.

**למה הודעה-אחידה ולא פר-סוג:** כי זה מסיר לוגיקת-החלטה מ-CC. אין צורך להחליט "איזו הודעה לאיזו תקלה" — תמיד אותה הודעה. פחות קוד, פחות מקום-לטעות, והבטחה-למשתמש עקבית. (השיקול המקורי היה הודעה-רזה-רק-כשהמשתמש-רואה-את-הסימפטום; ההכרעה הפשטה את זה להודעה-תמיד, וזה עדיף.)

### 3.6 ערוץ-ההתראה למשתמש — פתוח, להכרעת גיא

ההודעה-האחידה מוצגת ב-thread-בממשק תמיד. השאלה הפתוחה: **האם גם לשלוח אותה במייל (ואולי וואטסאפ)?**

הנטייה הנוכחית של אמיר: **מייל כן**, וואטסאפ אולי. אבל זה **לא סגור** — להתייעצות עם גיא לפני מימוש. מה שבטוח: ההודעה-בממשק (ב-thread) תמיד קורית. ערוץ-המייל/וואטסאפ הוא תוספת שתוכרע. אם מייל — דרך Resend הקיים (תשתית 4.6). אם וואטסאפ — כפוף ל-gate-ה-WABA (רדום עד production-ready, כמו שאר התראות-הוואטסאפ).

> **סימון ל-CC:** אל תממש את ערוץ-המייל/וואטסאפ עד הכרעת-גיא. בנה את ההודעה-בממשק (ב-thread) כברירת-מחדל; השאר את ערוץ-המייל כ-hook שקל-להדליק כשההחלטה תיפול.

---

## 4. אנטי-ספאם ואידמפוטנטיות של אסקלציות

תקלה יכולה לחזור-על-עצמה: קריאה-למטא-שנכשלת יכולה להיקרא שוב-ושוב תוך-דקות. בלי הגנה, זה יציף אתכם בטלגרם ואת המשתמש בהודעות-כפולות.

**הכלל:** אסקלציה מאותו-סוג על אותו-קמפיין (או אותו-חשבון, לתקלות-חשבון) נשלחת **פעם-אחת בחלון-זמן**, לא על כל-הישנות. אותו דפוס כמו התראות-המכסה (Phase 4.2, flags `notified_80_at`/`notified_100_at` — פעם-אחת-בחלון). המנגנון: על ה-thread-האסקלציה (או flag פר-קמפיין+סוג) נרשם מתי-נשלחה-אסקלציה-אחרונה; הישנות תוך-החלון → מעדכנת-בשקט (או מתעלמת), לא יורה טלגרם+הודעה מחדש.

**אידמפוטנטיות:** דרך `webhook_events` (לתקלות-וובהוק כמו דחייה) ו-flag-פר-קמפיין+סוג (לתקלות-API). אותו עיקרון שכבר-קיים בכל המערכת — לא להמציא מחדש.

זה חשוב במיוחד לתנאי-3 ו-4 (חשבון-מושהה, בעיית-הרשאה): תקלה כזו תחזור על **כל** קריאה-למטא עד שהיא נפתרת. בלי אנטי-ספאם, משתמש-אחד-עם-טוקן-פג היה מייצר עשרות התראות-טלגרם ביום. פעם-אחת-בחלון פותר את זה.

---

## 5. סדר-ביצוע מומלץ ל-CC

1. **הסרת bug-reports** — הסר `POST /me/bug-reports`, עמוד-האדמין-המצרפי, והמיגרציה. (אם יש דאטה קיימת שצריך לשמר — לשקול מיגרציית-העברה, אבל ככל-הנראה מיותר.)
2. **סכמה** — צור `support_threads` + `support_messages`. GRANT + RLS.
3. **תשתית-ההתכתבות** — ממשק-המשתמש (כפתור-תמיכה + מסך-thread) + ממשק-הצוות (רשימת-threads + מסך-תשובה). זה החלק היזום, כולו-חדש, והכי-פשוט — טוב להתחיל בו.
4. **צינור-הטלגרם** — שליחת-התראה-החוצה דרך jobs. חבר לכפתור-התמיכה קודם (נתיב-פשוט לבדיקה).
5. **אנטי-ספאם** — flags פר-קמפיין+סוג לפני שמחברים אסקלציות (אחרת ההצפה בבדיקות).
6. **אסקלציות** — חבר את ארבעת-התנאים למסווג-הקיים. תנאי-1 ו-2 ב-`rejection_intake`/`rejection_push` הקיימים; תנאי-3 ו-4 במסווג-השגיאות ו-Phase-6-handler.
7. **ההודעה-האחידה** — כ-`system` ב-thread. ערוץ-המייל/וואטסאפ נשאר hook לא-מחובר עד הכרעת-גיא.

**עיקרון-על:** אל תבנה מנגנון-וובהוק-חדש לתקלות-הלא-וובהוק (§1). התקלות מתגלות בנקודות-הקריאה-הקיימות למטא — תפוס אותן שם דרך המסווג-הקיים.

---

## 6. שאלות פתוחות (לא לממש בלי הכרעה)

**ערוץ-ההתראה למשתמש (§3.6)** — מייל? גם וואטסאפ? נטייה: מייל כן. להכרעת-גיא. בממשק תמיד; ערוץ-חיצוני כ-hook.

**חלון האנטי-ספאם (§4)** — כמה זמן בין אסקלציות-חוזרות-מאותו-סוג? (שעה? יום? עד-שנפתר?). ברירת-מחדל סבירה: עד-שהתקלה-לא-חוזרת-בחלון-של-X-שעות. לכייל.

**מי סוגר את הלולאה עם המשתמש אחרי אסקלציה?** ההודעה אומרת "תקבל עדכון מצב בקרוב" — כשהצוות מתקן, האם יש עדכון-אוטומטי ("התקלה נפתרה") או שהצוות כותב ידנית ב-thread? כרגע ההנחה: הצוות כותב ידנית (thread פתוח-תמיד, אין אוטומציה-של-סגירה). לאשר.

---

## נספח: מה משתנה מול spec.md ו-ROADMAP

| נושא | הישן | החדש (מסמך זה) |
|---|---|---|
| דיווחי-תקלות | `bug-reports` — איסוף-פאסיבי-לניתוח (ROADMAP "דיווחי תקלות") | מוסר לגמרי; מוחלף בתמיכה-דו-כיוונית |
| ערוץ-דיווח | חד-פעמי, בלי-מענה | thread דו-כיווני + טלגרם לצוות |
| התראת-דחייה | בעיה-3: תיקון-אוטומטי + אישור (פר-מודעה) | נשאר לדחייה-בודדת/כפולה; 3-נדחו או דחייה-שנייה → אסקלציה |
| תקלות-לא-וובהוק (חשבון/הרשאה) | נתפסות במסווג, בלי-התראה-ייעודית | אסקלציה-לטלגרם + הודעה-למשתמש |
| notification-ללקוח (Phase 6) | סומן לא-ממומש | ממומש כאסקלציה תנאי-4 |

**מה ש-*לא* משתנה:** כל שכבת-הסוכן-והאופטימיזציה (בעיה-3 נשארת לדחייה-בודדת), מסווג-השגיאות הקיים (מורחב, לא מוחלף), תשתית-ה-jobs, Resend, ה-gate-ה-WABA. השינוי הוא **שכבת-תמיכה-ואסקלציה חדשה** מעל הסיווג-הקיים — לא נגיעה בליבה.
