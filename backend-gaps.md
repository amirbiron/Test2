# פערים והשלמות נדרשות ב-Backend

> **מסמך דינמי.** כל פער שמתגלה בחיווט הפרונטד — ה-frontend צריך endpoint/פיצ'ר שה-backend **לא** מספק —
> נרשם כאן. משלים את `frontend-integration.md` (מפת מסכים per-screen); כאן ה-**action-list המרוכז ל-backend**.
>
> **כלל:** כשמתגלה פער כזה בחיווט — מוסיפים שורה לטבלה **באותו PR** (ראה CLAUDE.md, סעיף "מסמכי הפרויקט").
> פער שקיבל endpoint עובר לסעיף "הושלמו" עם מספר ה-commit.

## פערים פתוחים

| פער | מסך | תיאור | התגלה | סטטוס |
|-----|-----|------|------|------|
| חשבונית פייסבוק | My Plan | endpoint לשליחת חשבונית פרסום Meta למייל (`requestFbInvoice`). כרגע frontend mock (toast). דורש משיכת נתוני הוצאות מ-Meta + מנגנון שליחת מייל. | חיווט My Plan | 🔴 פתוח |
| הורדת PDF חשבונית מ-frontend | My Plan | `GET /me/invoices/{id}/download` מחזיר 302 ל-URL חתום cross-origin — לא נגיש דרך `fetch` (תגובת opaque) ולא דרך `window.open` (דורש Bearer). ה-frontend מסתמך על `pdf_url_he` בלבד; כשהוא NULL (webhook מ-Green Invoice בדרך) אין הורדה. תיקון: endpoint שמחזיר JSON `{url}` (או signed URL same-origin). | חיווט My Plan | 🔴 פתוח |
| `GET /campaigns` (list) | Dashboard | רשימת הקמפיינים של המשתמש ל-KPIs ולכרטיסים. קיים רק `GET /campaigns/{id}`. לבדוק אם MVP חד-קמפייני מסתדר עם `campaign_id` שמור, או שצריך endpoint רשימה. **צ'אט-האופטימיזציה (הוראה 4) כבר לא תלוי בזה** — שואב את הקמפיינים הפעילים מ-`GET /me/agent/conversations`. הפער נשאר רק ל-Dashboard KPIs. | מיפוי החיווט | 🔴 פתוח (Dashboard בלבד) |
| magic-link signup | Login | **מומש** (Session 1.3): `POST /auth/magic-link` (שליחה, כניסה-לרשומים-בלבד) + `GET /auth/magic-link/verify` (verify_otp server-side → refresh cookie → `/login/success`). `sendMagicLink` מחווט. דורש התאמת email-template ב-Supabase (SETUP_CHECKLIST). | מיפוי החיווט | ✅ מומש |
| Dashboard KPIs aggregate | Dashboard | KPIs: קמפיינים פעילים (count), עלות ממוצעת לליד, סה״כ הושקע (spend), וכרטיסי לקוחות. אין endpoint; **spend לא חשוף כלל** (קיים רק ב-`meta_service.get_campaign_insights` internal, ל-LLM). אין `GET /campaigns` list. צריך `GET /me/dashboard-kpis` (אגרגציה) או `GET /me/campaigns` + insights. כרגע מחווט רק "לידים סה״כ" מ-`GET /me/quota`. | חיווט Dashboard | 🔴 פתוח |
| OAuth return route (`/connected`) | Meta connection | **נסגר:** (1) `SPAStaticFiles` (main.py) מגיש index.html לכל path לא-קיים (SPA fallback — ה-mount catch-all אחרון, 404 של API אמיתי לא מושפע); (2) `_handleMetaConnectRedirect` (frontend) מזהה `/connected`+`/connect-error`, בודק `/me/fb-connection` (מחובר?) + `/me/onboarding-draft` (מסלול — נשמר ב-`connectMeta` לפני ה-redirect, טבלה 0088) וממשיך ל-step בחירת החשבון; (3) ה-state האבוד נפתר דרך draft בשרת (לא sessionStorage). | חיווט Meta | ✅ נסגר |
| בחירת Page (`fb_page_id`) | Wizard שלב א | **נסגר:** מסך בחירת-עמוד (`leads-pages-stage`/`wa-pages-stage`) — stage שלישי בתוך step-2-leads/step-3 אחרי בחירת החשבון, מרנדר את `meta-assets.pages` (`loadMetaPages`), בחירה **מפורשת** → `APP.page`. **בלי fallback** (אין auto-select; גם עמוד יחיד דורש בחירה). אין עמודים → הודעה + כפתור חסום (אסור להמשיך). (ה-`fb_page_id` יישלח כש-`POST /campaigns` יחווט — פער נפרד, כמו `APP.account`.) | Wizard שלב א | ✅ נסגר |
| VIP-flag + מחיר-מנוי | My Plan | **נסגר (PR-A+PR-B):** VIP פר-קמפיין. הדגל ב-`CampaignBillingSummary.vip_owner_alerts`/`vip_cancel_at_period_end` (`/me/campaigns`), לא ב-`SubscriptionResponse`. בחירת-VIP בהרשמה → קמפיין-הראשון (`register_campaign_billing`/0133, PR-A). פאנל VIP ב-My Plan (`renderVipPanelSection`/`renderVipPanel`) מחווט ל-`GET/PATCH /campaigns/{id}/vip` פר-קמפיין (PR-B): קמפיין-יחיד→פאנל, כמה→בורר (כמו `startAgentChat`); preview→אישור→חיוב. תא ה-VIP + מחיר-מנוי מ-`/me/campaigns`. | חיווט My Plan | ✅ נסגר (PR-A+PR-B) |
| התראת-השלמה ל-A3 (תמונות חדשות) | Action page / צ'אט | approve→`generating`→worker מייצר ודוחף. הפאנל עושה polling (`_pollActionUntilSettled`, ~2 דק') → תקין. אך **אין push/notification** כשהמודעות עולות: בצ'אט נשמרת הודעת "מייצר..." בלבד; אחרי החלון/רענון רואים תוצאה. שיפור עתידי: notification/SSE על `pushed`. **בנוסף:** כשל-יצירה permanent (כולל no-key OpenAI) → `push_failed` (terminal, **בר-שחזור** — ה-action נכשל אך הקמפיין חי; לא degradation/skip כמו WhatsApp, כי כאן terminal לא שורף ליד). | A3 (תמונות חדשות) | 🟡 ידוע (UX) |
| Diagnostics improvement-history | Diagnostics | ה-view (`diag-list`) מציג היסטוריית שיפורים/אופטימיזציות, אך אין `GET` שמחזיר אותה. הנתונים קיימים ב-`optimization_monitor_service` (internal, ל-cron/LLM) — צריך לחשוף endpoint (`GET /me/optimizations` או דומה). תלוי **קמפיין live** (אין שיפורים בלי קמפיין רץ). כרגע ה-view הוא mock. | חיווט Diagnostics/Context | 🔴 פתוח |
| Context (זיכרון הסוכן) | Context | `view-context` הוא **mock טהור** — 6 items hardcoded (type/protocol/issues/opportunities/glossary/rules); `showView('context')` רק מעדכן timestamp. **אין מקור כלל** (endpoint/table/service) שמספק את הנתונים. אם ייבנה — תלוי quiz (סוג עסק) + agent + **קמפיין live**. | חיווט Diagnostics/Context | 🔴 פתוח |

| חידוש קמפיין-כבוי (resume, §6) | קמפיין-כבוי/נעילה | **נסגר (PR8b-B):** `resume_campaign` (charge-first→reactivate Meta, §6.1) + `POST /campaigns/{id}/resume` (200 resumed/reactivate_pending · 400 no_credit · 402 declined · 409 · 503) + מסך-נעילה עם כפתור-חידוש (`_resumeCampaign`, resume-mode polling). | חקירת PR8 | ✅ נסגר |
| שאלת-יום-28 (חידוש-מחזור, §4.3) | קמפיין | **נסגר (PR8b-C):** `run_cycle_renewals` (cron auto-renew ב-cycle_end → `charge_renewal`; wiring ב-runner) + `POST /campaigns/{id}/shutdown` ('לא') + `/cancel-shutdown` ('התחרט') + banner יום-28 (`_maybeShowRenewalBanner`). "כן" = auto-renew (ברירת-מחדל, terms 'מתחדש אוטומטית'). | חקירת PR8 | ✅ נסגר |
| cron-חשבוניות-קמפיין ייעודי (§7) | — | **נסגר (PR8b-D):** `retry_failed_campaign_invoices` (cron) + `retry_pending_campaign_invoice` (disambiguation לפי `description` — F-C) + RPC `claim_pending_campaign_invoices` + עמודת `description` (migration 0122). | PR7 review | ✅ נסגר |
| מצב-חיוב-פר-קמפיין ל-frontend (§6) | קמפיין-כבוי/יום-9 | **נסגר (PR8a):** `GET /me/campaigns` → `CampaignBillingSummary` (billing_status/cycle_start_at/cycle_end_at/continuation_approved_at פר-קמפיין) — משמש מסכי יום-9/נעילה/יום-28. **read-only-לקמפ-כבוי בדשבורד (§6.1 מצב-1) נשאר פתוח** (דורש multi-campaign dashboard view). | חקירת PR8 | ✅ נסגר (מצב-1 read-only פתוח) |
| מכסת-לידים פר-קמפיין (§4.2/§119) | Dashboard / My Plan / התראות | **נסגר (Phase 1a):** `get_campaign_quota_status(campaign_id)` + `get_user_leads_quota_aggregate(user_id)` (subscription_service). `check_quota_thresholds(*, campaign_id, user_id)` — anchor `{campaign_id}:{cycle_start_at}` + payload `service_name`. Endpoints: `/me/quota` נשאר aggregate (transparent ללקוח-single) + חדש `GET /campaigns/{id}/quota`. Email: `_QUOTA_80/100_BODY` כותרת פר-קמפיין; טענת-אכיפה הוסרה עד Phase 2. `lead_intake_service:201` — passes `campaign_id`. Phase 1b/1c/1d (agent-chat/creative/agent-alerts) + Phase 2 (bot enforcement) + Phase 3 (VIP) בהמשך. | מעבר §4.2 | ✅ נסגר |
| מכסת agent-chat פר-קמפיין (§4.2/§119) | Agent chat | **נסגר (Phase 1b):** `get_campaign_agent_chat_quota_status(user_id, campaign_id)` + `get_user_agent_chat_quota_aggregate`. RPC `insert_user_and_assistant_messages` (0123) COUNT+lock פר-conversation. `agent_service:671` קורא עם campaign_id שכבר יש לו (conversation ב-0048 פר-קמפיין). endpoints: `/me/agent/quota-status` aggregate + חדש `/campaigns/{id}/agent/quota`. | Phase 1b | ✅ נסגר |
| מכסת agent-alerts WhatsApp פר-קמפיין (§4.2/§121) | Agent WhatsApp alerts | **נסגר (Phase 1d):** `sent_notifications.campaign_id` (0124, nullable) + index. RPC `create_agent_notification` COUNT מסונן campaign_id + חלון-קמפיין. `notification_service.send_agent_notification` שולף cycle של הקמפיין. 5 callers מעבירים campaign_id. `get_campaign_agent_alerts_quota_status` + aggregate. endpoints: aggregate + חדש `/campaigns/{id}/agent/alerts-quota`. Creative quota (Phase 1c) נדחה — משאב-משותף. | Phase 1d | ✅ נסגר |
| אכיפת מכסה בבוט WhatsApp (§5) | Bot | **נסגר (Phase 2):** `bot_service._skip_if_quota_exhausted(campaign_id, context)` — silent skip על `get_campaign_quota_status(...).is_over_limit && !is_unlimited`. 3 gates: `handle_inbound` (תשובות של הליד), `initiate_conversation` (opening לליד חדש), `send_followup` (cron template). `conversation_service.get_campaign_id_for_conversation` helper משותף. Basic ללא-בוט by construction (`bot_config` דורש Premium); WhatsApp-tier `is_unlimited=True` → no-op. `_QUOTA_100_BODY` החזרת "הבוט מפסיק להגיב". §119 anti-abuse by construction — קמפיין A ב-100% לא-משתיק קמפיין B. | Phase 2 | ✅ נסגר |
| VIP add-on פר-קמפיין (§8) | קמפיין / My Plan | **נסגר-backend (Phase 3, PR-V1→V4):** מנגנון-VIP עבר מפר-user לפר-קמפיין. עמודות `campaigns.vip_owner_alerts/vip_alert_phone/vip_cancel_at_period_end` (0114+0125). `set_campaign_vip_addon` + `_charge_campaign_vip_addon_now` (proration payment-first, **anchor=cycle_end_at**, period_start=cycle_end−30d) + `preview_campaign_vip_addon` + clear-on-renewal ב-charge_campaign (`effective_vip`+`p_clear_vip`). delivery: `insert_lead_and_event`→`campaigns.vip_owner_alerts` (0126) + `get_campaign_vip_alert_target`. endpoints `PATCH/GET /campaigns/{id}/vip[/preview]`; `CampaignBillingSummary` +vip fields (phone לא — PII). adversarial-review (2 reviewers). ה-endpoint הישן פר-user נוטרל→410. **פער-frontend פתוח: ה-VIP panel פר-קמפיין נדחה** — יבנה עם מסך-הקמפיין המפורט (אין כזה מסך היום; ה-toggle שמיש דרך API). | Phase 3 | ✅ נסגר-backend (panel frontend פתוח) |
| ניקוי-שרידים per-user (PR9) | — | **PR9-safe נסגר:** (1) תיקון-regression — `create_agent_notification` repoint `vip_alert_phone`→`agent_alert_phone` (§93 מספר-סוכן ייעודי; migration 0127; היה רדום — WABA soft-disabled → אפס behavior-change בפועל, רק תיקון-יעד לעתיד). (2) הוסר dead VIP subtree per-user (11 פונקציות: `set_vip_addon`/`_charge_vip_addon_now`/`preview_vip_addon`/`get_vip_alert_target`/`_set_vip_flag_only`/`_set_vip_cancel_at_period_end`/`_resolve_existing_vip_attempt`/`_read_subscription_for_vip`/`_mark_attempt_unknown`/`_VIP_SUB_COLUMNS`/`_parse_period_start`) — הגיעו רק מ-endpoints-410. (3) DROP `finalize_vip_addon_charge` (migration 0128; ה-per-campaign משתמש ב-`finalize_campaign_vip_addon_charge`/0125). ה-**reuse-boundary נשמר**: `_raise_vip_failure_reason` (חי — `set_agent_whatsapp`), `_vip_confirm_ok`, `get_billing_token_for_user`, 6 error-classes, `VipToggleRequest`/`VipAddonPreview`, endpoints-410. **follow-up נדחה (known-debt, החלטת "הכי-בטוח"):** DROP 4 עמודות `subscriptions` (`lead_quota`/`vip_owner_alerts`/`vip_alert_phone`/`vip_cancel_at_period_end`) — כולן **BLOCKED** ב-hot-path (signup-trigger 0001/`_PUBLIC_COLUMNS`/`update_tier`/admin `resolve_charge_unknown`/agent-RPC 0124); + הסרת legacy per-user charge path (`_charge_subscription`+3 crons+`finalize_charge_attempt`) — DEAD-by-design (guard `_has_campaign_billing`) אך wired ורץ יומית. MEDIUM-risk + ערך-נמוך (clean-slate; עמודות עם default לא מזיקות). נדחה להחלטה מודעת נפרדת. | PR9 | 🟡 חלקי (PR9-safe ✅; legacy-path→PR10-A; DROP-columns נדחה) |
| ניקוי legacy per-user charge path (PR10-A, חלק A) | — | **נסגר (PR10-A):** הוסר נתיב-החיוב-האוטומטי-הפר-user שהיה DEAD-by-design (מאחורי `_has_campaign_billing`) אך רץ יומית: `_charge_subscription`+3 wrappers, 3 crons (`process_trial/monthly/past_due`), `cleanup_stuck_charging`+reaper-wiring, `resolve_charge_unknown` (service+admin endpoint+models), guard, helpers יתומים (`_revert_charging`/`_delete_attempt`/`_resolve_cps`/`_extract_rpc_success`). DROP 3 RPCs (`finalize_charge_attempt`/`mark_stuck_attempt`/`resolve_charge_unknown`, migration 0129). **נשמרו:** `cleanup_*_sessions` (token-setup משותף פר-קמפיין), `retry_failed_invoices`+`billing_invoices` (safety-net היסטורי), `get_billing_token_for_user` (reuse פר-קמפיין). **נותר follow-up (חלק B):** DROP 4 עמודות `subscriptions` + `issue_invoice_for_charge` (התייתם→dead) + `_PUBLIC_COLUMNS`/`update_tier`/signup-trigger. ה-`charge_unknown` היום פר-קמפיין (`reap_stuck_campaign_charges`); אין endpoint יישוב-ידני פר-קמפיין (known-gap — `runbooks/stuck-charges.md`). | PR10-A | 🟡 חלקי (חלק A ✅; DROP-columns חלק B נדחה) |
| write-never: `campaigns.tier`+`lead_quota` (§4.2) — Phase C | קמפיין / חיוב / מכסה | 🔴 **באג-שורש שהתגלה (חלק B):** המעבר לפר-קמפיין בנה read-side+billing-logic אבל **write-side נשמט** — `campaigns.tier`+`lead_quota` (0114) **write-never** (`create_campaign`/`register_campaign_billing` לא כתבו). התוצאה: מנוע-החיוב קורס (`derive_monthly_price_agorot(None,None)`→KeyError) על קמפיין חי ראשון, ו**כל** מכסות-הפר-קמפיין (1a/1b/1d/2) dormant (`lead_quota=NULL`→unlimited). מוסתר כי clean-slate. ⚠️ **Phase 1c (creative) discrepancy:** מסומן ✅ ב-tasks אך read-side עדיין per-account (`get_billing_period` על `subscriptions`) — יטופל ב-C3. **PR-C1 נסגר (write-side, template):** `subscription_service.get_user_tier_and_quota` + `create_campaign` מעתיק tier/lead_quota בזמן-לידה + migration 0130 (`register_campaign_billing` coalesce מ-subscription — backstop ב-go-live). מפעיל את מנוע-החיוב + כל המכסות. **adversarial (2 reviewers, money+security):** תוקן forgery כספי — `campaigns` billing columns היו פתוחים לכתיבת-משתמש דרך PostgREST (`PATCH discount_pct=100`→חיוב-אפס), migration 0131 (trigger `campaigns_guard_billing` מקפיא ל-non-service_role) + create→admin; + silent-loss guard (`is_chargeable_tier_quota` לפני reserve ב-`charge_campaign` — מונע KeyError→attempt-יתום); + backfill (0130). **re-review MEDIUM (follow-up):** ה-guard החזיר `SKIPPED` בלי terminal → ה-crons (60s) בררו את הקמפיין כל tick → `capture_alert` 1440x/יום + systemic-false (הפרת Cron-11/כלל 8b-1). תוקן: `ChargeOutcome.UNCHARGEABLE` חדש → ה-wrappers מכבים (kill→paused_*, **סימטרי ל-no-token/DECLINED**) — ה-kill מוציא מ-scan → שובר re-pick + edge-triggers את ה-alert (פעם-אחת); fail-safe מול trial-אינסופי-בחינם (write-side שבור לא-נפתר מעצמו, בשונה מ-degradation). + nits: `access_token` הוסר מ-`create_campaign` (dead — ה-INSERT דרך admin) + הערת-0130 (`nullif pending`) תוקנה. **PR-C3a נסגר (gates→per-campaign):** ה-Audit גילה **3 סוגי-gates** (לא 1): `require_premium_active` ×18→`require_premium_campaign` (aggregate על `campaigns.tier`), `whatsapp_line._assert_eligible`, `set_agent_whatsapp` CAS (gate מופרד — check-then-act חוצה-טבלאות, מקובל לערוץ-חינמי) — כולם דרך helpers חדשים (`_active_campaign_tiers`/`user_has_premium_campaign`/`user_has_billable_campaign`); bot-runtime→`campaign_is_premium` (§119 anti-abuse — ליד של קמפיין-basic לא מקבל בוט). מסיר קוראי-`subscriptions.tier` מ-hot-path (blocker C4); `UserNotFoundError` (whatsapp-line) הוסר (dead). adversarial (hot-path gates). **PR-C3b נסגר (creative-quota→per-campaign):** generation/publish/upgrade/revise עברו per-campaign (migration 0132 — 6 RPCs re-keyed `p_campaign_id`, advisory-locks per-campaign פרט ל-revise per-source, CAS/TTL-OR של 0071 נשמר). window→`campaigns.cycle_*`; **`get_billing_period` הוסר** (הקורא-האחרון של `subscriptions.tier`/`current_period_start` ב-quota-path). service: `get_campaign_creative_quota_status` + `get_user_creative_quota_aggregate` (len==1 passthrough → zero frontend); `campaign_id` דרך `get_live_campaign` (interim single-live). endpoint חדש `GET /campaigns/{id}/creative/quota`. helper `list_live_campaign_ids` (canonical; 4 aggregates קיימים מכפילים — follow-up). adversarial (money — locks/CAS/reserve-attribution). **known-gap (C2):** creative-mutations resolve campaign server-side — frontend picker נדחה. **המשך:** C2 (wizard בחירה-פר-קמפיין) · C4 (DROP `subscriptions.tier`/`lead_quota`/3-VIP; **`current_period_start` נשאר** — has_paid_access grace). | Phase C | 🟡 C1+C3a+C3b ✅ · C2/C4 בהמשך |
| premium-uniqueness: resume מול upgrade — a-symmetry (כלל 12) | קמפיין / Upgrade / resume | ✅ **נסגר (התגלה בחקירת Upgrade, תוקן):** `set_campaign_tier_upgrade` אוכף premium-uniqueness (`basic→premium` נחסם אם `'premium' in _active_campaign_tiers` — בוט-הסינון `bot_config` הוא UNIQUE(user_id), שאלון-אחד לחשבון), אבל `resume_campaign` (PR8b-B) **לא** בודק זאת. תרחיש: קמפיין A premium נכבה (`paused_*` — לא-נספר ב-`_active_campaign_tiers`) → המשתמש משדרג B `basic→premium` (מותר, כי A לא-פעיל) → `resume` A → **2 קמפייני premium חיים** חולקים שאלון-בוט יחיד. LOW-MED correctness (**לא-money** — לא כפל-חיוב; clean-slate → לא-נגיש כרגע, דורש רצף ספציפי). **Upgrade פותח את הפרצה** (לפניו לא היה `basic→premium` בכלל → מקסימום קמפיין-premium-יחיד). תיקון-שורש: `ResumeOutcome.PREMIUM_CONFLICT` + מיפוי-router (409) + UX (הודעה: "קמפיין premium אחר פעיל") + סבב-review (resume הוא money-adjacent שנסגר) → **✅ נסגר** (נבחרה חסימת-resume — המשתמש: "ניתן להפעיל רק קמפיין Premium אחד בו-זמנית"): `_fetch_resume_target` +`tier`, `ResumeOutcome.PREMIUM_CONFLICT`, `resume_campaign` בודק לפני credit/charge (fail-fast, read-only), endpoint 409 (`code=premium_conflict`), frontend (`_resumeCampaign` + `submitPayment` polling — permanent-stop כמו declined), 5 טסטים. **נותר known (מקובל, לא-חוסם): premium-guard TOCTOU** — גם ה-check ב-upgrade וגם ב-resume read-only לפני reserve (ללא CAS): 2 flows מקבילים (2× `basic→premium`, או resume+upgrade) על אותו user → שניהם עוברים → 2 premium. **לא double-charge** (כל אחד על קמפיין שלו); `bot_config` UNIQUE(user_id) ממילא מונע 2 בוטים; edge (2 requests סימולטניים). אכיפה אטומית (partial-unique על premium-campaigns per-user) = follow-up אם יידרש. | חקירת Upgrade | ✅ resume נסגר · TOCTOU known |
| "לא יפעל בשבת" — day-parting לא-מחווט ל-Meta | Wizard תקציב | ✅ **נסגר (התגלה בבדיקת-חיווט):** ה-checkbox "לא יפעל בשבת" (`wiz-budget`) נאסף (`shabbat_pause`) ונשמר, אך **לא** נשלח ל-Meta — השימוש היחיד היה רמז-קופי (`copy_service:146`); ב-`_build_ad_set_params` לא היה `adset_schedule` → הקמפיין רץ בשבת **בפועל** (UI מבטיח, backend לא מספק). **תוקן:** `_build_budget` מוסיף `pacing_type: ["day_parting"]` + `adset_schedule` (ראשון-שישי; שבת=Meta-day-6 מושהה) כש-`shabbat_pause` **וגם** `mode='lifetime'` (Meta day-parting נתמך **רק** עם lifetime_budget); `daily` → backstop מתעלם; frontend מאפס את ה-checkbox במעבר ל-daily. helper `_shabbat_adset_schedule` + 3 טסטים. **known (follow-up, לא-חוסם):** (א) אם הסוכן מעלה תקציב אח"כ עם `daily_budget` (`agent_orchestrator:978`) על קמפיין lifetime+shabbat — עלול לדרוס את ה-day-parting; (ב) ⚠️ timezone: day-parting לפי ה-Meta ad-account (הנחה: ישראל); (ג) ⚠️ מספור-ימים (0=ראשון..6=שבת) — לאימות סופי מול Meta sandbox. | בדיקת-חיווט תקציב | ✅ נסגר |
| lifetime budget-raise ע"י הסוכן (בעיה 4) | Agent chat | ✅ **נסגר:** הסוכן חסם העלאת-תקציב על קמפיין תקציב-כולל (lifetime, `_budget_action_guard` → "עדכון ידני"). הורחב לתמוך **בדיוק-לשקל**: `new_lifetime = spent + new_daily × remaining_days` (`spent` מ-`get_campaign_insights().campaign.spend` בשקלים; `end_time` מ-`meta.get_ad_set` **חדש** ב-4 שכבות — meta.py/Protocol/Real/Sandbox; ההמרה שקל→אגורה **פעם-אחת** על new_lifetime). `_assess_budget_increase` הסתעפות lifetime, `_parse_meta_end_time` (⚠️ Meta מחזיר `end_time` כ-**ISO string** בקריאה, לא unix), `update_budget_to_lifetime` (Q-B: quiz שומר `new_daily×days` → `_normalize_daily_budget` הבא = new_daily; ה-quiz snapshot להצגה/feasibility, לא re-pushed ל-Meta). edges: remaining≤0→degradation, Meta-read-fail ב-`_assess` **לפני** record (בלי rollback), error-map transient→503/permanent→degradation/unknown→500. chips ל-lifetime = "הגדל תקציב" (כמו daily). adversarial money-review (2 reviewers) → תיקן **HIGH** (כלל 12ד/11): כשל `get_push_client`/fb_service ב-`approve` יושב באותו try **אחרי** record, אך ה-except תפס רק meta-errors → propagate **בלי rollback** → record יתום → reactive-reminder כוזב ("העלינו תקציב" בזמן ש-Meta לא עודכן). תוקן: `rollback-על-כל-כשל` (`except Exception → _safe_rollback` תמיד) + `isinstance(TransientError)` (fb_service transient → 503), **תקף גם ל-daily** (קוד קיים). ⚠️ **staging-verify (לא ניתן מקומית):** partial-update של lifetime_budget שומר `end_time` + `adset_schedule` (שבת). | חקירת agent-budget | ✅ נסגר (⚠️ staging: partial-update) |
| **double-CHARGE ב-race של targets-שונים** (Upgrade — money residual) | Upgrade / חיוב | 🟠 **money residual מתועד (2 money-reviewers: "accepted-by-design", money-core תקין):** ה-`idempotency_key` כולל target (`{cid}:upgrade:{cycle}:{tier}:{vol}` — תיקון #1, כדי לאפשר שדרוגים-הדרגתיים). לכן 2 שדרוגים **שונים** מקבילים מאותו old-state (A→premium/500, B→basic/1000) מקבלים keys שונים → 2 INSERTs עוברים → **2 J4 גובים במקביל**. ה-CAS-כפול ב-`finalize_campaign_upgrade_charge` (0135) מונע double-**upgrade** (השני CAS-miss→rollback→`unknown`+alert), אבל ה-charge השני **כבר ירד** → refund-ידני (reactive, לא preventive). **מכוסה**: `never-double-upgrade`/`payment-first`/state-correctness נשמרים; רק `never-double-charge` מכוסה ב-detect+alert (כמו `charge_unknown` בכל הפרויקט). **edge כמעט-בלתי-נגיש ב-UI** (כפתור disabled, מסך יחיד; דורש 2 טאבים + targets שונים + אישור סימולטני תוך חלון J4). **פתרון-שורש מוצע (מומלץ, מינימלי, שקוף):** `partial-unique index` על `campaign_charge_attempts (campaign_id, cycle_anchor) WHERE attempt_type='upgrade' AND status='pending'` → attempt pending **יחיד** פר (campaign,cycle) → ה-INSERT השני נכשל (23505 על ה-partial)→`_resolve` לפי key שונה→not-found→`UpgradePendingError` (409 "נסה שוב") **בלי J4**. **שומר שדרוגים-הדרגתיים** (אחרי succeeded=לא-pending → השני עובר). **✅ נסגר (migration 0136 `campaign_upgrade_single_pending_idx`):** ה-INSERT השני 23505→resolve→not-found→UpgradePending(409) בלי J4; טסט `test_upgrade_charge_race_different_target_23505_pending_no_charge`. double-CHARGE נמנע שורשית (preventive, לא reactive). **LOW נוספים (Reviewer-1, לא money-loss):** (1) `proration<=0` double-click אותו-target → 409 (במקום idempotent-success; אין כסף, חלון-שניות ליד cycle_end). (2) 503-אחרי-finalize-מוצלח (DB-blip ב-`_fetch_campaign_vip_row` הטרי) → retry→409 no-op (state נכון — נגבה+שודרג פעם-אחת; שדרוג לא-idempotent-by-nature, בשונה מ-VIP flag). | חקירת Upgrade (2 reviewers) | ✅ נסגר (0136) |

## הושלמו

| פער | מסך | פתרון | סטטוס |
|-----|-----|------|------|
| אישור-המשכיות (§9/§4.2, יום-9/10) | קמפיין-trial | **נסגר (PR8a):** `POST /campaigns/{id}/approve-continuation` → `continuation_approved_at` (ה-gate שרק אחריו `run_trial_expiry` מחייב; token לבדו אינו אישור). דורש billing_token (400 `no_credit`); CAS על trial_active (409); 503 transient. | ✅ נסגר |
| עריכה ידנית של קופי (`PATCH /ads`) | Wizard | **נסגר (הוראה 5):** ה-wizard איפשר עריכה ידנית של headline/body אך לא היה endpoint לשמירה (`regenerate-copy` רק מחדש דרך LLM). נוסף `PATCH /ads/{id}` (`AdEditRequest{headline?,body?,cta?}`, coalesce, draft-gate, בלי LLM/paid) ב-`ads.py`+`copy_service.edit_variant` (ממחזר את `regenerate_variant`). | ✅ נסגר |
| Dream Wizard מקצה-לקצה (קופי+תמונות+פרסום) | Wizard | **נסגר (הוראות 5-7):** ה-wizard חוּוט במלואו מ-mock לאמיתי — `POST /campaigns` → `quiz` → `generate-copy` → 3 מודעות (ערוך `PATCH /ads` / רענן) → **תמונות** (`generate-images` async + polling + `regenerate-image`) → **פרסום** (`POST /campaigns/{id}/push`, 8 שערים, async + polling + `GET /campaigns/{id}` לסטטוס live/failed; מודאל-אישור). | ✅ נסגר |
| `GET /campaigns` (list) — dedup draft חוצה-סשן | Wizard/Dashboard | אין `GET /campaigns` list → סשן wizard חדש יוצר draft חדש (ב-MVP מקובל; `WIZ.campaignId` מונע dup תוך-סשן). עדיין פתוח ל-Dashboard KPIs ול-dedup חוצה-סשן. | 🔴 פתוח |
| קמפיין-נוסף (C2) — backend + frontend | Dashboard/Wizard | **נסגר (C2 מלא — 5 סבבי adversarial money):** `POST /campaigns` מקבל `tier`/`volume`/`confirm_charge_agorot` (MVP: basic/whatsapp; premium→422); `GET /campaigns/price-preview` (discount 15%). **register-gate אטומי (migration 0134 — register = single-source-of-truth ראשון/נוסף, סימטרי ל-CAS על `trial_consumed`):** (א) עמודה `charge_confirmed` — `create_campaign` מסמן קמפיין-נוסף שעבר confirm-amount; register דוחה "נוסף בלי אישור" (`rejected_unconfirmed` → `CampaignChargeNotConfirmedError` permanent → rollback+failed) — **סוגר 2-drafts-race charge-without-consent** (2 drafts בטריאל → השני היה מחויב עד ₪762 בלי הסכמה). (ב) `type↔tier` gate (`rejected_type_mismatch`) — **סוגר §119**: `type='lead'`+מנוי-`whatsapp` = לידים-unlimited במחיר-whatsapp הזול; backstop אטומי **מעל** אכיפת-האפליקציה (`_resolve_campaign_package`) שמדלגת על pending (tier=None ב-create) ו-PostgREST-ישיר (type לא-מוקפא). ה-gate **לפני** ה-CAS → mismatch לא-מבזבז trial. (ג) `lead_quota` מותנה-tier (whatsapp→NULL, לא coalesce → מונע UNCHARGEABLE-kill של קמפיין-נוסף-whatsapp אצל מנוי-basic). guard 0134 מקפיא `charge_confirmed`. **frontend (payment-confirm, דרישת-משתמש — 2 שכבות UX+server):** `wizOpen` → קמפיין-נוסף → `#modal-campaign-package` (בחירת-חבילה → preview server-authoritative → **"אשר וחייב ₪X" מפורש** → בחירת-נכסים [`loadMetaAssets`/`Pages` + whatsapp-number] → `createCampaign(tier)`); שער-אשראי (`!has_paid_access`) → modal-payment → re-entry ל-`wizOpen`→openCampaignPackage (מונע createCampaign בלי tier→rejected). epoch-guard נגד price-drift; confirm=`amount_agorot` (server, לא חישוב-לקוח); type↔tier מסונכרן (APP.type→WIZ.data.goal). קמפיין-ראשון לא-מושפע. **observation (LOW cosmetic):** `ads.status='live'` dangling בטבלה על rejected (מסונן ע"י כל ה-scans על `campaigns.status='live'`→campaign=failed; לא-מזיק, לא-money). | חיווט C2 | ✅ נסגר (C2 מלא) |
| `business_type`/`business_name` ב-quiz | Wizard | **נסגר (3.1.6):** `business_type` **בוטל** (לא נצרך ע"י ה-LLM). `business_name` + `business_description` נאספים ישירות ב-wizard (מסכי business-context: שם+קהל ב-wiz-1, שירות+תיאור ב-wiz-business, בעיה ב-wiz-problem) ונשמרים **per-user** ב-`business_profile` (migration 0091, `GET/PATCH /me/business-profile`). `service_name` + `problem_solved` נשמרים **per-campaign** ב-holding `onboarding_draft` (migration 0092). `extract_business_context` (LLM) צומצם ל-`extract_industry` (industry בלבד מ-business_description+service_name); 3 השדות נאספים ישירות במקום להתחלץ. עמוד עריכה/השלמה: `view-business`. **מסך-הבעיה = שדה-פתוח יחיד** (אוחדו 2 השדות wizprob-q1/q2 ל-`wizprob-answer`; שאלות-ההנחיה = טקסט-עזר). **דילוג חוסם יצירת-קופי**: הוסר ה-fallback `problem_solved=bizDesc` ב-`_buildQuizBody`; שערי-frontend ב-`wizStartLoading`/`wizOpen` (→ `modal-complete-problem` → `view-business`) + שער-backend קיים (`BusinessContextExtractionError` על `problem_solved` ריק). השלמת הבעיה בהגדרות → שחרור החסימה. | ✅ נסגר |
| נעילת-עריכה + נראות פרטי-העסק | פרטי העסק | **נסגר (שיפורים):** (1) **נראות מותנית-דילוג** — פריט-התפריט "פרטי העסק" מוצג רק למי שלחץ "דלג לבינתיים" (`problem_skipped` ב-`onboarding_draft`, migration 0096; דגל מונוטוני). (2) **נעילת-עריכה בקמפיין פעיל** — `editable` ב-`GET /me/business-profile` (server: `campaign_service.has_locking_campaign`, סטטוסים `pending_review/pushing/live/paused`); ה-frontend נועל את 4 השדות + באנר. **אכיפה ב-frontend בלבד, לא 409 על הכתיבה** — ה-wizard חולק את אותם endpoints (PATCH business-profile / PUT onboarding-draft), כך שנעילת-כתיבה גלובלית הייתה שוברת יצירת קמפיין חדש (סטייה מודעת מהתוכנית; הנתון אינו רגיש — own-data). (3) **השפעת service/problem על הקמפיין הבא בלבד** (`onboarding_draft`→wizPrefill→quiz) — **ללא** סנכרון רטרואקטיבי ל-quiz של draft קיים (החלטת מוצר). `has_locking_campaign` הוא query צר, **לא** מחליף את `GET /campaigns` list (נשאר פתוח). | ✅ נסגר |
