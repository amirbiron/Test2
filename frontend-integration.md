# חיווט הפרונטד ל-Backend — מפת אינטגרציה

> **מסמך דינמי.** מסמנים ✅ ככל שמסך מחווט. משלים את `ROADMAP.md` (שמתעד את ה-backend לפי phases) —
> כאן החתך ההפוך: **per-screen**. הפרונטד החי יושב ב-`app/web/` (`index.html` + `api.js`), מוגש same-origin.

## רקע
ה-backend בנוי (~79 endpoints ב-22 routers). הפרונטד נבנה כמוקאפ עם נתוני mock. המשימה: לחווט כל מסך
ל-endpoints האמיתיים. ה-**Creative Studio** חווט ראשון ומשמש **reference pattern** לכל השאר.

## דפוס החיווט הקנוני (reference: Creative Studio ✅)
1. **קריאות API דרך `window.api.apiFetch(path, opts)`** (`app/web/api.js`): מצרף Bearer token (בזיכרון),
   refresh יחיד על 401 (`POST /auth/refresh` עם httpOnly cookie), זורק `ApiError(status, msg)`. FormData
   (העלאות) — בלי כפיית `Content-Type` (הדפדפן קובע boundary).
2. **hook ב-`showView(v)`**: כניסה למסך → טעינת נתונים. הדגם: `loadCreativeView()` → `loadCreativeQuota()`
   + `loadGallery()`.
3. **מצב טעינה לכל פעולה** (קריאות AI/Meta אורכות 10-30s) + **מיפוי שגיאות לעברית** דרך helper מרכזי
   (כמו `_creativeErr`): עדיף את ה-`detail` מהשרת; אחרת לפי קוד (402 Premium / 403 מכסה / 503 לא זמין).
4. **רענון מלא אחרי פעולת כתיבה** (re-fetch) — מבטיח עקביות מול השרת בלי ניהול state ידני.

## טבלת מסכים — סטטוס וחיווט
מקרא: ✅ הושלם · 🟡 חלקי · 🔴 mock · ⚪ סטטי/אין-צורך

| מסך | id (`view-*`/modal) | סטטוס | endpoints (קיימים ב-backend) | פער/הערה |
|-----|----|----|------|------|
| **Creative Studio** | `view-creative` | ✅ | `/me/creative/*` — quota · gallery · generate · upgrade · revise · publish · חדש `GET /campaigns/{id}/creative/quota` | — (reference). **PR-C3b: המכסות עברו per-campaign בשרת** (חלון `campaigns.cycle_*`); `GET /me/creative/quota` נשאר aggregate (shape ללא-שינוי, single-campaign passthrough → **zero frontend change**) + endpoint per-campaign חדש ל-C2. ה-mutations פותרות קמפיין server-side (`get_live_campaign`, single-live); **campaign-picker בסטודיו נדחה ל-C2** |
| **Login / Signup / Hero** | `access-modal` | 🟡 | `POST /auth/login` ✅ · `POST /auth/signup` ✅ · `/auth/refresh` ✅ · `GET /auth/oauth/facebook/start` ✅ · `GET /auth/oauth/facebook/callback` ✅ · `/auth/logout` · `GET /me` | signup מחווט (confirm-email מופעל → תשובה בלי `access_token` → מסך "אשר מייל", לא כניסה אוטומטית); **כפתור "חיבור מהיר באמצעות פייסבוק" מחווט (1.3)** — `startFacebookLogin()`→`/start`, חזרה דרך `_handleOAuthLoginRedirect()` (`/login/success`→`window.api.refresh()`+`_routeAfterAuth`; `/login?error=`→הודעה). go-live מותנה ב-runbook (`docs/deployment/facebook-login-verification.md`); magic-link UI עדיין ללא endpoint → להחליט |
| **My Plan** | `view-myplan` | ✅ | `GET /me/subscription` · `GET /me/quota` · `GET /me/invoices` · `GET /me/invoices/{id}/download` · `POST /me/subscription/cancel` · ~~`PATCH /me/subscription/vip`~~ · ~~`GET /me/subscription/vip/preview`~~ (נוטרל→410; VIP→`PATCH/GET /campaigns/{id}/vip[/preview]`, §8/PR-V4) · **שדרוג-חבילה** `GET /campaigns/{id}/upgrade/preview` + `POST /campaigns/{id}/upgrade` · `PATCH /me/subscription/agent-whatsapp` · `GET /me/subscription/agent-whatsapp/quota` · `GET/PUT /me/subscription/agent-notify` (toggle עדכוני-סוכן + יעד per-account) | חשבונית פייסבוק = mock (אין endpoint — `backend-gaps.md`); **VIP עבר לפר-קמפיין** (§8/PR-V4): toggle/preview/proration פועלים על `campaigns.vip_*` דרך `PATCH/GET /campaigns/{id}/vip[/preview]` (ownership דרך get_campaign/RLS); מצב פר-קמפיין ב-`CampaignBillingSummary.vip_owner_alerts/vip_cancel_at_period_end` (phone לא נחשף — PII). ה-endpoint הישן פר-user נוטרל (410, clean-slate). **ה-panel פר-קמפיין (frontend) נדחה** — יבנה עם מסך-הקמפיין המפורט (ראה backend-gaps). **toggle עדכוני-סוכן בוואטסאפ** (חינם, מונה "X/30", מספר עצמאי `agent_alert_phone` נפרד מ-VIP [0110], prefill מ-`GET agent-notify`, עדכון-מספר במצב פעיל) — `renderAgentWaPanel`; מצב מ-`agent_whatsapp_enabled`. **שדרוג-חבילה פר-קמפיין** (money-UX, Upgrade): קמפיין `paid_active` basic/premium עם יעד-שדרוג → picker יעדים (`_upgradeTargets` תואם `is_valid_upgrade`: same-type, tier/quota לא-יורדים, ≥1 עולה) → preview (`GET .../upgrade/preview`, `proration_agorot` server-authoritative) → **אישור מפורש "אשר וחייב ₪X"** → `POST .../upgrade` (`confirm_charge_agorot`=proration מה-preview, לא חישוב-לקוח). `renderUpgradePanelSection` (single→פאנל/multi→בורר/premium-1000/trial/paused→מוסתר); `_upgradeEnableActive`→`_upgradeConfirm` (double-submit-guard); מיפוי 402/409/503. מצב מ-`CampaignBillingSummary.tier/lead_quota/billing_status` |
| **פרטי העסק** | `view-business` | ✅ | `GET/PATCH /me/business-profile` (שם+תיאור per-user; GET מחזיר `editable`) · `GET/PUT /me/onboarding-draft` (שירות+בעיה + `problem_skipped`) | עריכת זהות העסק + השלמת שאלת-הבעיה למי שדילג ב-wizard (3.1.6). **הצגת-תפריט מותנית-דילוג**: פריט "פרטי העסק" מוסתר כברירת-מחדל, נחשף ע"י `_applyBusinessNavGate` רק כש-`problem_skipped` (נדלק בלחיצת "דלג לבינתיים", מונוטוני). **נעילת-עריכה** כשיש קמפיין פעיל: `editable=false` (server: `has_locking_campaign`) → `_applyBizEditLock` מנטרל את 4 השדות + באנר "בקרוב יתאפשר". שירות/בעיה משפיעים על **הקמפיין הבא** (`onboarding_draft`→wizPrefill→quiz; אין סנכרון רטרואקטיבי). **השלמת-הבעיה משחררת חסימת-קופי**: דילוג בוויזרד חוסם יצירת-קופי (שערי `wizStartLoading`/`wizOpen` → `modal-complete-problem`); מילוי `problem_solved` כאן → שחרור. מילוי דרך `.value` (XSS-safe) |
| **סוכן WhatsApp** | `view-bot` | ✅ | `GET/PUT /bot/config` (הגדרת הבוט per-user: `business_name`/`service_label`/`closing_cta` + `fallback_action`/`fallback_value` + שאלות סינון) · `GET /me/subscription` (נראות-תפריט) | עמוד-הגדרות סוכן ה-WhatsApp (Premium), לעריכה בכל עת. **הצגת-תפריט מותנית-Premium**: פריט "סוכן WhatsApp" מוסתר כברירת-מחדל, נחשף ע"י `_applyBotNavGate` רק כש-`tier==='premium'` (מקור-אמת של ה-PUT gate; **GET ללא gating** — ה-config שלו נשמר גם אחרי downgrade). **אין נעילת-קמפיין** (בשונה מ-view-business — לעריכה בכל עת). איסוף+ולידציה+מיפוי-שגיאה **משותפים לוויזרד** (`_collectBotConfig`/`validateBotConfig`/`mapBotConfigError`, כלל 12 — שני flows מקבילים לא ייסטו). פעולת-סיום: `calendly_link` (קישור https) / `human_handoff` (טלפון E.164); `bot_schedule_appointment` נדחה (v1). שגיאות: 402 לא-Premium · 422 ולידציה · 412 יומן לא-מחובר (`detail=dict` → הסתעפות על status) · 503. שאלות נטענות לפי `order_index` (מיון הגנתי) |
| **Leads list** | `view-leads` | ✅ | `GET /me/leads` (פילטר status + offset pagination) · `PATCH /me/leads/{id}` (סטטוס) | CRM-lite (PR-E2): רשימה + טאבי-פילטר סטטוס + "טען עוד" + קליק→עמוד הליד. פילטר קמפיין נדחה (אין רשימת קמפיינים בפרונטד) |
| **Lead page** | `view-lead` | ✅ | `GET /me/leads/{id}` · `PATCH /me/leads/{id}` (עריכת סטטוס) · deep-link `/leads/{id}` (SPA fallback) | יעד התראת VIP (PR-E). email + שאלות סינון (`screening_answers` ישירות — self-describing, בלי join) + בורר סטטוס (PR-E2). "חזרה" → `view-leads` |
| **Action page** | `view-action` | ✅ | `GET /me/actions/{id}` · `POST .../approve` · `PATCH /me/actions/{id}` (ערוך/draft) · `POST .../refresh` (רענן) · `POST .../feedback` (מצב 2 — הוראה 3) · deep-link `/action/{id}` (SPA fallback) | עמוד-אינטראקציה מקושר-מייל (הוראה 2). `approval_state` נגזר ב-server: `pending` → מצב 1 (3 וריאציות + "אשר והעלה"); `awaiting_feedback` → מצב 2; `done`/`in_progress`/`failed` → מצב 3 (תצוגה + מטריקות, בלי כפתור). AD_REJECTED=אישור (מצב 1). **ערוך+רענן (2.5):** עריכה inline + autosave (`PATCH`→RPC 0094 CAS) + רענן 3 חדשות מה-LLM (`refresh`); flags `editable`/`refreshable` (server, editable⊊pending). **מצב 2 (הוראה 3):** `awaiting_feedback` → "השתפר? כן/לא" (מייל-מעקב `QUALITY_FOLLOWUP` 120ש' ל-low_quality): כן→resolved (סוגר את הסדרה), לא→advanced (יצירת השלב הבא → מצב 1)/exhausted (אחרי שלב 4). **A3 (תמונות חדשות, async):** approve ל-3 סוגי-הרענון (`creative_refresh`/`angle_change`/`offer_change`, פרוד תמיד-ON) מחזיר `status:'generating'` (200, `window_ends_at:null`) → ה-action ב-`in_progress` בזמן שה-worker מייצר תמונות ודוחף → `_pollActionUntilSettled` עושה re-GET כל 3ש' עד `done`/`failed` (תקרת ~2 דק'/ניווט). מתג-חירום (`app_settings.optimization_images_forced_off`) → recycle סינכרוני (`pushed`). 401→resume (`cai_pending_action`) |
| **Series review** | `view-review` | ✅ | `GET /me/sessions/{id}` · deep-link `/session/{id}` (SPA fallback) | מצב 3 (SERIES_RESOLVED) — סיכום הטיפול (הבעיה נפתרה), מידע בלבד. 401→resume (`cai_pending_session`) |
| **פאנל התורים** | `view-appointments` | ✅ | `GET /appointments` (list + פילטר status/from/to) · `POST /appointments/{id}/approve` · `.../reject` · `.../cancel` · `GET /appointments/{id}/ics` · deep-link `/my-appointments` (SPA fallback — **נתיב נפרד** מ-API `/appointments`) · `GET /me/google-connection` (badge 11.3) | פאנל אישור-תורים לבעל-העסק (Premium; `bot_schedule_appointment`, חלק 11). `nav-appointments` נחשף ע"י `_applyBotNavGate` (Premium, כמו `view-bot`). approve→CAS `pending→confirmed`+אירוע Google Calendar+ICS לליד; reject/cancel-with-reason (`window.prompt`). **deep-link `/my-appointments`** (יעד הודעת-הבעלים; **נתיב-SPA נפרד** מ-API `/appointments` שנתפס ע"י ה-router — כמו `/leads/{id}` מול `/me/leads`) → `_handleAppointmentDeepLink`→`openAppointmentsView` (auth-aware: 401→`cai_pending_appointments`+resume ב-`_routeAfterAuth`). **11.3 badge** מצב-חיבור-יומן (מיחזור `_loadGcalStatus`). **toggle רשימה/לוח-שנה** (`_setApptView`): גריד-חודש (`_renderApptCalendar`, RTL, כותרות עבריות) ממחזר את אותו `GET /appointments` עם `from`/`to` (גבולות-החודש; בלי backend), ניווט חודש, קליק על תור→מודאל-פרטים+פעולות. טלפון-לחיץ (wa.me) + סינון pending/confirmed/cancelled/rejected. **תאריכים גלויים = DD/MM/YYYY** (`_fmtApptDate`), פנימי-בלבד ISO. render XSS-safe (`textContent`/`createElement`). שגיאות 402/404/409/503. *(pagination + `duration_minutes` ב-approve — נדחו, דורשים backend)* |
| **Dashboard** | `view-home` | 🟡 | `GET /me/quota` (לידים סה״כ) | רק "לידים סה״כ" מחווט; שאר ה-KPIs (CPL/spend/קמפיינים) + כרטיסים — פער (`backend-gaps.md`) |
| **Payment + חבילה** | `modal-payment` / `ob-finish` / `wizOpen` | ✅ | `PATCH /me/subscription` (חבילה+VIP, `ob-finish`) · `GET/PATCH /me/billing-profile` · `POST /me/subscription/start-billing` (Pelecard iframe) · `GET /me/subscription` (gate+polling) | **מחווט אמיתי**: `submitPayment` → `saveBillingProfile` → `startBilling` → iframe Pelecard (הכרטיס מוזן שם, PCI-minimal) → polling על `has_paid_access` (מקור-אמת=backend/IPN, לא ה-iframe). gating ביצירת קמפיין (`wizOpen`) ופג-תוקף (`checkTrialStatus`, status=`expired`) מ-`has_paid_access`. ה-mock (`cai_trial_*`) הוסר. מסלול-1 VIP ✅ |
| **מסך-נעילה + חידוש + יום-28** (§6/§4.3, PR8b) | `checkTrialStatus` / `_renderLockScreen` / banner / `modal-payment` | ✅ | `GET /me/subscription` (account_access_status) · `GET /me/campaigns` · `POST /campaigns/{id}/resume` · `.../shutdown` · `.../cancel-shutdown` · `POST /me/subscription/start-billing` | **נעילת-חשבון (§6.3)**: `account_access_status='locked'` → `_renderLockScreen` (XSS-safe) — רשימת-קמפיינים-כבויים + כפתור-"חדש" לכל אחד. **חידוש (§6.1)** `_resumeCampaign`: `resume`→`resumed`/`reactivate_pending`(200)→`reload`; `no_credit`(400)→`modal-payment` resume-mode (polling **על `resume` עצמו**, idempotent — לא `has_paid_access`) → charge→reactivate Meta; `declined`(402)→הודעה. **יום-28 (§4.3)** `_maybeShowRenewalBanner` (על `GET /me/campaigns` הקיים): `paid_active` שמתקרב ל-`cycle_end` (≤48h) → banner "יתחדש אוטומטית" + `[אל תחדש]`→`shutdown`(paid_active→pending_shutdown); `pending_shutdown` → banner "ייכבה ב-X" + `[התחרטתי]`→`cancel-shutdown`. dismiss ל-session פר-קמפיין+mode. auto-renew עצמו ב-cron (§4.3, ה-charge ב-`cycle_end`; ה-banner=תזכורת+ביטול). **פער — read-only לקמפ-כבוי בדשבורד (§6.1 מצב-1)**: לא מומש (דורש multi-campaign dashboard view; ה-backend מוכן — `resume` עובד לכל קמפ-כבוי). כרגע חידוש זמין דרך מסך-הנעילה (מצב-2) + banner יום-28. |
| **Meta connection** | (בתוך wizard) | ✅ | `GET /me/meta-assets` (render חשבונות+עמודים) · `GET /auth/meta/start` (התחבר) · `GET/PUT /me/onboarding-draft` (מסלול) | OAuth-return ✅ (scope תוקן + SPA fallback + draft) · בחירת Page ✅ (`loadMetaPages`, stage שלישי, בלי fallback) |
| **Dream Wizard** | `modal-dream-campaign` | ✅ | `POST /campaigns` · `.../quiz` · `.../generate-copy` · `POST /ads/{id}/regenerate-copy` · `PATCH /ads/{id}` · `POST /campaigns/{id}/generate-images` (async) · `GET /me/jobs/{id}` (polling) · `POST /ads/{id}/regenerate-image` · `POST /campaigns/{id}/push` (פרסום) · `GET /campaigns/{id}` (סטטוס) · `GET/PUT /me/onboarding-draft` · `PUT /bot/config` (שלב wiz-bot — Premium) · `GET/PUT /me/subscription/agent-notify` (שלב wiz-contact) | **ה-wizard מלא end-to-end (הוראות 5-7):** create-campaign → quiz → generate-copy → 3 מודעות (ערוך `PATCH /ads` / רענן) → **תמונות** (`generate-images` job + polling + `regenerate-image`, כשל-חלקי) → **פרסום** (`push`, 8 שערים, async): מודאל-אישור → polling → `GET /campaigns/{id}` → status `live` (🎉 toast + `showCampaignLiveState`) / `failed` (ללא retry — re-push=409). **job='done' ≠ הצלחה** → תמיד GET campaign. 409 דו-משמעי → GET campaign (draft=Meta-disconnect / pushing / live). **שלב wiz-bot (Premium+leads):** לפני יצירת-הקמפיין — הגדרת סוכן ה-WhatsApp (service_label/closing_cta/שאלות/פעולת-סיום) נשמרת ב-`PUT /bot/config` דרך `saveBotThenContinue` (ולידציה מקומית → save → `wizStartLoading`; כשל→נשארים בשלב עם הודעה, לא ממשיכים). ולידציה+איסוף משותפים עם `view-bot` (Chunk 5). **שלב wiz-contact (0110, עמוד לפני "טופס לידים"):** יעד עדכוני-הסוכן per-account — מייל (חובה, prefill מ-`GET agent-notify`/`/me`) + וואטסאפ (אופציונלי) → `PUT /me/subscription/agent-notify` ב-`wizSubmitContact` (נפרד מ-VIP/הרשמה; ריק→בלי וואטסאפ). **C2 — קמפיין-נוסף:** לפני ה-wizard, `wizOpen`(hasExisting+has_paid_access)→`modal-campaign-package`: בחירת-חבילה (basic/whatsapp+volume)→`GET /campaigns/price-preview` (discount 15%, server-authoritative)→**"אשר וחייב ₪X" מפורש**→בחירת-נכסים (`loadMetaAssets`/`Pages`+whatsapp-number)→`createCampaign` עם `tier`/`volume`/`confirm_charge_agorot`. שער-אשראי (`!has_paid_access`)→modal-payment→re-entry ל-`openCampaignPackage`. epoch-guard נגד price-drift; confirm=amount_agorot (server); register-gate בשרת (`charge_confirmed`) = שכבת-ביטחון; קמפיין-ראשון לא-מושפע. |
| **Diagnostics** | `view-diag` | 🔴 | אין GET ל-`diag-list` (improvement-history) — הנתונים ב-`optimization_monitor_service` internal | **אין מקור backend; תלוי קמפיין live** (`backend-gaps.md`) |
| **Context** | `view-context` | 🔴 | אין — mock טהור (6 items hardcoded) | **אין מקור backend כלל** (endpoint/table/service); תלוי quiz/agent/live (`backend-gaps.md`) |
| **Onboarding radar** | `radar-overlay` | ⚪ | — | אנימציית UX בלבד; אין צורך אמיתי |
| **Legal** | `view-legal` | ⚪ | — | תוכן סטטי |
| **תמיכה** | `view-support` + `/admin/support` | ✅ | user: `POST /me/support/threads` · `POST .../threads/{id}/messages` · `GET .../threads` · `GET .../threads/{id}/messages` · admin: `GET /admin/support/threads` · `GET .../threads/{id}/messages` · `POST .../threads/{id}/reply` | **SUPPORT_ESCALATION_PLAN סבב-1** — thread דו-כיווני משתמש↔צוות (מחליף bug-reports). כפתור-תמיכה גלובלי (nav `data-view="support"`) → מסך-צ'אט (`loadSupport`/`sendSupport`; בועות `textContent` XSS-safe, **לא** innerHTML): thread-יזום אחד מתמשך פר-user (partial-unique). כל הודעת-user → notification(channel=`telegram`, anchor=`msg:{id}`) לצוות (degradation: בלי env→skip שקט). פאנל-צוות `/admin/support` (page_router shell לא-מוגן + data מוגן X-Admin-Token): רשימת threads (+email best-effort) → מסך-שיחה → תשובת-`staff`. אסקלציות-אוטומטיות (4 תנאי Meta) = סבב-2 |

## פערים שהצלבת frontend↔backend חשפה

### (א) endpoints שהפרונטד צריך אך חסרים / לא ודאיים
- **`GET /campaigns` (list)** — ל-Dashboard (KPIs + כרטיסים) בלבד. כרגע קיים רק `GET /campaigns/{id}`. לבדוק אם
  MVP חד-קמפייני מסתדר עם `campaign_id` שמור, או שצריך endpoint רשימה. (**צ'אט-האופטימיזציה כבר לא תלוי בזה** —
  הוא שואב את הקמפיינים הפעילים מ-`GET /me/agent/conversations`, הוראה 4.)
- **magic-link signup** — ה-UI קיים (`sendMagicLink`) אך אין endpoint. להחליט: להסיר את ה-UI, או למפות
  ל-`POST /auth/signup` הקיים.
- **חשבונית פייסבוק** (My Plan, `requestFbInvoice`) — לבדוק אם יש endpoint או שהזרימה ידנית.

### (ב) endpoints קיימים בלי מסך בפרונטד (ה-backend בנה, המוקאפ לא כיסה)
- **Leads / CRM:** `GET/PATCH /me/leads` — אין מסך ניהול לידים.
- **Appointments + Scheduling:** פאנל **אישור-התורים** (`/appointments` — list/approve/reject/cancel/ics) **מחווט ✅** (חלק 11; ראה שורת `view-appointments` בטבלה + deep-link + badge 11.3). שאר ה-endpoints של תצורת-התזמון (שעות-עבודה, ימים-מיוחדים, זמינות — premium) עדיין ללא מסך ייעודי.
- **Agent chat (צ'אט-האופטימיזציה):** מחווט ✅ (הוראה 4). `GET /me/agent/conversations` = מקור הקמפיינים הפעילים
  (אין צורך ב-`GET /campaigns`); `POST .../diagnose` (high_cpl; low_quality **תלת-שלבי** — subcategory→value-turn
  [אזור/מחיר ל-filter_addon]→ניתוח); `POST .../propose`
  → לינק שמפעיל `openActionView(action_id)` → עמוד-הפעולה `/action/{id}` (אישור/עריכה/רענן ב-view-action — DRY);
  `POST .../messages` (צ'אט חופשי + STAGE_C ג'-2). meta_rejection מטופל דרך מייל→view-action (לא בצ'אט). האישור
  **delegated** לעמוד-הפעולה (לא inline). `quota-status` → כרטיס המכסה.
  בעיה 4 ("מעט פניות", budget_mismatch — מחווט ✅): `POST .../budget/{preview|approve|decline|force-creative}`
  (כולם `DiagnoseResponse` type=`budget_options`). מסלול א': הגדל תקציב→`preview`→"מאשר"→`approve` (double-confirm;
  "מאשר" **gated** ל-`approve` רק אחרי preview — `CHAT.budgetConfirm`). מסלול ב': השאר→`decline`→
  `[נשאיר את המודעות / בכל זאת קריאייטיב]`; "בכל זאת" **gated** ל-`force-creative` (יוצר מודעות = spend —
  `CHAT.creativePending`). לולאת reactive מקדימה תזכורת מההחלטה הקודמת (תאריך + ערכים + `cpl_before`/`cpl_now`).
- **תמיכה (support): מומש ✅ (סבב-1).** ה-bug-reports הישן **הוסר** (מנגנון איסוף-פאסיבי; migration 0137 DROP),
  הוחלף במערכת-תמיכה דו-כיוונית (`docs/SUPPORT_ESCALATION_PLAN.md`): מסך `view-support` (כפתור גלובלי → thread),
  פאנל-צוות `/admin/support`, וערוץ-טלגרם (notification channel חדש — פאזה 4). ראה שורת **תמיכה** בטבלה למעלה.
  אסקלציות-אוטומטיות (4 תנאי-כשל Meta → escalate + הודעה-אחידה) = **סבב-2** (טרם מומש).

→ לכל פריט: החלטה מפורשת — **לבנות מסך**, **לדחות**, או **out-of-scope ל-MVP**.

## סדר עבודה מומלץ (פשוט→מורכב, תוך בניית הדפוס)
1. ✅ **Creative Studio** — הושלם (reference).
2. ✅ **My Plan** — מחווט (מנוי + מכסה + חשבוניות + ביטול). חשבונית פייסבוק נשארה mock (`backend-gaps.md`).
3. **Dashboard (home)** — מסך מרכזי; דורש הכרעה על `GET /campaigns`.
4. **Payment + בחירת חבילה** — onboarding קריטי (Pelecard iframe).
5. **Meta connection** — תלות ל-wizard.
6. **Dream Wizard** — הגדול והמורכב (jobs + polling); תלוי 4-5.
7. **Diagnostics / Context** — agent flow; אחרי שיש קמפיינים חיים.

> ההיגיון: בונים את דפוס החיווט על מסכים פשוטים, ומשאירים את ה-wizard (10+ endpoints) לאחר שהתשתית
> (auth, campaigns, Meta) מחווטת. הסדר פתוח לשינוי לפי עדיפויות עסקיות.

## סטטוס נוכחי
- **✅ Creative Studio** — מחווט end-to-end (backend Session 7.7Q + פיצ'ר revise; frontend מלא).
- **✅ My Plan** — מחווט (מנוי + מכסה + חשבוניות + ביטול).
- **🟡 Dashboard / Meta** — חלקי: "לידים החודש" + render חשבונות. שאר ה-KPIs ו-OAuth-return — פערים (`backend-gaps.md`).
- **⏸️ Payment / Wizard / Diagnostics / Context** — חסומים. הכרעה: **עוצרים את חיווט-הפרונטד כאן** — כל מה
  שלא-חסום כבר חובר. הצוואר בקבוק: **Pelecard credentials → trial → Wizard → קמפיין live → נתוני agent**
  (Diagnostics/Context). Payment חסום על Pelecard; Wizard על Meta+Payment (business-context + `fb_page_id` נסגרו; הקופי mock);
  Diagnostics/Context חסרי-מקור backend לחלוטין (ראה `backend-gaps.md`).
- המסמך מתעדכן בסוף כל מסך שמחווט.
